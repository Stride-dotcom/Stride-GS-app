# GS Inventory — Session History (Archived)

> Compressed one-liner summary of every builder session. Read this when you need historical context on when a feature was built or a decision was made. For active work see the main `CLAUDE.md` and `Docs/Stride_GS_App_Build_Status.md`.

---

## 2026-03

**2026-03-20 → 03-22:** Billing schema standardization, Ledger Row ID migration, invoice PDF via Google Doc templates, Import Inventory tool, storage charge dedup, non-destructive headers, Will Call email PDF, COD banner, T&C updates.

**2026-03-25:** Task Board shared handler refactor (4 shared handlers with SH_ prefix), operation-specific idempotency markers, portable PDF generation, discount logic fix.

**2026-03-26:** QR Scanner + Label Printer web apps, Autocomplete DB (per-client Sidemark/Vendor/Description), warehouse Locations system (CB centralized → client Location_Cache), REPAIR_APPROVED/DECLINED email templates.

**2026-03-26:** Stride GS App (React) — 11 pages built (Login, Dashboard, Inventory, Receiving, Tasks, Repairs, Will Calls, Billing, Payments, Claims, Settings). Live at https://www.mystridehub.com. TanStack Table, Lucide icons, Rivian design.

**2026-03-27:** Phase 7A read API (StrideAPI.gs v5.0.0) — 11 endpoints (getClients thru getClaims + 4 user endpoints). All pages wired to live data with mock fallback. Server-side client isolation on all endpoints. Phase 6 Auth Batch 1 backend complete (Users tab, auto-create on onboard).

**2026-03-27/28:** Task creation refactor (menu-driven batch, deferred Start Task), per-item folders eliminated, Set Release Date dialog, Complete Will Call dialog update, Import fixes (IMP-format, photo URLs), duplicate Item ID protection, email template cache, Master Price List sync, admin remote execution tools.

**2026-03-28 session 2:** Shipments page + detail panel. Phase 7B parity tests. Phase 7B #1 completeShipment (v6.0.0) + #2 completeTask (v7.0.0). Browser-based script injection technique established.

**2026-03-28 session 3:** Phase 7B #3 sendRepairQuote + #4 respondToRepairQuote (v8.0.0→v9.0.0). RepairDetailPanel wired for full quote flow.

**2026-03-28 session 4:** Phase 7B #5 completeRepair (v10.0.0). All 3 Repairs write endpoints complete.

**2026-03-29 session 5:** Phase 7B #6 createWillCall (v11.0.0). CreateWillCallModal wired.

**2026-03-29 session 6:** Phase 7B #7 processWcRelease (v12.0.0). Full + partial release with inline checkbox selector.

**2026-03-29 session 7:** Phase 7B #8 transferItems (v13), #9 generateStorageCharges (v14), #10 generateUnbilledReport (v15). Billing page wired. Apps Script REST API push technique established.

**2026-03-29 session 8:** Phase 7B #11 createInvoice (v16.0.0) + #12 resendInvoiceEmail. Full invoice flow: Google Doc template → PDF → Drive → Consolidated_Ledger → client Billing_Ledger → email.

**2026-03-29 session 9:** Phase 7B #13-15 onboardClient/updateClient/syncSettings (v17.0.0). OnboardClientModal dual create/edit mode. Settings Clients tab wired.

**2026-03-29 session 10:** Phase 7B #16 batchCreateTasks (v18.0.0). CreateTaskModal. v19.0.0: auto-upsert client user row on onboard. Auth plan locked: email+password only. Phase 6 auth complete.

**2026-03-29 session 11:** Phase 7B #17 startTask (v21.0.0) — Drive folder, Work Order PDF, hyperlinks, idempotency. Settings Maintenance tab added (8th tab). Phase 7B COMPLETE.

**2026-03-29 session 12:** Claims backend (v22.0.0) — full module: withAdminGuard_, handleGetClaims_, handleGetClaimDetail_, handleCreateClaim_, handleAddClaimItems_, handleAddClaimNote_, handleRequestMoreInfo_, handleSendClaimDenial_, handleGenerateClaimSettlement_, handleUploadSignedSettlement_, handleCloseClaim_, handleVoidClaim_, handleReopenClaim_. 5 claim email templates (inline fallback).

**2026-03-29 session 13:** Claims parity audit + fixes (v22.1.0) — 6 critical runtime bugs resolved: response structure, field renames, settlement response fields, optional params. React: inline forms for Request More Info + Reopen.

**2026-03-29 session 14:** Claims email templates (5 new .txt files). DOC_SETTLEMENT.txt layout reference. EMAIL_TOKEN_REFERENCE.txt updated. push-cb.mjs + push-master.mjs created. setup-auth.mjs v3.1.0 broader OAuth scopes.

**2026-03-30 session 15:** Remote Admin Bulk Actions — Web App `doPost()` pivot (scripts.run blocked by 403). RemoteAdmin.gs v1.1.0, run-remote.mjs v1.2.0. All 4 commands verified on all 5 clients: health-check, update-headers, install-triggers, refresh-caches.

**2026-03-30 session 16:** Forensic code audit (part 1) — static analysis of all 12 React pages. Identified 17 issues. Created PHASE_7_FORENSIC_TEST_REPORT.md + PHASE_7_REPAIR_CHECKLIST.md.

**2026-03-30 session 17:** 15 QA bugs fixed. Critical: `useClients.ts` was generating fake abbreviated IDs — root cause of "Illegal spreadsheet id" errors. Loading spinners. RepairDetailPanel Item card uses repair fields. Single-click detail panels. Dashboard task clicks open detail panel.

**2026-03-30 session 18:** Cancel Will Call (v23.2.0) + Print Release Doc (v23.3.0) wired. Shipments detail panel mock data lookup removed.

**2026-03-31 session 19:** All mock data removed from React app. UniversalSearch rewritten. Password reset flow fixed. StrideAPI.gs v24.0.0 — full email + PDF parity: SHIPMENT_RECEIVED + DOC_RECEIVING, INSP_EMAIL/TASK_COMPLETE, DOC_REPAIR_WORK_ORDER, DOC_WILL_CALL_RELEASE. New helpers: api_buildItemsHtmlTable_, api_generateDocPdf_, api_mergeEmails_.

**2026-03-31 session 20:** 7 QA bug fixes (class default, service codes, autocomplete, WC pre-selected items, ProcessingOverlay, task completion UX). "In Progress" task status added across all systems: StrideAPI.gs v24.1.0, Code.gs v4.1.0, task board v1.5.0. push-taskboard.mjs + push-stax.mjs created — all 6 Apps Script projects now deploy via terminal.

**2026-03-31 session 21:** Full E2E QA pass. Shipments page browser freeze fixed — `getShipments` no longer reads Inventory; new `getShipmentItems` endpoint (v24.2.0→v24.3.0). Inventory detail panel: related records, action buttons, item history. Login UX: three-phase flow.

**2026-03-31 session 22:** Performance overhaul — server-side CacheService caching (600s TTL, chunked >100KB) + cache invalidation (v26.0.0). Batch endpoint `handleGetBatch_` returns all 6 entity datasets in one call (v26.1.0). BatchDataContext. StartTask race condition fix (v26.2.0) — LockService + conflict detection. Sort column highlighting.

**2026-03-31 session 23:** Storage billing fix — `recalcUnbilledRates_` was overwriting correct STOR rates. `loadClassSizes_` header mismatch fixed. Task Board checkbox fix. Async remote admin — fire-and-forget triggers with sync-status polling. Non-destructive update-headers (v4.3.0). Flat Drive folder structure — eliminated nested entity folders. Start Repair endpoint (v26.3.0).

**2026-03-31 session 24:** Folder buttons standardization + Fix Missing Folders tool. Shared `FolderButton` component. v26.7.0: removed `readFolderUrls` guard, hardened `api_readIdFolderUrls_` for partial rich-text hyperlinks. New `fixMissingFolders` POST endpoint.

**2026-03-31 session 25:** 11 queued fixes. Item History section reordered. Transfer pre-select. Folder buttons in Item Details. Start Task checkbox stays TRUE. Task completion LockService. Location autocomplete in TaskDetailPanel. Column drag-to-reorder on Tasks + Repairs. Clickable item card in Task/Repair panels.

**2026-03-31 session 26:** Role-based access + per-user table persistence. 3-tier nav: admin=full, staff=7 items, client=7 items. `RoleGuard` in App.tsx. Settings admin-only. `useTablePreferences` keyed by user.email. Column drag-to-reorder on ALL 8 TanStack Table pages.

## 2026-06

**2026-06-03 DT item-ID-swap detection note (PR [#609](https://github.com/Stride-dotcom/Stride-GS-app/pull/609), dt-sync-statuses v22 + migration 20260603180000):** Investigation of "does changing a DT item ID break sync/release" found: changing to ANOTHER valid active inventory ID already auto-releases (live trigger dt_order_items_resolve_inventory_id_trg, migration 20260512230000, stamps inventory_id on the orphan insert) — only ID changes to arbitrary/non-inventory text orphan + stay stuck. The gap was visibility: the re-link is silent. v22 reads inventory_id back from the orphan insert and, on delivery orders, posts an idempotent entity_notes review note (postIdRematchNote_) so staff verify the swap. Purely additive — trigger owns the link/release. Companion migration widens entity_notes.note_type CHECK to allow dt_id_rematch:% AND auto_release_skipped:% (the latter fixes a latent 23514 in release-on-dt-finished's postSkippedNote). Abandoned a redundant inventory_id-restamping approach before merge. Migration applied via MCP, EF via Supabase CLI.

**2026-06-03 Client rename propagates to per-tenant Settings CLIENT_NAME (PR [#600](https://github.com/Stride-dotcom/Stride-GS-app/pull/600), StrideAPI v38.260.0):** Justin renamed "Arkitektura Forty" → "Arkitektura" but inspection emails still showed Forty. Cause: rename routes through update-client-sb → writeThroughReverse → __writeThroughReverseClients_, which wrote SB + CB Clients but not the per-tenant Settings tab CLIENT_NAME (which api_getSettings_ → handleCompleteTask_ inspection email reads). CLIENT_FIELDS_.clientName lacks a clientSettingsKey, and unlike email/contact/phone it had no explicit writeSetting_ line in the writer. Added `writeSetting_("CLIENT_NAME", row.name)` alongside the existing three identity writes. Backfilled Arkitektura's Settings tab post-deploy via an update-client-sb re-fire (verified no writethrough_reverse failure). Web App v558.

**2026-06-02 InsuranceBlock pre-fills from refresh intake (PR [#594](https://github.com/Stride-dotcom/Stride-GS-app/pull/594), `fix/intake/insurance-block-prefill`):** Follow-up to PR #593 — that fix wrote insurance on Save & Sync, but operators still saw an empty "$ ___" input on modal open (InsuranceBlock reads from client_insurance only). Now: PendingIntakeOverride gains insuranceChoice + insuranceDeclaredValue; OnboardClientModal threads them to InsuranceBlock; InsuranceBlock pre-fills seedDraft from intake value when client_insurance row is empty + operator hasn't typed; empty-state hint now reads "Client elected Stride coverage on YYYY-MM-DD with a declared value of $X". eis_coverage treated as stride_coverage. own_policy + pre-intake clients fall through to original UI. Reported by Justin on AubreyMaxwell.

**2026-06-02 Intake insurance propagated to client_insurance (PR [#593](https://github.com/Stride-dotcom/Stride-GS-app/pull/593), `fix/intake/propagate-insurance-to-client`):** Reported by Justin — refresh intakes had insurance_choice + insurance_declared_value captured but neither the auto-apply EF nor the manual Apply Refresh handler wrote them to client_insurance, so InsuranceBlock showed $0 and the auto-billing cron either skipped or billed $0 for affected tenants. Both code paths now upsert client_insurance after the clients UPDATE (stride_coverage → upsert with rate from service_catalog.INSURANCE; own_policy → deactivate existing row; eis_coverage treated as legacy alias for stride_coverage). UPDATE path preserves inception_date/next_billing_date/monthly_rate_per_10k so in-flight cycles aren't reset. Audit identified 2 already-activated tenants needing backfill (Arkitektura & B&B Italia SF + COUCH Seattle, both $10K stride_coverage) — backfilled via direct INSERT. EF deployed via Supabase CLI; React via canonical.

## 2026-05

**2026-05-31 Failed Operations Retry for EF-direct actions (PR [#583](https://github.com/Stride-dotcom/Stride-GS-app/pull/583), `fix/delivery/retry-ef-direct-actions`):** Direct follow-up to PR #582. Operator clicked Retry on a dt_push_order_after_pu_sync row and got "Unknown POST action: dtPushOrderAfterPuSync" because the retry handler hardcoded everything through apiPost → GAS, but that action_type is written by an Edge Function (dt-sync-statuses) and has no GAS handler. New EF_RETRY_MAP at top of retry() dispatches specific action_types directly to supabase.functions.invoke() with the right body shape (matches original dt-sync-statuses v21 call). Error path uses the same err.context.text() body-extraction from v21. Adding new EF-direct retry paths is now a one-entry map change.

**2026-05-31 dt-sync-statuses pu_propagate error body (PR [#582](https://github.com/Stride-dotcom/Stride-GS-app/pull/582), `fix/delivery/dt-sync-error-body`):** Triggered by 2026-05-30 incident — 3 `dt_push_order_after_pu_sync` gs_sync_events rows had identical generic "Edge Function returned a non-2xx status code" messages and the actual dt-push-order error had to be inferred from logs. v21 of dt-sync-statuses reads `err.context.text()` (the underlying Response carried on supabase-js's FunctionsHttpError) and appends the body to `gs_sync_events.error_message`. Body read wrapped in try/catch so a consumed-stream edge case degrades to msg-only. Edge function deployed via Supabase CLI.

**2026-05-29 Impersonation JWT sync for RLS (PR [#574](https://github.com/Stride-dotcom/Stride-GS-app/pull/574), `fix/impersonation/jwt-tenant`):** Staff impersonating a client saw empty Inventory / Tasks / Repairs / Will Calls. PR #474's true-session-swap impersonation correctly swaps the live JWT to the target via verifyOtp but suppresses handleSession via impersonationSwapRef — and handleSession is where user_metadata.clientSheetId + accessibleClientSheetIds normally get synced into the JWT (AuthContext.tsx:290-301 and :384-394). The user_has_tenant_access RLS helper (migration 20260504210000) reads both fields straight off auth.jwt() -> 'user_metadata', so when the target's stored raw_user_meta_data is empty (clients who haven't signed in via password) or stale (after tenant-assignment changes), every gated row silently filters out. Fix: after resolveUserFromApi returns the target's profile and BEFORE setAuthState, mirror the same in-sync check + supabase.auth.updateUser({data}) block. Sync only fires when out of sync; resulting USER_UPDATED is absorbed by the impersonationSwapRef guard at onAuthStateChange (line ~518). Best-effort try/catch matches the two pre-existing sync sites — a transient blip doesn't bounce impersonation. Refresh-during-impersonation path was already correct (goes through handleSession which already syncs). Code review (Opus 4.7 general agent) returned no Critical / no Important. tsc + build clean. React deployed from dedicated deploy clone `/c/dev/stride-deploy-impersonation` per the 2026-05-21 dist-race memory. No GAS / migration / Edge Function deploy required.

**2026-05-28 Fabric Protection category + tasks + bottom-sort (PR [#560](https://github.com/Stride-dotcom/Stride-GS-app/pull/560), `feat/pricelist/fabric-protection-category-tasks`):** Three coordinated changes from operator request. (1) PriceList.tsx ALL_CATEGORIES gained 'Fabric Protection' so the sidebar tab appears (was missing from this hardcoded list while present in SHAREABLE_CATEGORIES + the type union). (2) Migration flips show_as_task=true for all 11 FAB_* services — "all-on by policy" for this category. Verified 11/11 enabled post-apply. (3) CreateTaskModal partitions task picker into [nonFabric, fabric] and concats so the 11 rarely-used fabric codes don't push everyday INSP/ASM below the fold. Fabric identified via category match — no hardcoded code list. Pairs with PR #559.

**2026-05-28 CreateTaskModal honors service_catalog.show_as_task (PR [#559](https://github.com/Stride-dotcom/Stride-GS-app/pull/559), `fix/tasks/create-modal-show-as-task`):** The Create Tasks modal was surfacing every priceList row except a small denylist, so shipping/billing-only services (Blanket Wrap, Custom Crating, Photo Documentation, etc.) appeared as if they were task types. Fix: gate non-CORE codes on the matching service_catalog row having both active=true and showAsTask=true. CORE_TYPES (INSP, ASM) still bypass for floor coverage. Catalog-empty fallback preserves legacy behavior during load. One-spot change to taskTypes memo.

**2026-05-28 OrderPage shows item-ID count + cubic volume (PR [#557](https://github.com/Stride-dotcom/Stride-GS-app/pull/557), `feat/delivery/order-piece-cubic-stats`):** Items card header on OrderPage now reads "N pieces · L item IDs · X.X ft³". Relabelled the existing "lines" to "item IDs" per operator vocabulary; added cubic volume sum (qty × per-unit cubic_feet from dt_order_items, per PR #543). Cubic suffix is suppressed when total rounds to 0 so all-ad-hoc orders don't display "0.0 ft³". One-spot edit in OrderPage.tsx:778. tsc + build clean.

**2026-05-28 Repair emails actually reach customer (PR [#556](https://github.com/Stride-dotcom/Stride-GS-app/pull/556), `fix/repairs/customer-email-recipients`):** Audit triggered by Justin found REPAIR_QUOTE / REPAIR_APPROVED / REPAIR_DECLINED / REPAIR_COMPLETE were going only to info@stridenw.com since 2026-05-14 (repair flag fleet-flip). Two causes: send-email's expandToken() didn't implement {{CLIENT_EMAIL}}; REPAIR_APPROVED + REPAIR_DECLINED templates didn't even list the token in recipients column. Fix: thread tenantId through resolveRecipients → expandToken; new CLIENT_EMAIL case loads clients.notification_contacts JSONB array with fallback to clients.email. Migration adds {{CLIENT_EMAIL}} to the two templates that were missing it. All 3 repair EF callers already pass tenantId — no caller-side change. Edge function deployed via MCP (v9 ACTIVE); migration applied.

**2026-05-28 DO modal inventory picker search includes Reference + Room (PR [#553](https://github.com/Stride-dotcom/Stride-GS-app/pull/553), `fix/delivery/do-modal-search-reference`):** CreateDeliveryOrderModal item-picker's filter predicate was missing the visible Reference column and the non-displayed Room metadata. Two-clause additive fix to the OR chain in the `filteredItems` memo — picker now matches against every visible column except Qty (numeric, not useful) plus Room. Both fields pre-existed on LiveItem; no schema change. tsc + build clean.

**2026-05-28 RUSH tasks auto-High + due-today; High → due-today rule extended (PR [#549](https://github.com/Stride-dotcom/Stride-GS-app/pull/549), `feat/warehouse/rush-and-high-due-date`):** Two related task-priority behaviors. (1) StrideAPI.gs v38.240.0: handleBatchCreateTasks_ forces priority='High' + due_date=today (PT) when svcCode==='RUSH', overriding any caller-supplied dueDate/priority and any RUSH SLA-hours entry. Server-side default covers every current and future task-creation path. (2) Tasks.tsx + TaskDetailPanel.tsx: extends the Dashboard rule from PR #399 (High auto-sets due_date=today) to the Tasks page chip and the detail panel priority span. Today computed via Intl.DateTimeFormat('en-CA', timeZone: 'America/Los_Angeles') to match the GAS Utilities.formatDate output. Code-review caught one Important bug: applyTaskPatch in useTasks.ts is a REPLACE, not a merge — initial draft called it twice with {priority} then {dueDate}, second call clobbered first. Fixed by combining into a single applyTaskPatch({priority, dueDate}) call on High transition. Dashboard PR #399 doesn't have this bug because it uses two independent override maps. Web App v535, React deployed from canonical.

**2026-05-28 self-service Verify toggle for DT account mapping (PR [#546](https://github.com/Stride-dotcom/Stride-GS-app/pull/546), `feat/delivery/dt-account-verify-ui`):** Closes the SQL-only gap left by PR #538 (v41 DT account-verification fallback). Symptom: STU-00057-WALKER republished 2026-05-27 landed under STRIDE LOGISTICS even though Studio AM Architecture was correctly mapped — the v41 backfill auto-verified only tenants with ≥2 prior pushes and Studio AM had 0. Settings → Integrations → DispatchTrack drawer gains an Unverified stat + filter and a per-row 3-state Status (Missing / Verify clickable button / Verified ✓ click-to-unverify), writing atomically to `dt_credentials.verified_account_tenants`. OrderPage fallback banner re-pointed at the new Settings flow. Toggle handler re-fetches the array immediately before write to prevent multi-row closure-race lost writes (caught by Opus 4.7 code-review). Data fix applied: Studio AM's tenant added to `verified_account_tenants` via UPDATE so the next republish lands correctly. tsc + build clean; deployed from canonical. No new migration, no GAS, no Edge Function — pure React + one row UPDATE.

**2026-05-19 tax rates wired to `tax_jurisdictions` table (`feat/billing/tax-jurisdictions`):** Removed the last hardcoded sales-tax literals (`DEFAULT_TAX_RATE=10.4` in `CreateDeliveryOrderModal.tsx`, `TAX_RATE_PCT=10.4` in the anon `PublicServiceRequest.tsx`) onto the operator-created `public.tax_jurisdictions` table (one `is_default` row enforced by a partial unique index; Kent 10.4% seeded; `get_default_tax_rate()` fn). New `useDefaultTaxRate` hook reads the default row once, fails soft to 10.4/Kent/WA; both consumers added `defaultTax.rate` to their useMemo/useEffect deps so the real rate propagates post-load (no stale-closure stick on fallback). New admin `TaxJurisdictionsPanel.tsx` in Settings → Pricing (under `<PriceList embedded />`, `isAdmin`-gated): City/State/Rate%/Default-star/Effective table, click-to-edit rate, star toggles the single default (clear-old-then-set, partial-index-safe), add-row, delete (blocked on default — UI + `remove()` + query-layer triple recheck). CRUD helpers + `TaxJurisdiction` type appended to `supabaseQueries.ts`. `TaxExemptBlock` (OnboardClientModal) now loads `clients.tax_rate_pct` and shows "Using default — Kent, WA 10.4%" vs "Custom rate — X%", with a per-client override input (blank ⇒ NULL ⇒ default, validated 0–100). Idempotent migration `20260519140000_tax_jurisdictions_rls.sql` is the git source of truth for RLS (anon+authenticated SELECT for the public form, authenticated write gated by UI `isAdmin`; `if not exists`/`drop policy if exists`/`create or replace` — safe vs operator's out-of-band objects). Opus review via `.claude/agents/code-reviewer.md` (registered subagent unavailable): no Critical/landmine; #2 judged neutral rate-source swap into the pre-existing #465/#466 snapshot; authenticated-write RLS + non-transactional setDefault window accepted by design; one nit fixed (0% configured default now honored via `>= 0`). tsc + build clean. React deploy NOT blocked (table already in prod, fail-soft); migration handed to operator as pending action (no Supabase token in builder env).

**2026-05-19 DO sales tax over-charged on whole subtotal (`fix/billing/do-modal-taxable-services`):** The DO modal's Task-8a tax snapshot computed `taxAmount = subtotalBeforeTax * rate`, taxing the ENTIRE pre-tax subtotal (base/zone fee, pickup leg, drive-out, XTRA_PC extra-piece, bundle discount, accessorials, coverage) — but only `service_catalog.taxable=true` services (felt pads `FELT`; `FAPROT` if its catalog row is set taxable) are taxable retail; delivery labor is not. Fix in `CreateDeliveryOrderModal.tsx`: new `taxableLines` memo looks each `selectedAccessorials` line up in the already-loaded `catalogServices` (`useServiceCatalog`) by `code`, includes only `svc.taxable===true` (fail-closed on unknown/unseeded code; skips `quotePending`/non-positive); `taxableSubtotal` = their sum; `taxAmount = taxableSubtotal * rate` (same unpriced/exempt/COD null-guards). Fully data-driven — admin toggling a catalog `taxable` flag re-flows with no code change. `taxFields` snapshot records new `taxable_subtotal` (COD always; bill_to null-when-exempt, mirroring `tax_amount`); 3 pickup-leg null-out payloads also null it. Migration `20260519130000_dt_orders_taxable_subtotal.sql` adds nullable `dt_orders.taxable_subtotal numeric(10,2)` (IF NOT EXISTS + COMMENT + NOTIFY pgrst). Pricing summary now labels "Sales Tax on <names> (rate% × $taxableSubtotal)" / "— no taxable services". Header v6. Opus review via `.claude/agents/code-reviewer.md` checklist (registered subagent unavailable in harness): no Critical, no Important, approved — confirmed it refines (narrows, fail-closed) the pre-existing accepted client-side snapshot, doesn't worsen React-billing posture. tsc + `npm run build` clean. PR merged to source; **React deploy intentionally HELD** until the migration is applied (builder env has no Supabase token; the new code writes `taxable_subtotal`, so deploying before the column exists would `PGRST204`-fail all DO creation) — flagged DEPLOY-BLOCKING in Pending User Actions.

**2026-05-19 dt_order_items double-insert on order submit (PR [#462](https://github.com/Stride-dotcom/Stride-GS-app/pull/462), `fix/delivery/do-item-dup-insert`):** Standalone delivery ALL-00097 had all 6 inventory lines inserted twice with identical `created_at`. Two root causes in `CreateDeliveryOrderModal.tsx`: (1) no synchronous re-entrancy guard on submit — `setSubmitting` async + `WriteButton.inFlight` resets the moment `performSubmit` resolves (incl. keep-modal-open repush-failure return) → second click re-enters; (2) single-leg edit/promote path re-read mutable `editingDraftRowIdRef.current` at delete + insert across awaits → null/stale read makes the `dt_order_items` delete a no-op while the insert still runs (mechanism already documented in `dt-push-order` `pruneDuplicateOrderItems()`). Fix: `performSubmit` + `submitInFlightRef`-latched `handleSubmit` wrapper; Submit disabled while `submitting`; new `orderSaveLocked` permanently disables Submit after a durable-save keep-open (all 3 repush-failure returns); single-leg path snapshots id once into `editId` (throw-if-falsy) and uses it for every delete/insert/update; single-leg warehouse insert now throws on `{ error }` (was the only swallowing insert in the block). Migration `20260519000000_dt_order_items_dedupe_unique_index.sql` soft-removes existing non-adhoc dups (FK-safe vs `parent_pickup_item_id` self-FK) then partial `UNIQUE INDEX dt_order_items_order_code_active_uniq (dt_order_id, dt_item_code) WHERE dt_item_code IS NOT NULL AND removed_at IS NULL` — structural backstop for all write paths. Code review (Opus locked-in): no Critical/landmine; both Important items (submit-after-save lock, swallowed insert error) fixed before merge. React deployed; migration handed to Justin as operator pending action (no Supabase token in builder env).

**2026-05-17 storage credits + Inventory action-bar label shortening (`feat/warehouse/storage-credit`):** Admin can grant a free-storage window on selected inventory items. New "Credit" button (admin-only) in the Inventory desktop floating bar (after Release) + mobile FAB → `StorageCreditModal` (free_from/free_to + reason + item preview); inserts one `public.storage_credits` row per item (resolving inventory.id by tenant_id+item_id) + best-effort `entity_audit_log`. Item detail Activity tab gains a "Storage Credits" Section (`StorageCreditsSection`, admin/staff-gated to mirror RLS) listing active credits with an admin-only Remove (soft-delete via `deleted_at` + audit). Migration `20260517000000_storage_credits_skip_in_charges.sql`: idempotent `CREATE TABLE IF NOT EXISTS public.storage_credits` + partial active-row index + RLS (read admin/staff/service, write admin/service) + `CREATE OR REPLACE FUNCTION _compute_storage_charges` = verbatim copy of the 20260502200000 body with one added block unioning active credit ranges into `v_billed` so the existing interval-subtraction loop drops credited days from both preview and generate (public wrappers untouched; React does zero billing date math). Also shortened 8 desktop + 8 mobile FAB action labels + Export (display text only; guard/toast strings kept long). Code review (Opus locked-in checklist): no Critical, inner function verified byte-identical except the intended block; applied one UX fix (admin/staff gate on the detail Section). Pending operator: apply migration via MCP + SQL spot-check (credit + no prior billing → days excluded) + prod `\d storage_credits` schema-drift check.

**2026-05-14 repair re-quote flow + photo/note polish (PR [#420](https://github.com/Stride-dotcom/Stride-GS-app/pull/420)):** Two-in-one PR per Justin's directive ("finish all migrations, build out the requote and polish the sort and grouping"). (1) Re-quote flow: staff can now add/remove items on an in-flight repair (Pending Quote / Quote Sent only) without cancel-and-rebuild. New SECURITY DEFINER RPC `re_quote_repair(tenantId, repairId, newItemIds[], performedBy)` atomically deletes existing `repair_items` + inserts new + UPDATEs `public.repairs` (status='Pending Quote', clears all quote_* fields, approved=false, primary item_id) + writes `entity_audit_log` row with action='requote'. Companion `re-quote-repair` Edge Function mirrors the parent repair row to the per-tenant Repairs sheet via the P1.4 reverse-writethrough framework; `Repair_Items` sheet is not mirrored (same scope as multi-item create flow — SB row is canonical). After save, staff invokes the standard sendRepairQuote to re-issue the customer-facing REPAIR_QUOTE email with the new item list. New `ReQuoteRepairModal` with inline inventory picker (search by item_id / description / sidemark / vendor / location) + per-row remove buttons; "Edit Items" button on RepairDetailPanel gated to Pending Quote / Quote Sent. (2) Photo + note polish: sort selector (newest / oldest / filename A-Z for photos; newest / oldest for notes) plus per-item grouping toggle in `PhotoGallery`, `NotesRollupView`, and `NotesGraphRollupView`. Group toggle visibility gated to ≥2 distinct items so single-item views stay clean. Lightbox keeps walking the flat filtered list across groups so arrow nav remains correct. Code-review fixes already applied: RPC OUT params renamed `repair_id`→`new_repair_id` to dodge the same 42702 ambiguous-column-reference trap that hit create_repair_quote_request in PR #400; edge function explicit `user_metadata.role ∈ {admin,staff}` gate before invoking the service-role RPC (the SECURITY DEFINER role check inside the RPC is bypassed when PostgREST sees service_role — without this gate a logged-in client JWT could re-quote any repair in any tenant). Same auth gap exists in the other 6 repair SB edge functions (flagged in FEATURE_BACKLOG for backfill). StrideAPI bumped to v38.218.0 — `REVERSE_REPAIR_FIELDS_` gained `item_id` + `approved`; Web App v513. `usePhotos.Photo` interface + `useGraphRollup.PhotoRow` gain `item_id` (already selected by `*`). 6-of-6 repair P3+P4a migration cluster complete. Photos/notes polish ships as reusable hooks (`useSortedGroupedNotes` + `NoteSortGroupControls` + `NoteListBody`) so future entity panels can adopt them without copy/paste.

**2026-05-14 [MIGRATION-P4a] completeRepair SB cutover (PR [#419](https://github.com/Stride-dotcom/Stride-GS-app/pull/419)):** Atomic `complete_repair_atomic(p_tenant_id, p_repair_id, p_result, p_final_amount, p_repair_notes, p_created_by)` SECURITY DEFINER RPC: UPDATE public.repairs (status, completed_date, repair_result, final_amount, billed) + INSERT public.billing (one row per quote_lines_json line, or single REPAIR row for legacy single-amount) + flush addons (`UPDATE addons SET billed=true, ledger_row_id=...`) + audit log — all one Postgres transaction. ON CONFLICT (tenant_id, ledger_row_id) DO UPDATE WHERE status != 'Invoiced' so re-completion after Void lands on the same row in-place but never overwrites an Invoiced/Voided→Invoiced chain. Idempotent on already-Complete / already-Cancelled — returns `skipped=true` with reason. Companion `complete-repair-sb` Edge Function calls the RPC, then fires per-billing-row reverse-writethrough to the per-tenant `Billing_Ledger` sheet via new `__writeThroughReverseBilling_` writer (StrideAPI v38.217.0 — 4th per-table writer registered against the P1.4 framework; 17-column FIELD_MAP covers all user-facing Billing_Ledger columns; Status='Invoiced' guard mirrors `api_writeBillingRowIdempotent_`'s skipped_invoiced semantics so re-completion never tramples an Invoiced row). Then repair-row mirror via existing `__writeThroughReverseRepairs_`. Then REPAIR_COMPLETE email via Resend (JWT-verified caller email). Per MIG-005 the CB Consolidated_Ledger sheet stays on its existing independent aggregation path (not driven by this writer) and is retired entirely in P4b. Feature flag `completeRepair` currently `active_backend='gas'`; flip in Settings → Migration to activate. Per MIG-013 standard 14-day canary applies since billing is in the path — Justin opted for fleet-flip after parity_enabled soak. 5-of-6 cluster handlers shipped (re-quote-repair followed in PR #420). React `postCompleteRepairSb(payload)` helper + `useFeatureFlag('completeRepair')` routing in `RepairDetailPanel.handleComplete`.

**2026-05-14 [MIGRATION-P3] requestRepairQuote single-item SB cutover (PR [#418](https://github.com/Stride-dotcom/Stride-GS-app/pull/418)):** TaskDetailPanel + ItemDetailPanel + Tasks bulk action "Request Repair Quote" buttons routed through the existing multi-item `request-repair-quote-sb` Edge Function with `itemIds:[oneItem]`. `create_repair_quote_request` RPC extended (DROP-then-CREATE migration because return signature changed) to accept `p_source_task_id` so the resulting repair preserves the task→repair linkage when called from TaskDetailPanel (multi-item bulk-quote leaves it null). New `request-repair-quote-shadow` v1 returns a tiny pure summary string. SHADOW_REGISTRY in `replay-shadow` gets the new entry. Feature flag `requestRepairQuote` gates the cutover. Closes one of the two remaining handlers from the 2026-05-13 repair-cluster session.

**2026-05-13 [MIGRATION-P3] repair-cluster GAS→SB migration, 4-of-6 handlers (PRs [#405](https://github.com/Stride-dotcom/Stride-GS-app/pull/405) + [#406](https://github.com/Stride-dotcom/Stride-GS-app/pull/406) + [#407](https://github.com/Stride-dotcom/Stride-GS-app/pull/407) + [#408](https://github.com/Stride-dotcom/Stride-GS-app/pull/408)):** Started the repair-cluster P3 migration per MIGRATION_STATUS.md MIG-013 (Path-C hybrid — keep feature-flag + shadow + reverse-writethrough + canary; skip 90-day historical replay since corpus is ~4 days old; 3-day canary OR fleet-flip for low-volume entities). Four of six handlers shipped SB-primary: `cancelRepair` (#405 — foundation + smoke-tested end-to-end against a test repair, JWT-verified caller email, audit log shape matches GAS exactly), `startRepair` (#406 — status flip + start_date stamp + Approved/In-Progress/Complete re-run rules, PDF stays React-side via lib/workOrderPdf.ts), `sendRepairQuote` (#407 — server-recomputed totals from quote lines, 11-column update including all quote_* fields, REPAIR_QUOTE email via Resend, idempotent re-send on identical lines+grand-total, StrideAPI v38.216.0 extended REVERSE_REPAIR_FIELDS_ to 17 columns), `respondToRepairQuote` (#408 — Approve→approved=true+REPAIR_APPROVED email or Decline→status='Declined'+REPAIR_DECLINED email, idempotent on already-resolved). Critical security fix carried through every handler: JWT verification via `supabase.auth.getUser(token)` against an anon-keyed client — the initial cancel-repair-sb draft just `atob`-decoded the JWT, code review flagged that as forgeable. SHADOW_REGISTRY in `replay-shadow` now lists 5 entries; `__writeThroughReverseRepairs_` writer (StrideAPI v38.215 → v38.216.0) covers all 17 repair columns idempotently. Architectural confirmation requested by Justin: `public.billing` is a SINGLE table — per-tenant view is `WHERE tenant_id=X`, consolidated view is the full table. GAS has two sheets (per-tenant Billing_Ledger + CB Consolidated_Ledger) only because Google Sheets can't aggregate across spreadsheets; that's a GAS limitation, not a billing-model requirement. After P4b retires CB, Supabase will be the only billing store. **Two remaining handlers next session:** `requestRepairQuote` single-item (~30 min — wire TaskDetailPanel/ItemDetailPanel to existing `request-repair-quote-sb` with `itemIds:[oneItem]`) and `completeRepair` P4a (~4-5 hrs — new `__writeThroughReverseBilling_` writer + new `mirrorBillingToCb` GAS endpoint for the separate CB spreadsheet + REPAIR_COMPLETE email + standard 14-day canary). Flags currently at `active_backend='gas'` for all four; Justin's plan is to fleet-flip after parity_enabled soak since repairs are low-volume and he wants to test on the next real repair end-to-end. All four edge function pairs (shadow + primary) deployed to Supabase v1; StrideAPI deployed as Web App v512; React bundle deployed via npm run deploy.

**2026-05-13 multi-item repair jobs (PR [#397](https://github.com/Stride-dotcom/Stride-GS-app/pull/397)):** Mirrors the will_calls/will_call_items pattern for repairs. Inventory bulk Request-Repair-Quote action now creates ONE repair holding N items (was: N separate repairs). New `public.repair_items` join table (id, tenant_id, repair_id, item_id, qty, item_result, item_notes) with RLS matching `public.repairs`. Migration backfills every existing repair → 1 row so legacy single-item and new multi-item repairs have identical data shape. SECURITY DEFINER RPC `create_repair_quote_request(tenant_id, item_ids[], ...)` does the atomic parent+items insert with tenant-scoped inventory-membership validation (`EXCEPT` query → 23503 with the missing IDs if any). New `request-repair-quote-sb` v1 edge function calls the RPC, resolves email tokens, server-renders `{{ITEM_TABLE_HTML}}` (the REPAIR_QUOTE_REQUEST template already had this token — just needed populating), and dispatches via existing `send-email` Resend pipeline. Zero GAS interaction in the create flow — closes Justin's "all in SB, no GAS/GS writes except strictly as needed" directive for this path. `fetchRepairByIdFromSupabase` eagerly joins repair_items + inventory overlay → exposes `ApiRepair.items[]`; RepairDetailPanel renders an items table when `items.length > 1`, legacy single-item view preserved for back-compat. Pricing/quote/status/completion-billing all stay parent-level: one quote per job, per-item `item_result` informational only and doesn't affect billing. Repair-id format kept `RPR-{first_item_id}-{millis}` (matches legacy GAS shape). Branch was rebased from a stale base before merge (code reviewer caught — would have reverted PR #395/#396); APP_URL trailing `#` was double-prefixing email tokens (fixed); `gs_sync_events` audit insert errors now log loudly instead of silently swallowed. Deferred: add/remove items after creation (cancel-and-rebuild or future re-quote flow), per-item pass/fail toggle UI (column exists, display read-only), legacy single-item GAS path cutover to SB (still routes through GAS for folder/email — separate Migration phase), multi-item versions of REPAIR_QUOTE/REPAIR_APPROVED/REPAIR_DECLINED/REPAIR_COMPLETE templates.

**2026-05-13 PU→Delivery item-sync engine (PRs [#388](https://github.com/Stride-dotcom/Stride-GS-app/pull/388) + [#389](https://github.com/Stride-dotcom/Stride-GS-app/pull/389)):** When a DT pickup leg completes, the linked delivery now (a) shows a green "Picked up [time] by [driver]" banner on the delivery `OrderPage` with a deep-link to the pickup, (b) shows a per-item ✓ Picked up indicator + a green "From pickup: picked up 2 of 3. '<driver note>'. Return codes: …" sub-row when the PU driver counted differently / added notes / flagged return codes, and (c) gets auto re-pushed to DT via `dt-push-order` so the eventual delivery driver's manifest reflects post-PU reality. Two-tier shared helper [`stamp-pickup-on-linked-delivery.ts`](stride-gs-app/supabase/functions/_shared/stamp-pickup-on-linked-delivery.ts): Tier A (order-level stamps + `picked_up_at` via FK match with `dt_item_code` legacy fallback) fires from `notify-pickup-completed` v3 (webhook path, `now()` placeholder for `linked_pickup_finished_at` since DT export.xml lags the webhook) + `dt-sync-statuses` v13 (poll path, overwrites placeholder with real DT timestamp + `driver_name`); Tier B (per-item field propagation: `pu.delivered_quantity` → `pickup_delivered_quantity`, `pu.item_note` → `pickup_item_note`, `pu.return_codes` → `pickup_return_codes`) fires only from the sync path where DT data is fresh. Authoritative `delivery.quantity` overwrite gated behind `quantity === original_quantity` (proxy for unedited-by-staff) so manual corrections aren't silently reverted; audit-column writes are unconditional since PU is source of truth. After Tier-B propagates ≥1 item, `dt-sync-statuses` fire-and-forget invokes `dt-push-order` for the linked delivery so the DT manifest reflects new qtys; failed dispatch logs to `gs_sync_events` for FailedOperationsDrawer. PR #388 schema: `dt_orders.linked_pickup_finished_at` + `linked_pickup_driver_name` + `dt_order_items.picked_up_at`. PR #389 schema: `dt_order_items.parent_pickup_item_id` self-referential FK + description-match backfill (strips "PICK UP: PU:" prefix variants) + fungible-items top-up backfill (zips dup-description pairs by row-id order) + `pickup_item_note` / `pickup_return_codes` / `pickup_delivered_quantity` audit columns. CreateDeliveryOrderModal generates client-side UUID for pickup ad-hoc rows so the mirrored delivery row carries the FK at creation. Original item_note sentinel-marker concat approach was replaced with dedicated audit columns after code review flagged staff-edit brittleness (regex would either accumulate stale `[FROM PICKUP] …\n---\n` blocks indefinitely or fail to strip after user edits). Deferred to Phase 2.5: `dt-push-order` should merge `pickup_item_note` into the per-item DT push so the DT delivery card shows it inline (today it stops at our DB and surfaces only on `OrderPage`). 2 historical P+D pairs (`MRS-00047`, `MRS-00049`) didn't auto-backfill the FK due to count mismatch / prefix variants; forward path covers everything new so backfill cleanup is non-blocking. Versions deployed: `dt-webhook-ingest` v18, `dt-sync-statuses` v18, `notify-pickup-completed` v3.

**2026-05-12 allow Re-issue on voided invoices (PR [#376](https://github.com/Stride-dotcom/Stride-GS-app/pull/376)):** Pure UI fix exposing an existing backend capability. `handleReissueInvoice_` already accepted Void rows (docstring: "Flip Invoiced/Void → Unbilled") but `Billing.tsx` was hiding both action buttons on Void with an em-dash placeholder, leaving operators with no path to re-bill voided charges short of manual sheet edits or direct API calls. Cell renderer at [Billing.tsx:1785](stride-gs-app/src/pages/Billing.tsx#L1785) now renders the Re-issue button on Void rows (Void button stays hidden — voiding a void is meaningless). Confirm-dialog copy branches on `isVoid` to drop the now-stale "removes from CB" clause + Stax/QBO precondition (CB rows already gone post-Void; external records still exist independently). Operator workflow: Status filter → Void → click Re-issue → rows flip back to Unbilled → re-bill via Create Invoices under a new number. Deploy hit the recurring schannel TLS error on the 3MB dist push; standard `pushWithRetry` flags failed, finally got through with `-c pack.windowMemory=10m -c pack.packSizeLimit=20m -c pack.threads=1` — flagged in BUILD_STATUS for `deploy.js` to absorb as a deeper fallback.

**2026-05-12 [MIGRATION-P2] DT order release flips Supabase-authoritative:** First production SB-primary write path. DT order detail page's modal-based ReleaseItemsModal flow replaced by an inline "Select items to release" panel (`DtOrderReleasePanel.tsx`) matching the WC release UX. Manual release now writes directly to `public.inventory` (status, release_date) + `entity_audit_log` via the React-side Supabase client — Supabase is authoritative on this path. Realtime fans the change through a new Status column on the items table + the "Release Items..." button gate (hides when all linked items are Released). New edge function `push-inventory-release-to-sheet` (v1) fires `reverseWritethrough()` once per item to mirror the change into the per-tenant Google Sheet via the P1.4 framework; failures land in `gs_sync_events` for the FailedOperationsDrawer with a working retry (now wired for `action_type='writethrough_reverse'`). First real per-table writer registered against the framework: `__writeThroughReverseInventory_` in StrideAPI.gs v38.208.0 (Web App v503). Companion hardening: `handleWriteThroughReverse_` now stores the FULL incoming payload in `gs_sync_events` on failure (was storing only `{op, table}` which would have broken retry). The strict `statusCategory === 'completed'` gate on the manual button is gone — manual release is now the universal escape hatch (driver finished but DT delayed, customer pickup at warehouse, partial-delivery exceptions). No optimistic patches, no manual refetch. Local-date YYYY-MM-DD derivation avoids UTC drift across day boundaries for late-evening PT releases. PR #2 (auto-release on DT-Finished) will reuse the same write path from dt-webhook-ingest + dt-sync-statuses.

**2026-05-12 public service-request form — pricing parity with internal modal:** Anon `/public/service-request` rebuilt to match `CreateDeliveryOrderModal` minus the client-account selector and inventory picker. New sections: Valuation Coverage (required for item-moving modes), Add-Ons (filtered to `visible_to_client=true`, all client-added entries forced quote-pending), Bill-To (radio "Same as pickup/delivery/service contact" or "Other" — auto-copies fields, dirty-flag prevents source-typo overwrites of manual bill-to edits, `bill_to_company` deliberately not auto-copied), Estimated Pricing Summary mirroring the modal (base + extras + PD_DISCOUNT + accessorials + coverage + Kent-WA 10.4% tax + total), required estimated-price acknowledgment checkbox. Service-only still computes drive-out fee from the service ZIP's zone. ZIP-not-in-zone and >20 pieces flip a "may require quote review" banner but submit still proceeds. New migration `20260512120000_dt_orders_bill_to_columns.sql` adds eight `bill_to_*` text columns; anon INSERT policy unchanged. Tax snapshot stores `customer_tax_exempt=null` + `tax_rate_pct=10.4` (informational until staff promotes to a client_id). Edge function `notify-public-request` (v6) exposes new `BILL_TO_*` + `ESTIMATED_*` + `ESTIMATE_DISCLAIMER` tokens for the customer-confirmation template. Pricing math (XTRA_PC threshold + per-piece rate, PD_DISCOUNT) sourced from `service_catalog` so admin tweaks flow through both surfaces without code edits.

**2026-05-05 Invoice Review overhaul — invoice_tracking + new tab UI (PR [#285](https://github.com/Stride-dotcom/Stride-GS-app/pull/285)):** New `public.invoice_tracking` table backs a from-scratch rewrite of the Billing → Invoices tab. Per-invoice push-state ledger (PK `invoice_no`) with separate `qbo_pushed_at` + `stax_pushed_at` timestamps + an `auto_charge` snapshot taken at create time so historical invoices keep their original Stax-eligibility regardless of later flag flips. Migration backfills 176 invoices from billing where status=Invoiced; pushed timestamps proxied from `stax_invoices.created_at` for the 79 already-pushed invoices (new pushes refine to per-path timestamps). RLS staff/admin only + service_role bypass; realtime publication enabled. StrideAPI.gs **v38.193 → v38.194** (Web App v487): five GAS-side hooks tie existing handlers to the new table — `handleCreateInvoice_` upserts on commit (auto_charge snapshotted from `public.clients` via REST); `handleQboCreateInvoice_` collects `pushedInvoiceNos` from results[] after the per-invoice loop and PATCHes `qbo_pushed_at = now()` in one PostgREST round-trip via `invoice_no=in.(...)`; `handleQbExport_` does the same with `batchInvoiceNos` for `stax_pushed_at`; new `api_deleteInvoiceTrackingRow_` helper called from `handleVoidInvoice_` + `handleReissueInvoice_` after their existing CB cleanup. `Billing.tsx` `InvoiceReviewTab` rewritten on top of `invoice_tracking`: 8 sortable columns (Invoice #, Client, Invoice Date, Total, Items, Created, QBO, Stax) with click-to-sort + arrow indicator, multi-select checkboxes with select-all-visible + indeterminate, sticky bulk action bar (Push to QBO / Push to Stax — gated when not all selected are auto_charge=true / Void / Clear), filters (search, client, push-status, auto-pay-only toggle, date range), realtime subscription on `invoice_tracking` so multi-operator pushes propagate live, optimistic timestamp stamps with rollback on error, line items lazy-fetched on row expand. Single-row Re-issue + Void retained from v38.193. `InvoiceReviewSummary` interface retired (was on-the-fly grouping shape); `InvoiceReviewLine` still used for the lazy line item subtable. Step 5 (Payments page Stax push status visibility) deferred by design — `stax_invoices` presence already implies a push, and `invoice_tracking` is queryable from anywhere in React for future follow-ups.

**2026-05-05 billing hardening pass — bugs #3-#9 closed (PR [#284](https://github.com/Stride-dotcom/Stride-GS-app/pull/284)):** Closed the cousin-bug backlog from the 2026-05-04 dup-number incident triage. StrideAPI.gs **v38.193.0** (Web App v486), React bundle `index-B2pd9yhv.js`. Six discrete changes split across all three storage layers so each is independently auditable: (B2 — Bug #4 + gap #9) `api_markClientLedgerInvoiced_` reads the Status column in the same `getValues()` as Ledger Row ID and throws `PRE_COMMIT_STATUS_ASSERTION` if any matched row drifted from Unbilled — closes the chain that produced INV-000135 billing the legitimately-Voided `INSP-TASK-INSP-62630-1` row on 2026-05-03; (B3 + B4 — Bugs #5 + #7) two new helpers `api_deleteCbRowsByInvoiceNo_` + `api_deleteCbRowsByLedgerIds_` (descending-grouped-`deleteRows`, modeled after `rollbackByInvoiceNo_`) wired into `handleVoidInvoice_` and `api_voidBillingRowsWhere_` so void/reopen flows mirror to CB Consolidated_Ledger; (C3 — Bug #3 defense-in-depth) server-side sidemark-uniqueness assertion in `handleCreateInvoice_` reads `clients.separate_by_sidemark` from Supabase REST and rejects mixed-sidemark payloads (fails open on Supabase outage); (C4 — gap #10) new `runBillingAnomalySweep` admin function — 5 checks against last 30 days (duplicate `invoice_create` entries, billing/stax line-count mismatch, mixed sidemarks for `separate_by_sidemark=true` clients, stale-Void rows in `stax_invoices.line_items_json`, STOR rows with negative qty/total) — emails Justin if findings, Logger-only on clean nights, trigger setup left manual; (D — Bug #6) new `handleReissueInvoice_` + `reissueInvoice` action backing the Re-issue button on Billing → Invoices, idempotent across all three storage layers — replaces the manual `runReleaseInvoicesForReissue` one-shot. Phase E pre-flight queries all green: clients table flags confirmed (Digs=false, Mary Kenngott=true, Nip Tuck=true), 42 Nip Tuck rows / $980 + 9 Mary Kenngott rows / $565 ready for re-issue, only the legitimate INSP-62630-1 Void remains, no new duplicate-create signatures since v38.182. **Verdict: GREEN** — Justin re-runs Create Invoices for Nip Tuck + Mary Kenngott; predicted invoice numbers continuing today's INV-001129+ sequence.

**2026-05-04 multi-tenant RLS access fix:** Bug report from a client assigned to 3 tenants — entity deeplinks (item / task / repair / will-call / shipment detail pages) returned "Item not found" for entities in the user's secondary/tertiary tenants. Root cause: every client-facing RLS policy compared `tenant_id` against the JWT's single primary `clientSheetId`, ignoring the React-side `accessibleClientSheetIds` array. Even though React correctly scoped fetches to all 3 tenants, RLS filtered out non-primary rows before they reached the access check at `useItemDetail.ts:67` (and parallel checks in useTaskDetail / useRepairDetail / useWillCallDetail / useShipmentDetail). Symptom: list pages showed all 3 tenants' items (those went through GAS, which knows the full scope), but Supabase-direct deeplink fetches via `fetchItemByIdFromSupabase` exposed the gap. Migration `multi_tenant_rls_access` adds two helper functions — `public.user_has_tenant_access(text)` (for `tenant_id` columns) and `public.user_has_tenant_access_storage(text)` (for storage paths, including the `_`→`-` substitution Supabase storage uses) — that return true if the input matches the JWT's primary `clientSheetId` OR appears in `accessibleClientSheetIds`. Every affected policy across 21 tables (inventory, tasks, repairs, shipments, will_calls, will_call_items, billing, claims, clients, client_insurance, entity_notes, entity_audit_log, documents, email_sends, item_photos, move_history, autocomplete_db, dt_address_book, dt_orders + all dt_order_* children, expected_shipments, photo_shares) and 4 storage buckets (documents, dt-pod-photos, invoices, photos) DROPped + CREATEd to call the helper — names preserved, semantics unchanged for single-tenant users. AuthContext.tsx companion change extends the user_metadata sync (cached + full-verify paths) to include `accessibleClientSheetIds` + `childClientSheetIds` alongside the existing `role` + `clientSheetId` fields, with a new `arraysEqualOrderless` helper to avoid firing updateUser on every page load. No backfill needed: the helper falls back to the primary `clientSheetId` check when the array is empty/missing, so multi-tenant users keep working at their primary tenant immediately after deploy and gain access to their secondary tenants on their next login (when AuthContext writes the array into JWT metadata). React-layer access checks at `useItemDetail.ts:67` etc. unchanged.

**2026-05-04 unified addons module:** Generalized the task-shaped `task_addons` system shipped 2026-05-02 (PR #193, never used in prod) into a polymorphic addons module that plugs into any entity panel. Migration `unified_addons` drops the empty `public.task_addons` table and creates `public.addons` keyed on `(tenant_id, parent_type, parent_id)` with CHECK constraints on parent_type ∈ {task, repair, will_call, inventory} + non-empty parent_id (matches the entity-key CHECK pattern from PR #229). Supabase realtime + RLS (staff/admin + service_role) enabled. New GAS helper `api_writeAddonsToLedger_(ss, parentType, parentId, ctx)` in StrideAPI.gs v38.177.0 replaces the inline task-only flush in `handleCompleteTask_`, and is called from `handleCompleteRepair_` + `handleProcessWcRelease_` (one line each, after the primary billing write). Reads unbilled rows via REST, writes one Billing_Ledger row per addon (rate snapshot + catalog fallback unchanged), mirrors via `resyncEntityToSupabase_`, then PATCHes addon back to `billed=true` with `ledger_row_id` stamped — making retries idempotent. Per-parent column mapping: task→Task ID="{parentId}-{svcCode}", repair→Repair ID="{parentId}-{svcCode}", will_call→Shipment #=parentId, inventory→Item ID=parentId. Ledger Row ID format unchanged: `{parentId}-{svcCode}-ADDON-{n}`. React side: new `useEntityAddons` hook (CRUD + realtime, blocks edits on `billed=true` addons), `useTaskAddons` reduced to a one-line compat alias, `AddTaskServiceModal` accepts optional `parentType` prop for title/filter (tasks keep `show_as_task` gate; others show all active services). `BillingPreviewCard` lifts the `entityType === 'task'` restriction at line 211, broadens the recorded fetch query for repair (`repair_id.eq OR repair_id.like-%`) and will_call (drops `svc_code='WC'` filter so addon rows appear), simplifies `isAddonBooked` to check `addon.billed` first. RepairDetailPanel + WillCallDetailPanel each pull addons via `useEntityAddons` and pass them into BillingPreviewCard with `editable` gated on entity status. TaskDetailPanel unchanged — picks up the polymorphic path via the alias. Step 2 (Supabase-native billing pipeline so handleCreateInvoice_ reads from `public.billing` directly) explicitly deferred per the 2026-05-04 handoff — separate strategic call advancing Decision #33 (out of Google).

**2026-05-03 session 93 — Stax payments architectural cleanup (PR [#214](https://github.com/Stride-dotcom/Stride-GS-app/pull/214)):** Retired the Stax Customers Google Sheet as a separate data source — it was a per-name lookup table that drifted from CB Clients. New `stax_buildClientStaxMap_()` helper in `StrideAPI.gs` (v38.153.0, Web App v443) indexes CB Clients by Client Name + QB Customer Name + Stax Customer Name into the same record so divergent-name invoices match; three Stax write paths (`handleQbExport_` auto-push, `handleImportIIF_`, `stax_lookupCustomerIds_` for the Refresh Stax IDs button) all resolve through it. Sheet-row dedup on Stax Invoices switches from the multi-field hash (`docNum|name|amount|date`) to the QB invoice number alone — fixes Justin's INV-000131 duplicate where a re-import drift on amount/date had been spawning a second row. Existing PENDING rows now UPDATE in place; CREATED/PAID/VOIDED rows stay untouched. Rows that resolve to a client without a Stax Customer ID are now logged as a `NO_CUSTOMER` exception and skipped instead of inserted as half-populated PENDING rows. React Payments → Customers tab refactored to derive from `clients WHERE stax_customer_id IS NOT NULL` via new `fetchStaxCustomersFromClients()`; renders the Billing Report's CreditCard + Auto Pay pill; Pull Customers (CB) / Sync With Stax / Sync Customers buttons removed; selected-row contrast fixed via explicit `theme.colors.orangeLight` background + `theme.colors.text` foreground. New admin entry `runStaxSheetsCleanup` collapses the existing duplicate clutter on both Stax sheets (idempotent — pending user action: run once from the Apps Script editor).

**2026-05-02 session 90 — GAS→Supabase email migration sweep (9 PRs):** Every email handler that doesn't need temp-password generation or PDF attachments is now off GAS MailApp. PRs against `source`, all merged + deployed: [#174](https://github.com/Stride-dotcom/Stride-GS-app/pull/174) `notify-new-order` + `notify-public-request` delegate to `send-email`; [#175](https://github.com/Stride-dotcom/Stride-GS-app/pull/175) new `send-onboarding-email` edge function for the admin Resend Onboarding flow; [#176](https://github.com/Stride-dotcom/Stride-GS-app/pull/176) CLAIM_STAFF_NOTIFY fires from CreateClaimModal; [#177](https://github.com/Stride-dotcom/Stride-GS-app/pull/177) handoff doc scoreboard update; [#178](https://github.com/Stride-dotcom/Stride-GS-app/pull/178) CLAIM_RECEIVED + CLAIM_MORE_INFO + CLAIM_DENIAL React-side; [#179](https://github.com/Stride-dotcom/Stride-GS-app/pull/179) `notify-order-revision` (ORDER_REJECTED + ORDER_REVISION_REQUESTED) routes through send-email; [#180](https://github.com/Stride-dotcom/Stride-GS-app/pull/180) ACCOUNT_REFRESH_INVITATION off GAS; [#181](https://github.com/Stride-dotcom/Stride-GS-app/pull/181) cleanup retiring ~383 lines of dead GAS handlers + React wrappers; [#182](https://github.com/Stride-dotcom/Stride-GS-app/pull/182) `send-email` gains `attachments` support (unblocks INSP_EMAIL + CLAIM_SETTLEMENT). StrideAPI.gs ends at v38.121.0 / Web App v424. send-email v5, send-onboarding-email v1, notify-new-order v8, notify-public-request v3, notify-order-revision v3 — all ACTIVE. Remaining work (INSP_EMAIL, CLAIM_SETTLEMENT, ONBOARDING activation/temp-password) needs design pass — they involve Drive PDF attachments or credential issuance.

---

## 2026-04

**2026-04-30 session 85 — client access to delivery orders restored:** Orders.tsx had three hardcoded `isAdmin` gates (tab default, URL→tab resolver, tab-content render) that hid the Orders tab and "+ New Delivery" button from client users even though `RoleGuard` permitted `client` on `/orders` + `/orders/:orderId`. Replaced with `canViewOrders = isAdmin || isClient`. DT Sync button kept admin-only. Existing accessibleClientNames filter (Orders.tsx:162-171) restricts visible rows so no RLS changes were needed.

**2026-04-30 session 84 — customizable add-on charges on delivery orders:** CreateDeliveryOrderModal v5. Selected add-ons now expose editable Qty + Rate inputs (used in both order entry and Full Edit). Subtotal = qty × rate for ALL units (including `flat`/`plus_base`, which previously ignored qty). Catalog rate is the default; staff/admin can override; clients edit qty only with rate locked. "Modified" badge surfaces overrides. Quote-required add-ons stay $0 until staff sets a rate. Per-order rate persists in `dt_orders.accessorials_json[].rate` (column already existed; previously catalog rate was re-looked-up at save).

**2026-04-26 session — retire per-item Drive folders:** Per-item Photos folders (created under PHOTOS_FOLDER_ID, named with Item ID) deprecated — never used (photos in Supabase) and confusing for clients. ItemDetailPanel.tsx: removed "Photos Folder" button. Apps Script: disabled per-item folder creation in `api_hyperlinkReceivedItems_`, `StrideFixMissingFolders`, `api_StrideFixMissingFolders`. New `StrideCleanupItemPhotoFolders_` (Utils.gs v3.5.0) under Stride Admin menu — dry-run + execute, trashes empty per-item folders, strips Inventory Item ID hyperlinks, reports non-empty to Cleanup_Report tab. StrideAPI.gs v38.130.0, Code.gs v4.7.0. PR #73.

**2026-04-01 session 27:** Inline Item Editing + Auto-Generated Item IDs (v27.0.0). `updateInventoryItem` POST endpoint. 5 editable field components. Auto-ID counter in CB Settings as NEXT_ITEM_ID (80000 start). LockService protected. Settings → Maintenance admin-only toggle.

**2026-04-01 session 28:** Custom Task Pricing (v28.3.0). Auto-repair on inspection fail REMOVED. Request Repair Quote endpoint + 4-surface UI. Email token audit. Full onEdit parity audit — 3 gaps fixed, `updateWillCall` endpoint added (v28.6.0). Free Receiving Toggle. Persistent Status Filters. Scanner + Move History rebuild. Billing Preview Mode + Commit to Ledger. Sidemark Color Highlighting. Mobile-Responsive Pages. StrideAPI.gs v28.6.0 deployed.

**2026-04-02 session 29:** Critical bug fixes (v28.9.0). completeTask crash fix (taskTypeLower). Task detail panel fire-and-forget saves. MM/DD/YY date format in all 7 detail panels. Cancel Task + Cancel Repair. Active row highlight. Claims page freeze fix. Claims end-to-end: client read access, email subject tokens, Coverage Type saved on create, updateClaim endpoint + Edit/Save. Refresh Data button in sidebar. Multi-select status filter chips. Clear Filters button. Unbilled Report multi-select. v29.0.0 deployed.

**2026-04-02 session 30:** WC add/remove items (v29.3.0). Partial WC UX (clickable split link, banner, faded rows). Per-page refresh on all 8 pages + `noCache=1`. WC release PDF deferred to release time (v29.6.0). Task Work Order PDF removed. Release Items FAB + endpoint (v29.7.0). Email template overhaul: CTA buttons → "View in Stride Hub". `{{APP_URL}}` auto-injected. SHIPMENT_RECEIVED onboarding box. Welcome email rewritten. push-templates.mjs. StrideAPI.gs v30.0.0.

**2026-04-02 session 31:** Stax Payments fully wired (v31.0.0). 18 endpoints: 6 read, 5 safe writes, 2 Stax API reads with retry/backoff, 5 financial writes. LockService + dedup + pre-charge validation. Admin-only.

**2026-04-02 session 32:** Parent/Child Account System (v32.0.0) — PARENT_CLIENT column, getAccessibleClientScope_() helper, scope arrays. Transfer History Logging (v32.1.0). Sidemark Filter on Billing (v32.2.0). Settings Maintenance: Fix Missing Folders moved, Sync Autocomplete DB, Send Welcome Email per-client. ItemDetailPanel history sections restyled.

**2026-04-02 session 33:** Detail panel UI improvements. Quick Action buttons 2-column CSS Grid. Section headers aligned. Resizable detail panels (useResizablePanel hook, 360-800px). Edit/Save mode replaces save-on-blur on Item + Task panels.

**2026-04-03 session 34:** Supabase Phase 2 complete (v35.0.0, Web App v114). Auth bug fix (USER_UPDATED guard). setupSupabaseProperties_(), notifySupabaseConfirmed_(), notifySupabaseFailed_(). 14 doPost cases wired for best-effort notifications. React: entityEvents.ts pub/sub emitter. useFailedOperations emits on confirmed rows.

**2026-04-03 session 35:** Supabase Phase 3 — Read Cache (Full Mirror) complete. 6 Supabase tables (inventory, tasks, repairs, will_calls, shipments, billing) with RLS. Write-through wired into all 23 doPost handlers. handleBulkSyncToSupabase_ + handleReconcileSupabase_. React: supabaseQueries.ts (6 entity fetchers + dashboard summary). All 6 entity hooks try Supabase first (~50ms) with GAS fallback.

**2026-04-03 session 36:** 10 React UX features. Admin "Login As" impersonation. AutocompleteSelect replacing 12+ native selects. Duplicate task/WC warnings with clickable links. MM/DD/YYYY date format. Sidebar drag-to-reorder. Sidemark multi-select filter. Print view with de-virtualized table. Smart export filenames. Signed settlement folder URL validation. Receiving page simplified (Shipment History tab removed). v37.4.0 deployed.

**2026-04-03 session 38b:** QR Scanner + Label Printer Hardening (ScannerBackend.gs v2.2.0). Dual label config (item/location independent, backward-compat migration). 7 location field defs. Bulk paste with dedup. upsertLocations API. Preview chunking (50-cap, batched print). Scanner decode throttle 60→20 FPS. SCAN_GAP 400→800ms. Progressive camera 640→1280. Deferred location load. Hardware wedge support. Persistent scan badge.

**2026-04-04 session 36:** Marketing Campaign Manager Phase 2 — Backend (v38.0.0, Web App v127). All 26 marketing endpoints: 8 GET, 12 POST, 3 GMAIL. ~1,930 lines. Full runCampaignNow cycle. Inbox check (reply/bounce/unsub detection). Admin-only + LockService.

**2026-04-04 session 37:** Marketing Campaign Manager Phase 3 — React Read-Only UI. Marketing.tsx with 7 tabs wired to 8 read endpoints. Admin-only route guard.

**2026-04-04 session 38:** Marketing Campaign Manager Phase 4 — Write Actions. 18 POST functions in api.ts. All modals. Confirmation dialogs. Preview Template (rendered HTML iframe). Settings edit/save. Check Inbox.

**2026-04-04 session 39:** Marketing Campaign Manager Phase 5 — Polish. Mobile responsive across 7 tabs + 9 modals. ProcessingOverlay on all write-action modals. Bottom-sheet modals on mobile. Marketing Campaign Manager ALL 5 PHASES COMPLETE.

**2026-04-04 session 40:** Billing sidemark display + storage preview picker fix. StrideAPI.gs v38.6.0 — handleGetBilling_ and handleGetBatch_ billing section resolve Item ID → Sidemark from Inventory. api_buildInvFieldsByItemMap_ extended with sidemark map. sbBillingRow_ forwards sidemark. Supabase billing table: added sidemark column via migration. React: ApiBillingRow + BatchBillingRow + SupabaseBillingRow carry sidemark. Billing table new Sidemark column (sortable, filterable, CSV). Fixed inverted toggle logic on 5 multi-select pickers in Storage Preview + Unbilled Report (clicking a box while "All" is active now correctly unchecks that one item).

**2026-04-04 session 41:** Discount/surcharge range widened ±10 → ±100. StrideAPI.gs v38.7.0 `api_applyDiscount_` + `handleTransferItems_` back-calc. Billing.gs v3.2.0 `applyClientDiscount_`. Transfer.gs v3.1.0 transfer re-apply. Triggers.gs v4.4.0 `SH_WriteBillingRow_`. Code.gs v4.5.0 Settings seed text. OnboardClientModal inputs: min/max -100/+100 with helper text. CLAUDE.md decision #12 updated. No backfill of existing unbilled rows — only new writes get surcharge. Root cause: 50% was clamped to zero; RCVG $10 became correct $15 after fix (S/M/L share base $15 on price list, so TEST50 class S and TEST123 class L both bill $22.50).

**2026-04-04 session 42:** PDF generation retry-with-backoff + GCP project link. StrideAPI.gs v38.8.0 — new `api_fetchWithRetry_` helper (1s/2s/4s/8s exponential backoff on 403 rate-limit / 429 / 5xx). Wraps `api_createGoogleDocFromHtml_` + PDF export in `api_exportDocAsPdfBlob_`. GCP project linked: StrideAPI.gs was on default shared pool (tiny quotas); linked to existing "Stride GS Inventory System" GCP project (number 1011527166052). Enabled Google Docs API (Drive/Sheets/Apps Script were already on). OAuth consent screen already configured from prior feature. Fixes "User rate limit exceeded" warnings on receiving/task/repair/WC/invoice PDFs.

**2026-04-04 session 43:** Docs cleanup — CLAUDE.md reduced from 1,151 lines to ~300 lines. Split hot/cold: archive files created in `Docs/Archive/` (Session_History, Deployment_Reference, Supabase_Integration_Plan, Marketing_Manager_Plan, QR_Scanner_Next_Phase, Architectural_Decisions_Log, Performance_Track_History). Consolidated 4 duplicated deploy sections into one table. Removed obsolete browser-based script replacement section. Enforced current-session-only policy on Build Status Recent Changes.

**2026-04-07 session 44:** InfoTooltips on OnboardClientModal (23 fields), Billing (storage preview + unbilled report), Payments (stat cards + QB mapping). Fixed Marketing campaign edit template save bug — backend `handleUpdateMarketingCampaign_` fieldMap used long keys but frontend sends short keys; added aliases. Added Type + Active columns to marketing templates (backend + frontend). Added missing `+ New Template` button. StrideAPI.gs v38.12.0 (Web App v156).

**2026-04-08 session 45:** Billing page redesign — tabbed report builder (Billing Report + Storage Charges + Invoice Review), starts blank, server-side filters, MultiSelectFilter component, removed Unbilled Report + Storage Preview modals. Payments overhaul — Import from Drive, Review tab with inline editing + selective Push to Stax, Dry Run mode, Test Invoices, per-invoice Auto/Manual charge toggle, Charge Queue, Pre-Charge Validation with real data, Stax invoice creation fix (url + due_at format), Void/Reset buttons. StaxAutoPay.gs v4.2.0 — trigger-safe _getSpreadsheet() (fixed recursive call), setupSpreadsheetId(), daily trigger installed and verified ($1 test charge successful). All 6 data pages: gated fetch (no API call until client selected), MultiSelectFilter v2 semantics ([] = none, explicit all, Clear works), status counts use filtered data not cached. Claims: always fetches (includes non-managed clients), crash fixed (removed nested FilterDrop component + columnFilters feedback loop). Stax Settings panel wired to real API. Welcome email direct GmailApp. Discount range ±100. PDF retry + GCP project. Billing sidemark column. Dashboard task Type fix. Client portal fixes. Sidebar branding. CLAUDE.md compressed. StrideAPI.gs v38.13.0 (Web App v159). **NOT YET BUILT: storage dedup (date-range overlap) — plan approved, code location identified.**

**2026-04-08 session 45 (continued):** Per-client Auto Charge (CB Clients column, OnboardClientModal toggle, charge handler gates in StrideAPI + StaxAutoPay). System Health Check endpoint + UI (Settings → Maintenance). Direct maintenance endpoints (syncAutocompleteDb, updateHeaders/installTriggers via Web App proxy, healthCheck — all bypass scripts.run). Import: skip ASM tasks for Released items (v4.2.0). Task completedAt + cancelledAt display. shipmentFolderUrl root cause fix (mapToAppItem was dropping it). User management: deleteUser endpoint, edit email via newEmail param, multi-email onboarding splits comma-separated emails. StaxAutoPay _getSpreadsheet() recursive call fix.

**2026-04-09 session 46:** Massive session — 30+ fixes/features. Import Class fuzzy matching (Size words). Custom task price → billing ledger (inline atomically in completeTask payload). Deep links fixed on duplicate warnings. Started/Completed timestamps (formatDateTime_). Shipment field deep link + fallback from Inventory. Stale data on page revisit FIXED (useApiData clears localStorage, skips Supabase on mount). Refresh buttons tied to real loading (all 7 pages). Task ID column links to Drive folder. LinkifiedText component (auto-detects entity IDs in notes). Bulk action toolbar wired (7 buttons + 4 new batch endpoints + BulkResultSummary). Mobile FAB on 4 pages. Mobile sticky checkboxes + status chips wrap. Email Template Manager (editor + preview + save + sync from React). ONBOARDING_EMAIL extracted to template. Logo URL updated in all 4 doc templates. Receiving PDF token fix. Fully automated client onboarding (auto-deploy Web App + auto-register in CB Clients). CB Clients as client registry (3 new columns). RemoteAdmin v1.7.0 hardened. CLAUDE.md mandatory deploy-clients rule. Billing inline editing (Unbilled rows, optimistic + backend persist). Billing 2-card layout + instant client list + refresh button. Auto Pay badge (Billing + Payments + Clients). Service filter shows names. Payments: status labels (Imported/Ready to Charge), Charge Selected button, Run Charges Now removed, Auto toggle fix (optimistic), search bars, Charge Queue headers. Dashboard Location + Vendor columns. StrideAPI v38.14.0+ (Web App v178).

**2026-04-08 session 47:** Job Detail Fast-Open Phase 1 (Tasks). Standalone task detail page at `#/tasks/:taskId` — opens in new tab from Dashboard, loads single task from Supabase (~50ms) with legacy getTaskById fallback. Supabase tasks table expanded with 7 new columns (vendor, sidemark, shipment_number, cancelled_at, started_at, billed, client_name). sbTaskRow_() + resyncEntityToSupabase_() + bulk sync updated. fetchTaskByIdFromSupabase + fetchRepairsByItemIdFromSupabase. Optimistic UI with "Saving..." indicator. StrideAPI.gs v38.14.0.

**2026-04-09 session 48:** Supabase Phase 4 — Cross-User Realtime confirmed working end-to-end. REPLICA IDENTITY FULL on all 6 cache tables. Task backfill (291 rows + all entities across 6 clients) via bulkSyncToSupabase. Verified pipeline: GAS write → write-through → Supabase Realtime event → entityEvents.emit → hooks refetch. Users see each other's changes within 1-2s.

**2026-04-09 session 49:** Delete-aware Supabase sync (`supabaseDeleteStaleRows_` helper removes orphans in `api_fullClientSync_` + `handleBulkSyncToSupabase_` across all 6 entity types, fixes stale dashboard data after direct sheet deletes). Bulk Sync to Supabase UI button wired in Settings → Maintenance. Cancel Task optimistic update (Phase 2C pattern). Optimistic UI Phase Plan executed: Phase 1 Claims (applyClaimPatch/mergeClaimPatch/clearClaimPatch/add+removeOptimisticClaim wired through ClaimDetailPanel + CreateClaimModal), Phase 2 Inventory (already implemented), Phase 3 WC Remove Items (items → Active, WC → Cancelled when empty), Phase 4 Create WC (already implemented), Phase 5 Transfer Items (items → Transferred), Phase 6 Complete Shipment (not needed — form flow already covered by ProcessingOverlay + Realtime). StrideAPI.gs v38.23.0 (Web App v211).

**2026-04-09 session 50:** Billing page invoice-list restructure — split report view into two distinct TanStack tables. Invoice Summary Table (top-level) shows one row per unique invoiceNo with invoice-level columns (Status, Invoice #, Client, Sidemark with 'Multiple' italic when mixed, Invoice Date, Line Items badge, Total, QBO with 'Mixed' branch). Expanding a row reveals a plain HTML `InvoiceLineItemsSubtable` with its own headers (Svc Code, Service, Description, Item ID, Qty, Rate, Total, Notes) — display-only with NaN/empty guards. Ledger table retains only unbilled rows (editable). Selection merges per-table state via `resolveSelectedRows()`; `selCount` counts top-level selections; `selectionTotal` sums both tables; Clear wipes both. Re-send Email now keyed off a single selected invoice summary row. Create Invoice modal body + footer use `resolveSelectedRows()`. Removed `consolidateView`, `canConsolidate`, `consolidatedData`, `_isConsolidated`/`_isChild` dead code + "By Invoice" toggle. Section headings only render when both sections exist. Expansion keyed strictly by `invoiceNo`. Sorts independent between tables. CSV export always uses `reportData`.

**2026-04-10 session 53:** Client Shipment Note — new `CLIENT_SHIPMENT_NOTE` field per client, surfaces as an amber highlighted banner at the top of the Receiving page shipment-details card whenever the selected client has a non-empty note. Text rendered bold 18px with amber border/background + AlertTriangle icon + "Client Receiving Instructions" uppercase header. Full-width, wraps on mobile, multi-line via `whiteSpace: pre-wrap`. Backend: CB Clients tab auto-creates "Client Shipment Note" column via `api_updateClientRow_`'s built-in column creator; `handleGetClients_` exposes `shipmentNote`; `api_clientRowToPayload_` + `api_writeClientSettings_` propagate to client Settings tab as `CLIENT_SHIPMENT_NOTE` (conditional write mirroring qbCustomerName convention); `api_appendClientRow_` + `handleOnboardClient_` default blank on new clients. No getClients cache invalidation needed — endpoint isn't cached. React: ApiClient/UpdateClientPayload/OnboardClientPayload gain optional `shipmentNote`; OnboardClientModal adds a textarea beneath the Notes field with tooltip; Settings.tsx `handleClientSubmit` passes the field in both create/update branches; Receiving.tsx derives `clientShipmentNote` via `useMemo([clientSheetId, liveClients, apiClients])` using the same name-match chain as the existing `autoInspect` handler so it stays correct on initial mount, async apiClients refresh, and client-clear. Client_Onboarding.js (legacy CB-bound path) not touched — production onboarding runs through StrideAPI.handleOnboardClient_. StrideAPI.gs v38.37.0 (Web App v225). React commit `2d8796a`.

**2026-04-10 session 54:** Stax Autopay Phase 4A+4B + setCol_ title-case hardening. **Phase 4A (v4.4.0):** closed the gap where due-today PENDING invoices got stranded because the daily trigger only charged CREATED rows. New headless `_createStaxInvoicesForRows_(options)` refactored out of `createStaxInvoices()` so both manual and auto paths reuse the push logic; `_prepareEligiblePendingInvoicesForChargeRun()` auto-pushes due-today auto invoices only (leaves future-dated and manual alone); `runChargesAuto`/`runCharges` now run push→reload→charge as one serialized LockService operation with combined Run Log. React Payments → Charge Queue gained a yellow "Needs Push to Stax" banner with per-row + bulk Push All. **Phase 4B (v4.5.0):** batch throttling, circuit breaker, wall-time watchdog, and policy alignment between prepare and charge stages. Fixed a real divergence where prepare skipped on `undefined` Auto Charge but charge stage let it through — both now use identical two-bucket fallback with distinct Exception reasons (`CLIENT_AUTO_DISABLED` vs `UNKNOWN_CLIENT`). New Config keys: `MAX_AUTO_CHARGES_PER_RUN` (25, clamp 1-100), `AUTO_CHARGE_DELAY_MS` (1500, clamp 0-10000), `AUTO_CHARGE_CIRCUIT_BREAKER_COUNT` (3, clamp 1-10), seeded via `_getIntConfig_` helper. `_executeChargeRun` refactored into 3 phases (build, sort+cap, charge loop). Circuit breaker counts only transport/network/5xx/timeout; HTTP 404/400/422 treated as bad-row and reset the counter. Wall-time watchdog aborts at 5m30s. `_chargeInvoice` return shape carries `httpStatus`. Expanded Run Log with `eligibleTotal`, `processedThisRun`, `deferredBy*`, `breakerTripped`, split skip-reason buckets. **Part 3 — setCol_ hardening (StrideAPI.gs v38.40.0):** all `setCol_` calls in `api_updateClientRow_` now use canonical Title Case (except `QB_CUSTOMER_NAME` which stays ALL_CAPS for QuickBooks). Prominent CONVENTION comment block at top of `setCol_`. `api_ensureColumn_` hardened with case-insensitive match + duplicate guard (previously could create duplicate columns on case-drifted sheets). StaxAutoPay.gs v4.4.0 + v4.5.0 → Stax Auto Pay bound script. StrideAPI.gs v38.40.0 → Web App v228. React commits `410aa59` + `dba0af4`.

**2026-04-11 session 56:** DispatchTrack Phase 1a — Supabase schema. 11 tables created (dt_statuses seeded, dt_substatuses, dt_orders, dt_order_items, dt_order_history, dt_order_photos, dt_order_notes, dt_webhook_events, dt_credentials, dt_orders_quarantine, audit_log) + dt-pod-photos storage bucket. Applied to Supabase via MCP tool. supabase/migrations/ dir established in stride-gs-app. CLAUDE.md updated with MCP deploy instructions. DT build plan in Docs/DT_Integration_Build_Plan.md. PR #1 open: Stride-dotcom/Stride-GS-app#1.

**2026-04-10 session 57:** DispatchTrack Phase 1b — Orders tab. supabaseQueries.ts: DtOrderForUI + Supabase row interfaces + fetchDtOrdersFromSupabase/fetchDtStatusesFromSupabase. useOrders hook (Supabase-only). Orders.tsx: TanStack Table with category filter pills, search, CSV export, virtual rows, OrderDetailPanel. App.tsx + Sidebar.tsx wired (admin-only, Route icon). Build `63207c2` deployed. Source committed to feat/dt-phase1a branch.

**2026-04-11 session 58:** releaseItems Supabase sync fix. Justin reported: marking items Released in React updated the client sheet correctly but left the app showing blank release_date. Supabase audit via MCP confirmed the test tenant's inventory `updated_at` was frozen at yesterday's Bulk Sync timestamp — the post-release sync was running but reading stale data. Two bugs: (1) `handleReleaseItems_` never called `SpreadsheetApp.flush()` before returning, so the router's downstream `api_fullClientSync_` re-opened the same sheet and read pre-write batched state, upserting blanks. (2) The `releaseItems` router case used the heavy `api_fullClientSync_(... ["inventory"])` which re-reads the entire sheet and upserts every row — wasteful and any silent failure leaves the full sheet stale. Every other single-entity write handler uses `api_writeThrough_(r, entityType, tenantId, entityId)` (calls `resyncEntityToSupabase_` to re-read + upsert only changed rows). Fix: (a) added `SpreadsheetApp.flush()` at end of `handleReleaseItems_` matching `handleStartTask` pattern. (b) switched `releaseItems` router case to `api_writeThrough_(r, "inventory", clientSheetId, payload.itemIds)`. No schema changes, no React changes. StrideAPI.gs v38.42.0 → Web App v231.

**2026-04-11 session 59:** Welcome email five-change bundle + CRITICAL build pipeline regression found. (1) `handleTestSendClientTemplates_` CLIENT_TEMPLATES now includes WELCOME_EMAIL/ONBOARDING_EMAIL — the React Template Editor had been erroring "Unknown templateKey". (2) `handleSyncTemplatesToClients_` header lookup fixed (was `"SPREADSHEET ID"`, actual is `"CLIENT SPREADSHEET ID"` — React "Sync to Clients" button had been erroring). (3) `handleSendWelcomeEmail_` accepts optional `payload.recipient` override. (4) New `api_sendWelcomeOnce_` helper with dedup via auto-created `Welcome Sent At` column on CB Users, wired into `api_upsertClientUser_` (onboarding path), `handleCreateUser_` (if active on create), and `handleUpdateUser_` (FALSE→TRUE activation transition) — only fires for role=client, never throws. (5) New `sendWelcomeToUsers` batch endpoint + React **Send Welcome** button on Settings → Users actions column (admin only, client-role users only, bypasses dedup for explicit resends). (6) Emails.gs client-bound `sendWelcomeEmail_` adds `{{APP_URL}}: "https://www.mystridehub.com"` to the token resolver so the spreadsheet custom menu path matches StrideAPI.gs parity (v4.2.0, rolled to all 6 clients). (7) **CRITICAL regression found**: `stride-gs-app/index.html` had been broken since commit `8441ff3` (session 58 Dashboard Created fix) — the `<script>` tag referenced a built asset `/assets/index-5gy4c4OL.js` instead of `/src/main.tsx`. Every `npm run build` since transformed only 6 modules and produced a no-op bundle that echoed the previously-built JS. Session 58's release_date fix, the Dashboard Created column fix, and DT Phase 1b Orders tab were NEVER shipped to mystridehub.com — the production bundle was locked at the pre-session-58 version. Fixed index.html entry, clean rebuild transformed 1,875 modules → `index-BuGBj9aB.js` (1,449,990 bytes). Deploy `5730f7a` contains ALL accumulated React changes from the past 3 sessions. StrideAPI.gs v38.43.0 → Web App v232.

**2026-04-10 session 55:** Dashboard Created column fix for Repairs + Will Calls. Both tabs on the Dashboard always rendered em-dashes in the Created column — a three-layer end-to-end gap. (1) Supabase `repairs` and `will_calls` mirror tables had no `created_date` column (only the auto `created_at timestamptz` row-insert time, which is different from business date). (2) `sbRepairRow_` and `sbWillCallRow_` in StrideAPI.gs didn't include `created_date` at all, and neither did any of the 5 callsites that build rows for them (2× in `resyncEntityToSupabase_`, 2× in `api_fullClientSync_`, 2× in `handleBulkSyncToSupabase_`). (3) The React Supabase read path had `createdDate: ''` hardcoded in 5 places: `fetchRepairsFromSupabase`, `fetchWillCallsFromSupabase`, the single-tenant repair mapper at line 542, and both `fetchDashboardSummaryFromSupabase` mappers. Since the dashboard is Supabase-first on mount, the empty string always won. Fix: additive Supabase migration `add_created_date_to_repairs_and_will_calls` (ADD COLUMN IF NOT EXISTS — no reordering, no drop); row builders + all 5 callsites now read `formatDate_(row["Created Date"])` from the sheet and pass it through; React `SupabaseRepairRow` + `SupabaseWillCallRow` interfaces gain `created_date: string | null`; all 5 React mappers now read `row.created_date || ''`. Tasks unaffected — they already had the field wired end-to-end from Session 47. Backfill required: Settings → Maintenance → Bulk Sync to Supabase once. StrideAPI.gs v38.41.0 → Web App v230. React commit `8441ff3` → GitHub Pages.

**2026-04-11 session 60:** Client isolation cache fix + standalone-client dropdown + signOut URL reset + canonical repo structure docs. Three interlocking bugs from session-60 client-access testing — root cause for all three: client users on shared browsers were inheriting the previous admin user's cached data. (1) **Frontend cache isolation** (`src/contexts/AuthContext.tsx`): `localStorage.stride_cache_*` was keyed globally by action name, not by callerEmail. An admin's cached `getClients` list (60 clients) was inherited by a subsequent client user's first paint. Fix: `cacheClearAll()` called at every auth transition — `handleSession` (successful login before state set, AND on denied login), `signOut` (first thing), `impersonateUser`, `exitImpersonation`. Also `useApiData` now sets `data=null` on error, not just the error flag, so stale cache visually clears. (2) **`signOut` URL reset** — left users on a deep-linked client-specific URL after logout; fixed by resetting HashRouter to `#/`. (3) **Standalone-client dropdown** (`handleGetClientsAuthed_`): blank dropdowns for single-client users after session-60 cache fix exposed a pre-existing bug. `getAccessibleClientScope_` returns null for standalone/single-client users by design (per its own doc comment) but the handler was treating null as "no access" and returning AUTH_ERROR. Fix: on null scope, fall back to `user.clientSheetId` as a single-element scope via `parseCSV_` (handles single and CSV). Multi-client/parent paths unchanged. (4) **Canonical repo structure doc** (`Docs/REPO_STRUCTURE.md` + CLAUDE.md Repository Structure section) — documents the two-repo parent + dist subtree model after session 59 cleanup, branch responsibilities (`source` = full workspace, `main` = built bundle force-push), rationale for keeping `stride-gs-app/dist/.git` as a separate repo, and the "never nest git init" rule. StrideAPI.gs v38.44.0 → Web App v233. AuthContext.tsx + useApiData.ts deployed via React build `fa31123`/`9ee4394`.

**2026-04-15 session 65:** Email deep links + billing Supabase filter mirror + repair quote confirmation. **Email deep links:** `Emails.gs v4.3.0` injects "View in Stride Hub →" orange CTA button into all notification emails via `{{APP_DEEP_LINK}}` token (post-body injection in `sendTemplateEmail_`). Token set by callers: `Shipments.gs v4.2.0` SHIPMENT_RECEIVED → `#/shipments?open=<SHP>&client=<ssId>`; `WillCalls.gs v4.4.0` WILL_CALL_CREATED + WILL_CALL_RELEASE → `#/will-calls/<WC>`; `Triggers.gs v4.5.0` INSP_EMAIL → `#/tasks/<taskId>`, REPAIR_COMPLETE/QUOTE/APPROVED/DECLINED → `#/repairs/<repairId>`, WILL_CALL_CANCELLED → `#/will-calls/<WC>`. Resend menu in Emails.gs also wired for INSP + REPAIR_QUOTE + REPAIR_COMPLETE. **React:** `deepLinkPendingTenantRef` pattern added to Tasks/Shipments/Repairs/WillCalls — `?client=<spreadsheetId>` URL param stashed on mount, resolved to clientFilter name once apiClients loads (committed `890b7f6`). **Billing Supabase filter mirror (merged):** `feat/billing-supabase-filter-mirror` — zero-tenant-ID guard in `supabaseQueries.ts`, unconditional `useMemo filtersKey` + `filtersRef` in `useBilling.ts`, cancelled flag in `Billing.tsx` sidemark effect. **Repair quote request:** persistent success/error banners, race fix (`setRepairRequested` before `removeOptimisticRepair`). **Auto-inspect race fix (Receiving.tsx):** `clientAutoInspect` → `useMemo` + `prevAutoInspectRef + useEffect` pattern, merged. GAS rolled out 47/47 + deployed 47/47. React bundle `index-CGEBbJ6Y.js` (commit `e0fdcf4`). Supabase migrations still pending (MCP unavailable): `20260411120000_dt_phase1a_schema.sql`, `20260413180000_add_cod_to_will_calls.sql`.

**2026-04-14 session 64:** Script-ID template-pollution cleanup. **Core problem discovered:** `npm run sync/rollout/deploy-clients` had been silently pushing all client code to the master template's bound script for months because CB Clients.SCRIPT ID was polluted with the template id (`1Pk2Oc0u7RRg…`) for 44 of 50 rows. Onboarding's `handleFinishClientSetup_` Drive search (`'<sheetId>' in parents and mimeType=script`) was returning the template's container-bound script (which has multiple parent links from old makeCopy operations) and that template id flowed into CB → every subsequent sync/rollout targeted the template → every client ran stale code. **Fix 1 — authoritative resolver:** StrideAPI.gs v38.52.4 added `api_resolveBoundScriptViaRedirect_(sheetId)` that fetches `https://script.google.com/d/<sheetId>/edit` with the caller's OAuth token and parses Google's 302 Location header for the bound scriptId — can't return template leakage, no Drive indexing lag. Wired as Strategy 0 in both `handleOnboardClient_` and `handleFinishClientSetup_`; template-id guard (v38.52.2) added to every write path that could flow into CB. **Fix 2 — client self-report:** RemoteAdmin.gs v1.5.1 added `get_script_id` doPost action that calls `ScriptApp.getScriptId()` in the client's own context and writes directly to `CB Clients.SCRIPT ID` via `CONSOLIDATED_BILLING_SPREADSHEET_ID`. **Fix 3 — bulk backfill:** new StrideAPI endpoint `backfillScriptIdsViaWebApp` iterates CB and POSTs `{token, action:"get_script_id"}` to each client's Web App URL. Token mismatch bug (first shipped reading `REMOTE_EXEC_TOKEN` property — nonexistent; fixed to `CLIENT_REMOTE_EXEC_TOKEN` with hardcoded fallback `"stride-remote-exec-9f3a2"` matching `handleRemoteAction_`). **Fix 4 — sync-clients.mjs:** rejects template id in every priority (CB column / cached / Settings / Drive) and falls through; new `getScriptIdViaBulkDrive` enumerates all accessible Apps Script projects with their parents and matches client-side. **Fix 5 — update-deployments.mjs v2.2.0:** `deployments.update` now falls back to `deployments.create` when the existing `deploymentId` doesn't belong to the target scriptId (exactly what happened: 42 of 47 clients had Web App URLs for template deployments). New deployment URLs written back to both `clients.json` and `CB Clients.WEB APP URL`. Rate-limited to ~40/min (1500 ms pacing) with 30/60/90 s backoff on "60 management requests per user per minute" quota. **Client-side workflow Justin executed:** manually pasted 38 of 50 real scriptIds into CB (from each client's Extensions → Apps Script → ⚙️ Project Settings), then `npm run sync && npm run rollout && npm run deploy-clients` — first time rollout ever hit the real clients. **Collateral fixes same session:** Import.gs v4.3.0 (Reference column mapping — legacy REFERENCE/REF/PO/PO#/ORDER/SO#/SALES ORDER/WORKROOM/INVOICE #/JOB headers now flow through to Inventory); WillCalls.gs v4.3.0 (WC completed email columns changed from Item ID/Description/Class to Item ID/Vendor/Description/Reference, Vendor+Reference backfilled from Inventory); StrideAPI.gs handleGenerateWcDoc_ v38.51.0 (full DOC_WILL_CALL_RELEASE token set — previously 9-token subset left most placeholders rendering as `{{TOKEN}}`); handleGenerateWcDoc_ v38.51.1 bug fix (was calling nonexistent `api_readClientSettings_`); handleStartRepair_ v38.51.6–9 (generates Work Order PDF into canonical Repairs/<id> folder, allows regen on Approved/In Progress/Complete, 3-tier fallback for Supabase write-through preserves Shipment Folder URL for legacy repairs); receiving `api_hyperlinkReceivedItems_` v38.51.3 pulls shipment-folder creation out of the email block (disabling notifications no longer breaks folder buttons); Receiving bulk "Paste from Excel" modal with configurable start-column; per-client Supabase Sync button on Settings → Clients cards; deep-link `&client=<sheetId>` passed from panels so Inventory auto-selects filter synchronously; Rediscover Script IDs button on Settings → Clients; Finish Setup button now shows when Script ID is missing (not just Web App URL). **Not fixed / landmines:** (1) cacheSubscribe pub/sub briefly shipped to sync AppLayout + Page useClients — cascaded refresh loops on WC/Repairs, reverted. A proper `ClientsProvider` singleton was attempted in session 63 and also reverted (see session 63 entry). The multi-instance `useClients` race is still mitigated only by the ref pattern in the 6 data hooks + in-memory cache short-circuit — open item. (2) Receiving auto-inspect race — a `useEffect` patch triggered React #300 on Inventory/Clients, reverted in bundle `index-XUulEEyK.js` (commit `530c358`). If user picks a client before apiClients resolves, `needsInspection` stays unticked — ~63 items in the last 30 days across 18 tenants affected, manual backfill by user. (3) 12 of 50 clients still show template scriptId in CB because their webAppUrl is a template deployment (rediscover returns template id, rejected by guard) — recovery workflow documented in CLAUDE.md. (4) Client dropdown reported 120s+ load times briefly during the session — unclear root cause, `useClients` goes through GAS with no Supabase mirror; first-load cold path slow. Live artifacts: StrideAPI v38.52.4 Web App v268, 47 client Web Apps all on their own bound scripts, React bundle `index-XUulEEyK.js` (commit `530c358`).

**2026-04-14 session 63:** Deep-link blank-page fix + Item ID ledger Phase 1-3. **Part 1 — Deep-link fix:** Item ID / Task / Repair / WC / Shipment deep-links (`#/inventory?open=X&client=Y` etc.) rendered a blank page. Root cause: Inventory/Tasks/Repairs/WillCalls/Shipments mount effects called `refetch()` on deep-link mount, and `refetch()` in `useApiData` explicitly skips the Supabase cache (`skipSupabaseCacheOnce`) and forces a GAS call — with no `clientFilter` yet set, that's an unscoped 90s-10min GAS scan (same root cause as session 62). Plus the WC panel item-ID link at `WillCallDetailPanel.tsx:641` was a raw `<a href="#/inventory?open=${itemId}">` with no `&client=` so it fell to the slower Supabase tenant lookup fallback. Fix: remove the `refetch()` call from all 5 pages (hook auto-fetches via `cacheKeyScope` when clientFilter populates from the `&client=` param); replace the WC raw anchor with `<DeepLink kind="inventory" id={item.itemId} clientSheetId={clientSheetId} />`. Live bundle `index-DiUAvZLS.js` (commit `20e9c1d`). **Part 2 — item_id_ledger (Phases 1-3):** Built cross-tenant Item ID uniqueness registry after legacy import had re-used 22 existing IDs across different clients. Phase 1: new Supabase table `item_id_ledger` (item_id PK, tenant_id, status ∈ active/released/transferred/voided, source, created_at, updated_at trigger, RLS select policy for authenticated, service_role writes). Backfilled 4,054 rows from `public.inventory` via `ON CONFLICT DO NOTHING` (first-seen wins). Companion view `item_id_ledger_conflicts` surfaced 22 pre-existing cross-tenant collisions — all legacy transfer leftovers (one Released, one Active); zero active-on-active collisions so no cleanup needed. Migration: `stride-gs-app/supabase/migrations/20260414180000_item_id_ledger.sql`. Phase 2: StrideAPI.gs v38.52.0 → Web App v262. New helpers `api_ledgerInsert_` / `api_ledgerBatchInsert_` / `api_ledgerUpdateStatus_` / `api_ledgerTransferTenant_` / `api_ledgerCheckAvailable_` (all best-effort, never throw). New endpoint `checkItemIdsAvailable({itemIds})` returns `{duplicates: [{itemId, tenantId, tenantName, status, source, createdAt}], degraded}` with client-name enrichment from CB Clients tab. Router-level pre-check on `completeShipment` rejects cross-tenant collisions with `ITEM_ID_COLLISION` (same-tenant resubmits pass through — idempotent). Write-through: `completeShipment` batch-inserts ledger rows on success (source=manual, status=active); `releaseItems` updates status → released; `transferItems` updates tenant_id → dest + status → active. Degraded mode (Supabase unreachable) allows save + logs per 2026-04-14 decision. Phase 3: React `Receiving.tsx` on-submit preflight via `postCheckItemIdsAvailable` — on cross-tenant duplicate shows multi-line error listing each ID + owning client + status; on degraded mode logs warning but proceeds; same-tenant hits tolerated. Live bundle `index-CNSokXQ8.js` (commit `3ee0913`). **Three post-ship fixes same session:** (A) StrideAPI.gs v38.52.1 / Web App v263 — ledger collision error now shows client name (resolved via new `api_clientNameMap_()` helper, CacheService-backed 5-min TTL, shared by both preflight handler and completeShipment router guard) instead of raw spreadsheet ID, multi-line bullet format with "Edit the Item ID column and try again" footer matching React preflight. (B) `useBilling` infinite render loop on Inventory page (React error #300 + cascading canceled Supabase requests) — session-62 `clientNameMap` ref-stabilization was applied to 5 hooks but missed useBilling; Inventory page runs all 6 data hooks so the bug fired there. Fix: mirror clientNameMap and filters into refs, narrow `fetchFn` deps to `[clientSheetId, hasServerFilters, filtersKey]`. Commit `91e8b5d`, bundle `index-KmG1qKHk.js` (intermediate, superseded by C). (C) **Root cause** of the ref pattern entirely: multi-instance divergence of `useClients`. Every consumer (page + 8 data hooks) called useClients independently → ~7 parallel useApiData instances per Inventory mount, each with independent `data` state — identical values but different array references across instances. Attempted a `ClientsProvider` singleton refactor (commit `2e91aa6`, bundle `index-BFRgl5Rm.js`) — reverted same session after two regressions. First: provider was above the auth gate in `main.tsx`, so fetch fired pre-login without a token and left the client dropdown empty for 3-5 min; moved provider into `App.tsx` below `if (!user) return <Login/>` (commit `ea74c8b`, bundle `index-BE58wRh8.js`). Second: React #300 on client-filter click reappeared, cause never isolated under minified production build (likely interaction between conditional `useContext` fallback and consumer lifecycles across auth transitions). Full revert to the pre-Context `useClients.ts` (commit `573e59b`, final session-63 bundle `index-BmcdaxbO.js`). The Fix-B `useBilling` ref stabilization is the actual load-bearing fix for today's symptoms; in-memory cache tier short-circuits subsequent consumer fetches so `clients` references converge in practice. Cleaner singleton refactor deferred. **(D) Receiving description multi-line.** New `multiline` prop on `AutocompleteInput` renders a `<textarea>` that auto-grows, suppresses plain Enter (no stray newline / accidental submit), and inserts `\n` on Ctrl+Enter / Cmd+Enter. Enabled on the Receiving Description cell with placeholder `"Item description... (Ctrl+Enter for new line)"`. Vendor / Sidemark / Room still single-line. Commit `c57890f`, bundle `index-XUulEEyK.js` (later superseded by the revert bundle). Not done: `Import.gs` ledger write-through (bound-script path, deferred); maintenance page ledger viewer; nightly reconciliation job; cleaner singleton refactor for `useClients`.

**2026-04-14 session 62:** React data-hook perf fixes for single- and multi-client views on Inventory / Tasks / Repairs / Will Calls / Shipments. Three sequential React deploys. (1) `875b5d6` — Shipments page-level `clientName` safety net (`shipIdToName` memo from `apiClients.spreadsheetId → name` + `.map()` resolving empty `clientName` via `r.clientSheetId` / `r.sourceSheetId` before the `clientFilter.includes(r.clientName)` filter); same pattern already in Repairs / WillCalls. (2) `b0057cd` — Stabilize `clientNameMap` via `useRef` in all 5 data hooks. Root cause: `clients` from `useClients` returns a new array reference every render → `clientNameMap` useMemo rebuilds → `fetchFn` useCallback rebuilds (it had `clientNameMap` in deps) → `useApiData` doFetch useCallback rebuilds → useEffect fires → aborts in-flight → refetches → perpetual loop. Workaround: `clientNameMapRef.current = clientNameMap` on each render; `fetchFn` reads the ref inside and keeps `[cacheKeyScope]` as its only dep. (3) `aab1a54` — Remove forced-GAS `refetch()` on client-filter change from all 5 list pages. Each had `useEffect(() => { if (clientFilter.length > 0) refetchX(); }, [clientFilterKey])` but `refetch()` in `useApiData` calls `skipSupabaseCacheOnce()` + `setNextFetchNoCache()` + `doFetch(bypass=true)` → every client-filter change was forcing an unscoped GAS scan (90s-10min). The hook already refetches automatically when `cacheKeyScope` (derived from `clientSheetId`) changes, via the Supabase-first path. Removed the useEffect from all 5 pages. Live bundle `index-lOoM3xSO.js`. No backend, no Supabase migrations. Billing / Claims / Payments / Marketing perf remain for a follow-up session — Billing is slow by design (server-side filter via GAS), the other three have no Supabase mirror yet. Also documented the `useClients` referential-instability bug as the root cause of the ref workaround; fixing that would let the 5 hooks drop the ref pattern.

**2026-04-11 session 61:** Import.gs perf fix (17 min → 10s) + StrideAPI.gs service_role JWT leak remediation. **Part 1 — Import.gs v4.2.1:** 98-row legacy client inventory import was taking ~17 min. Root cause: four per-row round-trips inside `importSheetRows_` each worth 1-3s. Fixes: (1) batch `photoRange.getNotes()` once instead of `photoRange.getCell(rt+1,1).getNote()` per row — biggest win on Released tabs where Photos column is sparse (85 round-trips → 1). (2) Pre-compute `asmCounterByItem` map once per call via single `taskSh.getRange(2, taskIdCol, lr-1, 1).getValues()` read with a regex pass over existing `ASM-<itemId>-N` IDs, then increment in-memory — replaces per-row `nextTaskCounter_()` which was re-reading the entire Task ID column plus getHeaderMap. (3) Batch `setRichTextValues()` into one call for photo-hyperlinked Shipment # cells, using `getRichTextValues()` to preserve non-photo rows (replaces per-cell `setRichTextValue()` in a loop). (4) Removed `SpreadsheetApp.flush()` before the insert — it was forcing full ARRAYFORMULA/conditional-format recalc of the Inventory sheet before every import batch, with no prior write to commit. Rolled out to all 6 clients via `npm run rollout && npm run deploy-clients` (Web App v6–v22). **Part 2 — Security: StrideAPI.gs v38.44.1:** GitGuardian detected a Supabase service_role JWT in `AppScripts/stride-api/StrideAPI.gs` line 556 within 20 seconds of push `9ee4394`. Root cause: `setupSupabaseProperties_` was a "run once then delete" helper that hardcoded the JWT as a string literal before calling `PropertiesService.setProperty`. Committed with live values. Second copy of the same JWT also sat in `StrideAPI.backup.pre-jobdetail-20260408.gs` — .gitignore only blocked `*.backup.pre-jobdetail-*.ts/.tsx`, missing `.gs`. Remediation: (a) user rotated the service_role key immediately in Supabase dashboard and migrated the project to the new publishable/secret API key model; (b) `setupSupabaseProperties_` rewritten to throw with guidance pointing users to Apps Script editor → Project Settings → Script Properties UI; (c) backup file deleted (`git rm`); (d) `.gitignore` extended to block `**/*.backup.pre-jobdetail-*.gs`, `**/*.backup.*.gs`, `**/*.bak`, `**/*.backup`; (e) new Script Property value set via editor UI by user. History rewrite skipped — the leaked JWT is dead, repo working tree was in CRLF churn (135 phantom modifications), and cosmetic. Parent repo commit `5c9ac57` force-amended (not force-pushed; normal fast-forward from `4e5b97f..5c9ac57`). StrideAPI.gs v38.44.1 → Web App v234. GitGuardian incident pending manual resolve as "Revoked". Also decoded all 8 prior GitGuardian incidents from historical scan — 4 are anon keys (false positives, safe to resolve), 4 are "Generic Password" rows (one in `stride-wms-app/supabase/functions/dev-admin-login/index.ts` worth investigating separately).

**2026-04-17 session 71 (v2 visual design system sweep):** Full app-wide visual refresh — no backend changes, all React UI + email templates. Introduced unified "v2" design language derived from marketing-email prototype: cream `#F5F2EE` page wrappers, `STRIDE LOGISTICS · <PAGE>` 20px/700/2px-letter-spacing page titles, dark `#1C1C1C` stat cards (28px/300 value, bright accent colors, 10px/600/2px-letter-spacing kickers, 20px radius) on Dashboard/Marketing/Quote/Shipments/Claims/Billing/Payments/Orders/Scanner/Labels, white or cream content-card wrappers around filter+table on all list/financial/settings/JobPages, pill (100px radius) filter chips (dark solid `#1C1C1C` active / white outline inactive with uppercase 1px letter-spacing) on every page with status filters, cream `#F5F2EE` table headers with 10px/2px letter-spacing `#888` column labels on all TanStack tables, pill search inputs, pill WriteButton/ConfirmDialog/BulkResultSummary footer buttons, 20px radius on all modals (CreateTask/CreateClaim/CreateWillCall/OnboardClient/TransferItems/BulkReassign/BulkSchedule/TemplateEditor) with softer `0 24px 60px rgba(0,0,0,0.25)` shadow, pill BatchProgress/ActionToast, pill TopBar global search. DetailHeader upgraded to dark `#1C1C1C` hero card with 28px/300 entity ID, orange 10px/4px kicker, pill sidemark chip — propagates to every DetailPanel and every standalone JobPage (Task/Repair/WillCall/Shipment). DetailField kickers upgraded to orange 10px/500 2px-letter-spacing. Sidebar already on cream bg from earlier; Settings sidebar nav switched to dark-pill active state. TaskDetailPanel "Start Task" button upgraded from outlined orange rectangle to solid purple `#7C3AED` pill with drop-shadow + filled Play icon — matches Repair START REPAIR / Will Call START WILL CALL bold CTAs. Login page redesigned with cream bg, Oswald `STRIDE / LOGISTICS` two-line wordmark (24px/600/2px + 10px/400/5px letter-spacing), 20px radius card. Labels page got pill mode tabs; Scanner/Labels got v2 cards + pill buttons/inputs; Orders got dark KPI strip + pill tabs + pill chips + cream table header. Email templates: all 19 client notifications rewritten from scratch using design tokens (dark hero card + cream/white info cards + pill CTA buttons + unified Stride header with Oswald wordmark + Stride Hub portal info block reused on client-facing templates); "About Inspection" text in SHIPMENT_RECEIVED updated to reference "View in Stride Hub" button + "Create Inspection Task" portal flow (old "Needs Inspection checkbox" text removed — checkbox no longer exists in React app); all `{{SIDEMARK_HEADER}}` occurrences removed; push-templates.mjs map extended to include WELCOME_EMAIL + ONBOARDING_EMAIL; pushed to Master Price List + propagated to 48/49 clients via refresh-caches (1 expected skip — Master Inventory Template marked inactive). 11 progressive bundle deploys: `index-BtbWEX87`→`C5ETxlFt`→`CyUi-_Xf`→`6jkTSwev`→`B0k9-Zki`→`uhi_2v7j`→`CJBbMeY4`→`Dstimwyg`→`BQlePTQB`→`Cb0awkmW`→`Bgf2I5P3.js`. Source commits `ad90cdc` through `c0f516a`. Open items: deep interiors of DetailPanels (TaskDetail/RepairDetail/WillCallDetail/ClaimDetail/ItemDetail each have their own internal section button rows and field grids with 8–10px corners that don't yet match v2); Master Inventory Template rollout inclusion (user flagged that onboarding template should receive refresh-caches updates so new clients inherit latest templates — currently skipped with "Requested entity was not found" because marked inactive/missing Web App URL).

**2026-04-16 session 70 (parallel chat — Scanner/Labels/Inventory-as-source-of-truth):** Massive session: (1) **Inventory as single source of truth** — `api_buildInvFieldsByItemMap_` extended from 4 fields to all 22 Inventory columns; every handler (Tasks, Repairs, WC, Batch) now OVERRIDES item fields from Inventory; Supabase-side `_fetchInvFieldMap` added to overlay inventory data on `fetchTasksFromSupabase`, `fetchRepairsFromSupabase`, `fetchDashboardSummaryFromSupabase`. Location moves via Scanner now reflect on ALL pages immediately. (2) **Native React Scanner** replaced GAS iframe — camera scanner (BarcodeDetector + html5-qrcode fallback), batch textarea, Supabase location picker with Realtime, +New Location modal, cross-tenant batch move via `batchUpdateItemLocations` (uses `item_id_ledger` + React pre-resolved `tenantMap`), mobile sticky bottom bar. (3) **Native React Labels** — QR via `qrcode` npm, Item/Location modes, field customization (toggle+font+drag-reorder), 4 sizes, save/reset template, print layout. (4) **Supabase mirrors:** `locations` (Realtime), `move_history`, `clients` (dropdown 120-240s→50ms), `delivery_availability` (calendar). (5) **Auth cache fix:** `cacheClearAll()` only on user change, not every refresh. (6) **Deep link fixes:** all 9 email CTAs use `?open=&client=` format; StrideAPI auto-inject adds `&client=`; `useClientFilterUrlSync` hook on 5 pages; docs in CLAUDE.md. (7) **React #300 fixes:** `printTitle useMemo` before early return; `apiClients.length` dep; `useClients` revert to useApiData. (8) **`npm run deploy`** single command (build+main+source). (9) **Supabase 1000-row cap** `.range(0,49999)` on all queries. (10) **Federal holidays** on Availability Calendar. (11) **Repair quote confirmation** persistent banner. npm deps added: `html5-qrcode`, `qrcode`, `@types/qrcode`. Bundles: multiple deploys, final `index-D6YdJQV_.js`. API versions through v38.56.0→v38.63.0+ (merged with parallel chat). Open item: centralize folder URLs on Inventory (plan saved).

**2026-04-16 session 70 (follow-up):** Client-role batch payload field parity. Client-role users (and admins via "Viewing as") were seeing blank Reference / Item Notes / Carrier / Repair Notes / Pickup Phone / etc. fields on detail panels because `handleGetBatch_` emitted a smaller payload than the individual-fetch endpoints; hooks then hardcoded missing fields to `''` / `false` / `null`. StrideAPI.gs v38.60.1 → Web App v280: `handleGetBatch_` now emits the full ApiInventoryItem / ApiTask / ApiRepair / ApiWillCall / ApiShipment / ApiBillingRow shape (WC `items` still loaded on-demand). Matching TS `Batch*` types extended; all 6 hooks (`useInventory`, `useTasks`, `useRepairs`, `useWillCalls`, `useShipments`, `useBilling`) updated to pass the new fields through. React bundle `index-BdBuunm2.js`. Commits: parent repo `da44684`, dist `16968c6`.

**2026-04-16 session 70 (continued — five-item UI/email batch):** (1) Room→Reference in all email/PDF item tables. `api_buildSingleItemTableHtml_` signature last arg changed `room`→`reference`; all 5 StrideAPI call sites + TRANSFER_RECEIVED `trCols` updated; Work Order PDFs now emit `{{ITEM_REFERENCE}}` alongside legacy `{{ITEM_ROOM}}` for template retirement at owner pace; `api_findInventoryItem_` return now includes `reference`. Emails.gs v4.6.0 (`buildItemsHtmlTable_` + `buildSingleItemTableHtml_` rebuilt with Reference replacing Room) rolled out to 49 clients + Web App deploys refreshed. (2) Add User modal now supports multi-client chip picker matching Edit modal; backend already accepted CSV, only UI parity was missing. (3) New `updateRepairNotes` POST endpoint + `handleUpdateRepairNotes_` handler lets office save Repair Notes on Approved repair before Start. `RepairDetailPanel` gets inline Save button under textarea (enables when dirty, "✓ Saved" feedback). Lightweight path: no lock, no email, no PDF regen. (4) Dashboard loading copy role-aware: client users see "Loading…" not "Fetching open jobs across all clients…". (5) Mobile TopBar z-index bumped 10→30 so Dashboard sticky thead (z-2) can't cover hamburger button on scroll. StrideAPI v38.63.0 → Web App v283. React bundle `index-qy8KYBNA.js`. Parent commit `3674f62`, dist `13a227c`.

**2026-04-16 session 70:** Three repair fixes. (1) **Repair Work Order PDF tokens (StrideAPI.gs v38.60.0 → Web App v279):** `handleStartRepair_` (~9635) and the Approve branch of `handleRespondToRepairQuote_` (~9190) were building only the legacy 11-token dict, so the new `DOC_REPAIR_WORK_ORDER` template rendered literal `{{SIDEMARK_ROW}}`, `{{DATE}}`, `{{STATUS}}`, `{{APPROVED_ROW}}`, `{{NOTES_ROW}}`, `{{PHOTOS_ROW}}`, `{{REPAIR_TYPE}}`, `{{ITEM_QTY}}`, `{{ITEM_VENDOR}}`, `{{ITEM_DESC}}`, `{{ITEM_SIDEMARK}}`, `{{ITEM_ROOM}}`, `{{RESULT_OPTIONS_HTML}}`. Extended both call sites to the full 24-token set using `api_esc_` + conditional row HTML snippets matching `Repairs.gs:100-122`. (2) **respondToRepairQuote "Unknown action" banner:** added a defensive `doGet` stub at line 3101 returning `{success:true, skipped:true}` instead of the generic error, so `RepairDetailPanel.handleRespond` no longer surfaces a red banner on any stray GET hit while the real POST path in `doPost:3225` continues to do the work. (3) **VIEW INSPECTION PHOTOS button opens Source Task folder (Triggers.gs v4.7.1):** `processRepairQuoteById_` was falling back to the Item folder because Source Task ID stores plain text, not a hyperlink. Added a fourth tier that looks up the Source Task row in the Tasks sheet and reads its Task ID cell's hyperlink (set by `startTask_` to the task's Drive folder). Rolled out to 49 clients + Web App deployments refreshed on all 48 active clients (Roche Bobois - PDX still needs scriptId onboarding).

**2026-04-16 session 69 (Phase 3, addendum):** Scanner + Labels native React rebuild (replace GAS iframes) + locations Supabase mirror + cross-tenant batch move endpoint + auth cache-wipe fix. **Migration `20260415200000_locations_and_move_history.sql`:** new `public.locations` (Realtime; everyone reads, admin/staff write — RLS) and `public.move_history` (`tenant_id, item_id, from_location, to_location, moved_by, moved_at, source, notes`; admin/staff see all, client sees own tenant). **StrideAPI.gs v38.56.0** added: `sbLocationRow_`, `resyncLocationToSupabase_`, `deleteLocationFromSupabase_` helpers; `createLocation`/`updateLocation`/`deleteLocation` (staff-guarded; soft-delete by `active=false`); `bulkSyncLocationsToSupabase` (admin one-shot backfill); **`batchUpdateItemLocations`** — cross-tenant batch move: resolves `item_id → tenant_id` via single Supabase query against `item_id_ledger` (~50 ms vs 20-60 s sheet-scan), groups by tenant, writes per-tenant Inventory + Move History tab + audit line in Item Notes column, mirrors each item to Supabase inventory, batch-inserts central `move_history` rows. URL-length safe via 200-id chunks. Returns `{updated[], notFound[], errors[], counts}`. **React (stride-gs-app):** new `src/lib/scanAudioFeedback.ts` (WebAudio oscillator beeps; success 880Hz/120ms, error two lower beeps; iOS suspended-context handling; `localStorage` opt-out), `src/lib/parseScanPayload.ts` (normalizes `ITEM:`/`LOC:` prefixes, JSON Stride labels, deep-link URLs, raw codes → `{type, code, raw, source}`), `src/components/scanner/QRScanner.tsx` (camera component ported from production WMS app — primary path: native `BarcodeDetector` API on Chrome/Edge desktop+Android, iOS 16.4+; fallback: `html5-qrcode` npm for older iOS / Firefox; UI: viewfinder with 4 glowing orange corner brackets, animated scan line, "SENSOR ACTIVE" chip, tap-to-start overlay, denied/error states with retry, embedded-iframe-blocked detection with "open in new tab" action, 400 ms repeat dedupe, haptic + beep on success). New `fetchLocationsFromSupabase()` and `fetchItemsByIdsFromSupabase()` in `supabaseQueries.ts` (latter resolves item IDs → inventory rows with client-name enrichment, used by Scanner queue verification + Labels print preview). `useLocations` hook rewrote Supabase-first with GAS fallback + Realtime subscription on `public.locations` (instant propagation of new locations). New API wrappers `postCreateLocation`, `postUpdateLocation`, `postDeleteLocation`, `postBatchUpdateItemLocations`. **`src/pages/Scanner.tsx` full rewrite:** camera scanner card (with smart `LOC:`/`ITEM:` dispatch — location scans auto-populate destination, item scans auto-add to queue with success beep + green hint) + textarea card (handheld scanner / paste). Each queued item shows Client/Vendor/Sidemark/Description/Current Location resolved from Supabase (~50 ms) for verify-before-commit. Inline `+ New Location` modal creates location in CB + Supabase one click. Single Move button fires `batchUpdateItemLocations`. Mobile: sticky bottom bar shows queue counts + target location + Move button (always reachable regardless of queue length); queue rows stack vertically with proper labels; ≥38px tap targets; 16px input font (prevents iOS zoom). **`src/pages/Labels.tsx` full rewrite:** uses `qrcode` npm library for client-side QR rendering (no external API dependency — replaced the dead Google Chart API references). Two modes (Item / Location) with prominent always-visible top tab bar. Per-mode field configuration with toggle + font size dropdown + drag-to-reorder, 4 label sizes (4×6 / 4×2 / 3×2 / 2×1), QR show/hide + size slider, border toggle, save/reset template (localStorage). Print via browser dialog with tight `@media print` rules (kills app chrome). Mobile: input-first layout, Settings collapsed by default, sticky bottom Print bar with live label count, no auto-focus on textarea (was popping the keyboard). **Auth cache-wipe fix:** `AuthContext.handleSession` was calling `cacheClearAll()` on every successful sign-in including same-user refreshes, wiping all 15+ data hook caches every time and forcing a refetch cascade. Now compares `session.user.email` against the cached `AUTH_CACHE_KEY` email — only wipes when the user actually changed (different login, missing cache). Same-user refresh keeps cache intact → instant nav. Security guarantee preserved (cross-user leakage on shared browsers still blocked). **One-time manual step after deploy:** run `bulkSyncLocationsToSupabase` once from DevTools console to populate the new mirror table (otherwise dropdown falls back to GAS — still works, just slower). **Live artifacts:** React bundle `index-BRhAqluY.js` (1940 modules, 1.69 MB — html5-qrcode + qrcode added ~400 KB); source commits `1a30c9f` (scanner port), `f437373` (labels mobile fix), `4921cb9` (auth cache-wipe fix); npm deps added: `html5-qrcode`, `qrcode`, `@types/qrcode`. Old GAS Scanner/Labels HTML web apps in `AppScripts/QR Scanner/` still exist for direct-URL access but are no longer served by the React `/scanner` and `/labels` routes.

**2026-04-16 session 69:** Optimistic bulk updates everywhere + Payments Supabase mirror. **Phase 1 — Optimistic bulk updates (source `b380e83`, bundle `index-CHdkUszQ.js`):** every bulk action now flips affected rows to target state in <50ms, fires batch endpoint in background, reverts per-row on failure. New `src/lib/optimisticBulk.ts` with `applyBulkPatch` + `revertBulkPatchForFailures` helpers. Wired into Tasks (`handleBulkCancel` → `{status:'Cancelled', cancelledAt}`, `handleBulkReassign` → `{assignedTo}`), Repairs (`handleBulkCancelRepairs`, `handleBulkSendQuote` → `{status:'Quote Sent'}`), WillCalls (`handleBulkCancel/Schedule/Release`), Inventory (`handleBulkRequestRepairQuote` via `addOptimisticRepair` with `REPAIR-TEMP-{itemId}-{ts}` ids that reconcile on refetch), Billing (Create Invoices optimistically removes unbilled rows from `reportData` + `useBilling.hideUnbilled`; per-group failures restore from snapshot), Payments (Bulk Void + Bulk Delete in Invoices toolbar + Review tab). **Bulk Charge stays non-optimistic** by design — real money flips to PAID only after Stax confirms. `BulkResultSummary` unchanged (already shows per-row failures). **Phase 2 — Payments Supabase mirror (StrideAPI.gs v38.59.0 → Web App v276, source `7665f80`):** 5 new admin-only Supabase tables — `stax_invoices` (qb_invoice_no unique), `stax_charges` (append-only with timestamp+qb+txn unique index), `stax_exceptions` (timestamp+qb unique), `stax_customers` (qb_name unique), `stax_run_log` (timestamp+fn+summary unique). All with RLS admin/staff SELECT + service_role ALL + REPLICA IDENTITY FULL. New StrideAPI helpers `api_sbUpsertStaxInvoice_` / `api_sbBatchUpsertStaxInvoices_` / `api_sbResyncStaxInvoice_(qbNo)` / `api_sbResyncStaxInvoices_(qbNos[])`. `seedAllStaxToSupabase()` one-shot seed function (run once from Apps Script editor). Write-through wired into 7 mutation handlers: `handleVoidStaxInvoice_`, `handleDeleteStaxInvoice_`, `handleBatchVoidStaxInvoices_`, `handleBatchDeleteStaxInvoices_`, `handleUpdateStaxInvoice_`, `handleResetStaxInvoiceStatus_`, `handleToggleAutoCharge_` (batch), `handleChargeSingleInvoice_` (invoice row resync + `stax_charges` append). Conflict column maps in `supabaseUpsert_` / `supabaseBatchUpsert_` extended. React: 5 new fetchers in `supabaseQueries.ts` (invoices unlimited, charges+run_log ordered DESC and capped at 2000/500). `Payments.tsx` `loadData()` now Supabase-first with GAS per-dataset fallback; `noCache=true` (Refresh button) skips Supabase. Config stays GAS. **Deferred to next session:** StaxAutoPay.gs batch write-through at end of `executeChargeRun_`/`prepareEligiblePending_` (daily 9am PT runs will leave Supabase status trailing by ≤24h on autopay-touched rows until this lands); initial `seedAllStaxToSupabase()` run (must be executed manually from Apps Script editor). 1940 modules, 1.69 MB bundle, all safeguards passed.

**2026-04-16 session 68:** Supabase caches + server-side batch endpoints + many bug fixes. **StrideAPI.gs v38.58.0 → Web App v274.** (1) **5 new Supabase mirror tables:** `claims` (RLS: admin/staff all, client sees own via company_client_name → clients join; seeded 3 rows; write-through in 9 claim mutation handlers), `cb_users` (admin/staff only; seeded 124 rows; write-through in create/update/delete), `marketing_contacts` (server-side filter+search+pagination via `.range()` + ILIKE multi-field; seeded 1,635 rows; write-through in 5 mutation handlers), `marketing_campaigns` (seeded 1 row; write-through in 7 handlers including runCampaignNow), `marketing_templates` + `marketing_settings` (singleton id=1). Marketing Dashboard now computed entirely from Supabase aggregates (contacts counts + rolling campaign totals) — drops Marketing page loads from 2-5 MIN (infinite loop bug) or 2-5s (GAS baseline) to ~50ms. (2) **Marketing infinite-loop fix:** 8 `useApiData` call sites in `Marketing.tsx` were passing new arrow functions per render → 13,000+ canceled requests per page load. All wrapped in `useCallback` with proper deps. One-line root fix, huge user-visible win. (3) **Server-side batch endpoints (eliminate tab-close partial completion):** 4 new `handleBatch*_` handlers following `handleBatchCancelTasks_` template — `batchVoidStaxInvoices`, `batchDeleteStaxInvoices`, `batchScheduleWillCalls`, `batchRequestRepairQuote`. Rewired Payments (Bulk Void, Bulk Delete, Review Delete), WillCalls (Bulk Schedule), Inventory (Request Repair Quote bar + mobile FAB) — one HTTP call instead of N. 4 heavy-side-effect bulks kept as `runBatchLoop` but upgraded: Payments Bulk Charge (2 Stax API calls + real money; red-flag confirm dialog), Billing Create Invoices (Drive PDF + email per group), WillCalls Bulk Release, Repairs Bulk Send Quotes. All now show `<BatchProgress>` overlay with inline "⚠ Keep this page open" warning + `<BulkResultSummary>` per-row modal. (4) **Settings maintenance warnings:** amber callouts on Bulk Sync to Supabase, Purge Inactive Clients, per-client sync banner. BatchProgress component gained inline warning. (5) **Password reset UX:** new `recovery_expired` AuthState variant (Supabase fires `SIGNED_OUT` on expired tokens instead of `PASSWORD_RECOVERY`; previous behavior silently dumped user to Login with no explanation). SetNewPassword renders "This reset link has expired" + Request new link / Return to login buttons. Also added missing `https://www.mystridehub.com` to Supabase Auth URL allowlist — reset flow now works end-to-end. (6) **TRANSFER_RECEIVED email fix (v38.54.1):** bug since shipping — `api_sendTemplateEmail_` called with `destSS` (Spreadsheet) as settings arg and `""` as recipient, immediate "No recipient email address" error, email never sent. Now passes `destSettings` + merged `CLIENT_EMAIL`/`NOTIFICATION_EMAILS` with explicit skip-reason warnings on response. (7) **Dashboard task type filter:** dropdown on Tasks tab button with all 19 service names (from Master Price List seed list, not codes) — Receiving, Inspection, Assembly, Repair (Flat), etc. Multi-select + Select All; persisted per user (`stride_dashboard_typeFilter_{email}` localStorage). Tab badge count reflects filter. (8) **Client dropdown leak fix:** client-role users were seeing full client list on Inventory/Tasks/Repairs/WillCalls/Shipments/Claims dropdowns. Added `dropdownClientNames` memo filtering to `user.accessibleClientNames` for `role==='client'`. (9) **Fragile code guards:** `⚠ FRAGILE` warning comments added to 6 page components + `FolderButton.tsx` after repeated React #300 / folder-button regressions. CLAUDE.md gained two new must-not-do rules. (10) **Rollout for all 47 clients:** `syncAutocompleteDb` action failures (Unknown action + HTTP 404) resolved via `npm run rollout && npm run deploy-clients` — all 47 clients now on fresh Web App versions.

**2026-04-18 session 72 — continued (Inventory inline editing with DB autocomplete):** New `src/components/shared/InlineEditableCell.tsx` click-to-edit cell. Six Inventory columns wired: Reference + Room (plain text), Vendor + Sidemark + Description (autocomplete from per-client `useAutocomplete` hook → `Autocomplete_DB` sheet), Location (autocomplete from Supabase-backed `useLocations` — already Realtime-subscribed). Enter saves, Esc cancels, blur saves with 120ms delay so suggestion-click wins. Optimistic paint via `useInventory.applyItemPatch`, rollback via `mergeItemPatch` on API failure, red corner indicator on fail. Up to 10 filtered suggestions, prefix-match > substring-match ordering. Role-gated — `canEditInventory = admin || staff`; client-role sees plain display (no hover, no click). Backend `handleUpdateInventoryItem_` already supported all 6 fields since v27.0.0 + v38.72.0 fan-out mirror to Tasks/Repairs → so edits propagate through router-level `api_writeThrough_` → Supabase → Realtime → every open tab within ~1-2s. **Room column behavior change:** previously derived from `sidemark.split(' / ')[1]`; now reads the actual `InventoryItem.room` field. Pre-existing rows with empty Room now show `—` instead of a derived value — correct behavior. Source commit `f6dfd13`, dist `9cd04dc`, bundle `index-LsfJ0RN1.js` (1,970 modules, 1,589,019 bytes). No GAS changes, no schema changes. Pure React.

**2026-04-18 session 72 — continued (Realtime sync Phase 2):** Finish cross-tab realtime sync by extending `src/hooks/useSupabaseRealtime.ts` (already mounted in AppLayout, already subscribed on inventory/tasks/repairs/will_calls/shipments/billing/clients) with three new listeners — INSERT+UPDATE on `public.claims` + `public.dt_orders`, INSERT-only on `public.move_history` (append-only log). `entityEvents.EntityType` union extended with `'claim' | 'move_history' | 'order'`. `useClaims` + `useOrders` wired to `entityEvents.subscribe` → `refetch()` on matching events. Pure React; zero GAS or schema changes. After Phase 1a + Phase 2, any write in any tab propagates everywhere in ~1-2s with no refresh. Full data-flow diagram added to root `CLAUDE.md` under new **"Cross-tab Realtime Sync"** section (write path → Supabase → Realtime → refetch pipeline, plus failure-mode table, plus list of what's NOT in Realtime — Autocomplete DB / Stax tables / email templates / cb_users / marketing tables). Deploy: source commit `1b80373`, dist `c6d3be7`, bundle `index-B6rcl-zy.js` (1,969 modules, 1,589,609 bytes). `EntityType` addition is backwards-compatible; `'move_history'` emissions are currently no-ops (no hook listens yet — future Move History panel can subscribe).

**2026-04-18 session 72 — continued (Realtime sync Phase 1a):** Audit + fix of GAS Supabase write-through gaps as prep for Phase 2 Realtime subscriptions. **Key finding:** the router-level `api_writeThrough_(r, entityType, tenantId, entityId)` and `api_fullClientSync_(tenantId, entityTypes)` wrappers — long-existing — already mirror the PRIMARY entity for every update / complete / cancel / start / batch write. A read-only Explore agent audit flagged 24 handlers as "missing mirrors"; 22 of those 24 were router-mirrored and the subagent missed it by only inspecting handler bodies, not router cases. Two genuine gaps patched in **StrideAPI.gs v38.72.0 → Web App v322**: (a) `handleUpdateInventoryItem_` Task/Repair fan-out — handler's field-sync loop (Location/Vendor/Sidemark/Description/Room/Reference) reached Google Sheets but not Supabase; now collects `mirroredTaskIds` + `mirroredRepairIds` and `resyncEntityToSupabase_("task"|"repair", ...)` each after the sheet writes settle; best-effort per invariant #20. (b) `handleAddClaimItems_` — router case uses only `withAdminGuard_`, no `api_writeThrough_`; added `resyncClaimToSupabase_(claimId)` before return. Pre-emptively patched in-handler mirrors for `handleUpdateRepairNotes_` + `handleUpdateWillCall_` were **reverted** after verifying router-level coverage — double-hit, no benefit. Verified router coverage for: `handleCompleteShipment_` (api_fullClientSync_ x4), `handleCompleteTask_`, `handleCompleteRepair_`, `handleRespondToRepairQuote_`, `handleStartTask_`, `handleStartRepair_`, `handleReleaseItems_`, `handleProcessWcRelease_`, `handleTransferItems_`, `handleCreateWillCall_`, `handleUpdateClient_`, `handleOnboardClient_`, all Stax handlers, all Marketing handlers, all batch handlers, Claims handlers, Users CRUD. No React bundle change (pure backend). Source commit `439f6e6`. **Phase 2 deferred** — React `postgres_changes` subscriptions for inventory / tasks / repairs / will_calls / shipments / billing / claims / move_history / orders. Once landed, edits propagate ~2s across all open tabs with zero refresh; today only `locations` has a Realtime channel (Scanner).

**2026-04-18 session 73 (worktree `beautiful-mclaren-e0a899`):** Price List Phase 1 — `service_catalog` + `service_catalog_audit` Supabase tables (schema + RLS), 31 seed services migrated from Quote Tool defaults, `useServiceCatalog` hook (CRUD + field-level audit), `ServiceCard` / `ServiceEditPanel` / `AddServiceModal` components, `PriceList` page with category + feature filter sidebar, search, active toggle. `/price-list` route (admin-only, RoleGuard), "Price List" + `BookOpen` icon added to `ADMIN_NAV`. TypeScript clean (0 errors), 1,976 modules, bundle `index-CBzAZFuG.js`. **Migrations written but NOT applied** — `gh` CLI unavailable in worktree environment; apply via Supabase dashboard SQL editor or `gh workflow run migrate.yml` from main session. Source branch `claude/beautiful-mclaren-e0a899` pushed to origin. Parent source is mid-rebase.

**2026-04-20 session 75 (worktree `elegant-kalam-bca557`) — marathon wrap-up:** Five shipped items stacked on top of the sessions 65-74 mega build. (1) **Optimistic calendar sync model:** per-entity module `EventTarget` buses on `useTasks` / `useRepairs` / `useWillCalls` that broadcast optimistic TEMP- row creates/removes across every hook instance in the tab. `useCalendarEvents` gained a `pending` flag set when `sourceId.startsWith('TEMP-')`; `CalendarEventPill` renders pending events with dashed left border, 60% opacity, italic label, and a pulsing dot via new `stridePulse` keyframe in `index.css`. Auto-reconcile via composite match keys when the real server-issued row arrives. Belt-and-suspenders `useExpectedShipments` hardening: direct `setItems` alongside bus broadcast; unique Supabase Realtime channel name per mount (`expected_shipments_rt_${random}`) to avoid registry collisions across `ExpectedCalendar` + `useCalendarEvents`. (2) **Free-text Carrier** in `AddExpectedModal.tsx` — `<select>` replaced by `<input list="carrier-suggestions">` + `<datalist>` (UPS / FedEx / USPS / Freight-LTL / White Glove / Client Drop-off / Other as suggestions, any free text accepted). (3) **Role-aware Compose recipient filter** (`ComposeMessageModal.tsx`): clients see only admins + same-account coworkers, broadcast `All Staff` / `All Clients` pills hidden; staff/admin unchanged. `useProfiles` extended to join `cb_users.id = profiles.id` and attach `clientSheetId` per profile (best-effort — if join fails, falls open to all). (4) **WhatsApp-style thread header** in `MessagesPage.tsx`: title on line 1, comma-separated participant names on line 2 (computed from unique non-self sender + recipient IDs across the thread, mapped via `useProfiles`), right-side orange pill chip `<Entity> <ID> ↗` that opens in a new tab. Chip renders unconditionally; `&client=<tenant>` appended once a Supabase lookup resolves the entity's tenant (`item_id_ledger.tenant_id` for inventory; `tasks.tenant_id` / `repairs.tenant_id` / `will_calls.tenant_id` / `shipments.tenant_id` otherwise). Client-role users short-circuit to `user.clientSheetId`. Cached per-session in a module-level `Map` (only successful lookups cached so retries stay possible). Per CLAUDE.md deep-link rule #2: without `&client=` the list-page handler never auto-opens the detail panel. (5) **Zip Codes category in Price List** — new `Zip Codes` pseudo-category in the sidebar that swaps the right pane to a `ZipCodeTable` component (inline edit, search, Add Zip modal, 4 stat chips). Supabase `delivery_zones` table was pre-existing from the Quote Tool's 20260417 migration with `zip_code` (pk) / `city` / `zone` / `base_rate` / `pickup_rate` / `service_days`; session 75's `20260420100000_delivery_zones.sql` extends it with `current_rate` / `updated_rate` / `out_of_area` / `call_for_quote` / `active` / `notes` columns, backfills `updated_rate := base_rate`, and seeds 398 zones from `PLT_PRICE_LISTS_v2 - 2025 ZIP CODE LIST (ACTIVE).csv`. New `useDeliveryZones` hook — CRUD + Realtime subscription, writes mirror `updated_rate → base_rate` so legacy Quote Tool readers stay coherent. New `anon_read` RLS (active=true only) so the public `/rates/:shareId` page can render zones without auth. `PublicRates.tsx` refactored to fetch zones in parallel with service rows and render a `PublicZoneTable` when the active tab is the `ZIP_TAB` sentinel. `exportPriceListExcel` accepts a `deliveryZones` arg and emits a new `Delivery Zones` worksheet. Share modal's "Include zip code schedule" toggle is live. TypeScript clean, bundle `index-DUK0tr2v.js`. Source commits `567f66e` (zip codes / public rates / excel / share) + prior session commits `8ca52ce` (client recipient filter), `9001d86` (thread header + chip), `81aaa68` (CLAUDE.md-compliant deep-link resolution), `271454b` (always-render chip fix). Migrations applied via MCP: `delivery_zones_price_list_columns` + `delivery_zones_anon_read`.

**2026-04-18 session 72 (worktree `gallant-stonebraker-995dad`):** Expected operations calendar + admin-set-password escape hatch + worktree `.env` deployment gotcha documented. **(1) Expected calendar (8 new React files):** Second tab on Shipments page (`Received` | `Expected`) with unified month/week calendar feed from three sources — user-authored expected shipments (localStorage, per-user-email keyed via `useExpectedShipments`), scheduled Will Calls (from `useWillCalls`), scheduled Repairs (from `useRepairs`). Color-coded pills (shipment=orange `#E8692A`, willcall=green `#4A8A5C`, repair=blue `#2882C8`), fixed-position dark-card tooltips on hover, 4 dark stat cards (Expected This Week / Month / Scheduled WC / Scheduled Repairs[orange]), legend bar, prev/next/Today controls + Month/Week pill toggle + "+ Add Expected" orange pill. `useCalendarEvents` builds `accessibleClientNames` set for client-role users and filters all 3 event types — defense-in-depth on top of batch-scoped data hook payloads. `+N more` day-cell expand in Month view. `AddExpectedModal` has Add + Edit modes, pre-fills from `editingEvent` prop, two-step Delete button (click → "Confirm Delete" → commit; auto-reverts after 3s), inline toast ("Expected shipment added / updated / deleted"). **(2) Calendar deep links:** Will Call / Repair pills → `useNavigate(\`/{page}?open={id}&client={sheetId}\`)` matching existing list-page deep-link handlers documented in CLAUDE.md. `CalendarEvent` interface gained `clientSheetId` + `sourceId`. Expected-shipment pills → open Edit modal. Month + Week views accept `onEventClick` prop, close tooltip before emitting. **(3) Admin-set-password escape hatch (StrideAPI.gs v38.70.0 → Web App v317):** new `handleAdminSetUserPassword_` endpoint for clients who can't complete self-serve Forgot Password. Admin-only, rate-limited (20/min), min 8 chars. Pages through `auth.users` (up to 5000 via page=X&per_page=1000, handleResyncUsers_ pattern) to resolve email → user ID, then `PUT /auth/v1/admin/users/{id}` with new password. Router case `"adminSetUserPassword"`. React: new `adminSetUserPassword(email, newPassword)` wrapper in `lib/api.ts`, new per-row "Set Password" button in Settings → Users (admin-only, hidden for self), modal with New Password + Confirm + inline validation + green success banner ("Password set for {email}. Share it with the user — they can change it after logging in."). **Self-serve Forgot Password flow untouched** — this is additive. **(4) `update-deployments.mjs` v2.3.0:** removed `!c.isTemplate` guard so Master Inventory Template receives Web App deployments like any active client. **(5) Worktree `.env` deployment gotcha documented in CLAUDE.md DEPLOYMENT RULES:** `.env` is `.gitignored`; a fresh worktree has no Supabase credentials; Vite silently inlines `VITE_SUPABASE_URL = undefined`; production bundle crashes at module load with `Uncaught Error: supabaseUrl is required.` Caught during session 72 by a live-site runtime error, not the build (bundle is structurally valid). Fix: `cp stride-gs-app/.env <worktree>/stride-gs-app/.env` before every worktree build. **Deploys:** source `4b78d3d` (admin-set-password, fast-forwarded through `ce4c793` expected calendar + `70b3ca9` deep links/edit/delete); dist `5a1a52b` → bundle `index-BJvexzOu.js` (1,969 modules, 1,585,152 bytes); StrideAPI push-api 200 OK + deploy-api v317; feature branch `claude/gallant-stonebraker-995dad` pushed to origin. Intermediate broken deploy `0dcde4b` / `index-j4pFpgH2.js` (worktree `.env` missing) was replaced with `cad544f` / `index-DoqY9moO.js` once `.env` was copied, then two subsequent clean rebuilds followed for the deep-links commit and admin-set-password commit.

**2026-04-22 session 78 — Email template cleanup + will call sidemark/reference fix + repair quote blank.** Fix 1: removed "Open Photos" CTA button from 7 email templates (INSP_EMAIL, REPAIR_QUOTE, REPAIR_QUOTE_REQUEST, SHIPMENT_RECEIVED, TASK_COMPLETE, WILL_CALL_RELEASE, REPAIR_COMPLETE) via SQL migration `20260422030000` applied to Supabase `email_templates` table + source .txt files updated. Fix 2: `RepairDetailPanel.tsx` quote amount field now defaults to blank instead of `0` when no prior quote exists. Fix 3: will call emails were missing sidemark + reference in items table — three-part bug in WillCalls.gs: (a) `wccItems` map for WILL_CALL_CREATED stripped vendor/sidemark/reference; (b) release `allItems` builder didn't read sidemark from WC_Items sheet; (c) `buildWcItemsEmailTable_` had no Sidemark column. All fixed in WillCalls.gs v4.6.0 rolled out to 52 clients. Also added `{{SIDEMARK_HEADER}}` placeholder to both will call template bodies (WILL_CALL_CREATED + WILL_CALL_RELEASE) via same SQL migration — GAS had been emitting the token since v4.5.0 but templates never had the placeholder. Supabase MCP wired via new project-root `.mcp.json`. React bundle `index-B_VPpp_d.js`.

**2026-04-22 session 77 — Mirror drift Tiers 1–3 + status undo + activity log + branch-first rule.** Shipment-folder-button bug on legacy imported items traced to `sbInventoryRow_` missing `shipment_folder_url` → expanded into a full `sbXxxRow_` vs `handleGetXxx_` drift audit + three staged fixes + user-requested features. **Tier 1 (v38.102.0, mig `20260422010000`):** inventory mirror + `shipment_folder_url` / `needs_inspection` / `needs_assembly`; React reader stopped hardcoding `shipmentFolderUrl: undefined`; adjacent fix to no-longer-discard `invoice_url`. **Stage A (v38.103.0, mig `20260422020000`):** shipments +`photos_url`/`invoice_url`; will_calls +`created_by`/`pickup_phone`/`requested_by`/`actual_pickup_date`/`total_wc_fee`; repairs +`source_task_id`/`parts_cost`/`labor_hours`/`invoice_id`/`approved`/`billed`; new `will_call_items` table (composite PK, RLS, Realtime) wired through `api_fullClientSync_`; inv-overlay fields NOT snapshotted per Invariant #27 — `_fetchInvFieldMap()` handles Supabase-first overlay. Start-button client-role gate on all 3 detail panels. **Stage B (v38.104.0):** `handleReopenTask_`/`handleReopenRepair_`/`handleReopenWillCall_` + `handleCorrectRepairResult_` (mirror of other builder's `handleCorrectTaskResult_`). Reopen-from-Complete auto-voids Unbilled billing via `api_voidBillingRowsWhere_` (BILLING_LOCKED if invoiced); Reopen-from-Start clears timestamps; WC reopen flips Released WC_Items → Scheduled. LockService-gated, admin|staff only, audit-logged. React Reopen UI via `window.prompt()` on all 3 panels. **Stage C (v38.105.0, PR #4):** filled 20+ audit-log coverage gaps (updateTask* fields, updateRepairNotes, WC CRUD, transferItems per-item on both tenants, billing CRUD, every claim action). `handleBackfillActivity_(sheetId, {force?})` synthesizes historical `entity_audit_log` rows from sheet timestamps (Tasks/Repairs/WC/Inventory/Shipments); tagged `source='backfill:v1'` → `api_tenantBackfilled_` makes re-runs no-ops; router action `backfillActivity` + convenience `backfillActivityAllClientsNow()`. Activity tabs already wired in all panels (Task/Repair/WC/Shipment/Item via TabbedDetailPanel; Claims via its separate History tab backed by `claim_history` + now mirrored to `entity_audit_log`). **Tier 3 resolutions:** (A) skip repair inv-overlay mirror fields, (B) leave QB/Stax out of billing mirror, (C) claims notes-summary reverse-drift was false positive, (D) WC items got their own mirror table. **Branch-first enforcement (PR #3):** CLAUDE.md Must-do rules added — "never commit directly to source; feat/<stream>/<desc>, PR to source" + "deploy AFTER merge." Session started violating this, and a concurrent builder's Dropbox sync mid-session wiped Stage B's in-progress GAS edits, forcing a re-apply. `gh` CLI installed via winget + `gh auth login`; PRs #3 + #4 + this session's docs cleanup PR are the first end-to-end branch → `gh pr create` → `gh pr merge --squash` → pull → deploy cycles. Apps Script versions: 356 (T1) → 357 (A) → 360 (B) → 361 (C). Bundles: `Inventory-NPWHLCo8.js` → `Inventory-DDw9cXgN.js` → `Inventory-C8gg-CyW.js`. **Pending user actions:** (1) `reconcileAllClientsNow` re-run for Stage A column backfill, (2) `backfillActivityAllClientsNow` for Stage C historical activity.

**2026-04-25 session 80 — Back-button URL state + STRIDE LOGISTICS default + intake notification safety + Dropbox signpost.** Hot-mess session that spent half its time on process recovery and half on real work. Started in a stale Dropbox worktree (`.claude/worktrees/happy-noyce-d54cea`) reading an orphan root `CLAUDE.md` from session 77, missed every "branch first / PR / squash-merge / code-review subagent / work from C:\dev" rule from the canonical `stride-gs-app/CLAUDE.md`, committed `06d8283` (StrideAPI.gs v38.119.0 — `handleNotifyIntakeSubmitted_` resolves `{{STAFF_EMAILS}}` token from CB Settings) directly to `source` against the no-direct-commit rule, and built up 7 commits on `claude/happy-noyce-d54cea` (intake auto-inspect + WH alert emails rename, `dt_credentials` admin-write RLS, DT statuses canonical seed + Orders Created column + review-queue filter, DT account_name_map invert {dtName→tenant}→{tenant→dtName} + STRIDE LOGISTICS default, intake notification wiring + trigger, INTAKE_SUBMITTED.recipients={{STAFF_EMAILS}}, useUrlState hook + Phase 1 conversions). Hit Dropbox-induced git pack corruption mid-push (recovered via `git fetch --refetch origin source`). User asked to review CLAUDE.md before committing further — that's when the canonical `stride-gs-app/CLAUDE.md` surfaced and the process drift became visible. Audit revealed all 5 migration files on the branch are byte-identical with `source` (independently shipped via parallel work from a different builder), and the 5 list pages already moved to standalone `/inventory/:id`-style routes (back-button handled natively, useUrlState for `?open=` is obsolete). True remaining surface narrowed to 4 items: (1) `useUrlState` hook + tab-state conversions on Orders/Settings/Billing (`?tab=` push-history), (2) `STRIDE LOGISTICS` default in `dt-push-order` `resolveAccountName` so unmapped tenants land on the house account instead of erroring 400, (3) defensive `BEGIN/EXCEPTION WHEN OTHERS` wrapper around `notify_admins_on_intake_submit` so a future RLS/constraint surprise can't silently roll back a prospect's intake, (4) Dropbox-root `CLAUDE.md` signpost that redirects future Claude sessions to `C:\dev\Stride-GS-app`. Cloned fresh to `C:\dev\Stride-GS-app` per CLAUDE.md, branched `feat/fix/back-nav-default-trigger-safety`, cherry-applied just those 4 changes against current source, ran tsc + `npm run build` (2,043 modules, all sanity checks passed). Migration applied via MCP: `intake_notification_trigger_safe`. PR opened, squash-merged. GAS commit `06d8283` deferred for retroactive PR cleanup at owner pace — flagged but kept since it's small, isolated, and already on `source`.

**2026-04-25 session 80 follow-up — Client filter persists across navigation.** Justin pointed out that the back-button "fix" from earlier in the session only addressed tab state — going from `/inventory` → `/inventory/:id` → back still re-mounted the list page with `clientFilter` reset to "all clients" because the dropdown was held in plain `useState` (sort + status filter + col visibility were already persisted via `useTablePreferences`; the dropdown was the gap). New `src/hooks/useClientFilterPersisted(pageKey, apiClients)` is a drop-in replacement for `useState<string[]>([])` with three-tier hydration: URL `?client=` (resolved through apiClients, wins for email deep-links) → localStorage `stride_client_filter_<pageKey>` (last-used scope) → empty array (falls through to the page's existing role-default-selection effect). Writes to localStorage on every change. Wired into all five list pages (Inventory / Tasks / Repairs / WillCalls / Shipments). Existing `useClientFilterUrlSync` write-side hook unchanged. Branch `feat/fix/client-filter-persists`, PR squash-merged. Build 2,044 modules, deployed.

**2026-04-25 session 82 — DT export.xml sync-back + driver-notes push fix + OrderPage Completion tab.** Two PRs landed back-to-back. **PR #61 (backend):** dt-push-order **v15** falls back to `dt_orders.details` when `order_notes` is empty so the modal's "Notes / Special Instructions" reaches DT's driver-app notes pane (was only showing in dispatcher description). dt-sync-statuses **v7** replaced code-only `get_order_status` with `/orders/api/export.xml?service_order_id=…`, parsing the rich completion payload back into the cache: dt_orders.{started_at, finished_at, scheduled_at, driver_id/name, truck_id/name, service_unit, stop_number, actual_service_time_minutes, payment_collected, payment_notes, cod_amount, signature_captured_at, dt_status_code, dt_export_payload}; dt_order_items.{delivered, item_note, checked_quantity, location, return_codes}; dt_order_history rows replaced-on-sync where source='dt_export'; dt_order_notes ditto. Migration `20260425230000_dt_sync_back_fields.sql` adds those columns and allows source='dt_export' on dt_order_notes. Replace-on-sync is scoped by source so app/webhook-authored rows survive each sync. Timestamps are normalized via `toIso()` so a malformed DT field can't nuke a batch insert. Migration applied + functions deployed via MCP. **PR #62 (frontend):** OrderPage gains a "Completion" tab between Items and Activity that renders driver/vehicle, timing (scheduled/started/finished/actual service time), proof-of-delivery (COD, payment_collected, signature_captured_at), DT-side notes feed, and a driver-activity timeline (with Google-Maps lat/lng deep-link per event). Items tab now badges "Delivered"/"Short", surfaces driver-posted item notes and return codes. New helpers `fetchDtOrderHistory(dtOrderId)` and `fetchDtOrderNotes(dtOrderId)` in `src/lib/supabaseQueries.ts` returning typed `DtOrderHistoryEvent[]` / `DtSideNote[]`. Type extensions on `SupabaseDtOrderRow` / `DtOrderForUI` / `DtOrderItemForUI` for the new sync-back columns; both fetchers (list + by-id) map them. tsc + `npm run build` clean (2,044 modules). **Photo sync deferred:** XML export.xml does not return photo URLs; the JSON Beetrack API at `/api/external/v1/dispatches/:identifier` returns `form.img_url[]` but needs a separate `X-AUTH-TOKEN` from DT support — pending user action.


**2026-05-01 session 87 — Task detail tenant scoping fix (duplicate INSP IDs after transfer).** Item 62630 received under J Garner (auto-inspect) → INSP-62630-1; transferred to Nip Tuck (also auto-inspect) → Transfer.gs cancelled the J Garner row and copied an active task to Nip Tuck with the same ID (per-spreadsheet counter, no global namespacing). Both rows synced to Supabase; clicking either row in the unified Tasks list returned "Task Not Found" because `fetchTaskByIdFromSupabase` used `.maybeSingle()` against `task_id` which fails on duplicates. Fix: optional `clientSheetId` param scopes the query by `tenant_id`. All in-app navigation now propagates the tenant via `?client=<spreadsheetId>` (Tasks list row click, Task ID cell click, `__openTaskDetail`, `?open=` deep-link effect, pending-open effect, ItemPage cross-link). `TaskPage` and `TaskJobPage` parse the URL param and thread it to `useTaskDetail` (new optional second arg `clientSheetIdHint`). Without a hint, the fetcher returns null on multi-row matches so the legacy GAS fallback can resolve. Existing email deep-links already include `&client=`, so they keep working. tsc + `npm run build` clean. PR #156 squash-merged, deployed.

**2026-05-01 session 87 follow-up — Storage RLS underscore tolerance.** Hillary @ Nip Tuck reported missing photos and task notes on inventory items in her client login while admin "login as" worked fine. Diagnosed: `usePhotos` / `useDocuments` upload paths run `sanitizeTenantForPath` which replaces `_` with `-` before composing the storage key (legacy guard for an "Invalid key" error that no longer reproduces — many `_`-prefixed objects exist successfully). The storage RLS policies (`photos_select_tenant`, `documents_select_tenant`) compared the raw JWT `clientSheetId` against `split_part(name,'/',1)`; tenants whose ID contains `_` (Nip Tuck `1_CINtvp...`, ~10 others) got blocked from reading their own photos. Admin/staff bypassed via the role branch, hiding the bug. Fix migration `20260501010000_storage_rls_underscore_dash_tolerance.sql` adds a third OR clause: `split_part(name,'/',1) = replace(clientSheetId, '_', '-')`. Verified as Hillary via simulated JWT: visible objects rose 188 → 280 (+92 recovered). No React changes needed; upload sanitization left in place (low risk). PR #157 squash-merged, migration already applied via MCP.

**2026-05-01 session 87 follow-up #2 — Photo upload scoped by source-filter sub-tab.** On the item detail Photos tab, the source-filter sub-tabs (All / Item / Task / Repair / WC) only filtered display; the upload button hard-coded `entity_type='inventory' + entity_id=item.itemId`, so a photo uploaded while filtered to "Repair" landed on the item. `usePhotos.uploadPhoto` now accepts an optional `{entityType, entityId}` override that flows through both the storage-key composition (`<tenant>/<type>-<id>/...`) and the `item_photos` row stamp (entity_type / entity_id, with item_id rollup preserved). `PhotoGallery` accepts a new `relatedEntities` prop and resolves the upload target from the active sub-filter: single match → routes there, zero or multiple → disable upload button with a tooltip explaining why ("No repair on this item…" / "Multiple repairs — open the specific record…"). `PhotoUploadButton` gains a `disabledReason` tooltip prop. `ItemDetailPanel.PhotosPanelProxy` mirrors the existing NotesPanelProxy pattern, threading linkedTasks / linkedRepairs / linkedWillCalls / shipmentNumber into PhotosPanel. tsc + `npm run build` clean (2,055 modules). PR #158 squash-merged, deployed.

**2026-05-01 session 87 follow-up #3 — Email deep-link self-heal + WillCalls query-param fix.** User reported "Task Not Found" landing from an inspection-email CTA for INSP-63026-1 (Vida Design - Waymark, tenant `1aFFjJwygoQuAglZ9fqYtmEvAE54d-FjVTTssik7pN_k`). Diagnosed: the row exists (single, status=Completed), email link uses correct `?open=<id>&client=<ssid>` format (Emails.gs:678), and admin RLS on `tasks` allows. Bug-for-this-row resolves on a hard-refresh of the React bundle to pick up [#156](https://github.com/Stride-dotcom/Stride-GS-app/pull/156)'s tenant-scoped fetcher. Two real defects found in the broader sweep and shipped: (1) WillCalls.gs:417/787 — WILL_CALL_CREATED + WILL_CALL_RELEASE used the forbidden route-style `/#/will-calls/<id>` with no `&client=`, switched to `?open=<wc>&client=<ss>`. (2) Emails.gs `sendTemplateEmail_` gains a defensive self-heal that rewrites any leftover `/#/<entity>/<id>` URL (across all six entity types) to query-param form with `&client=` before the existing missing-&client= patcher runs — keeps email CTAs durable as templates evolve in Settings → Email Templates. Versions: WillCalls.gs v4.6.1, Emails.gs v4.8.2. PR #159 squash-merged. `npm run rollout && npm run deploy-clients` completed cleanly across all 52 client scripts.

**2026-05-01 session 87 follow-up #4 — Auth JWT staleness fix for cold deep-link clicks.** User re-reported "Task Not Found" for INSP-63026-1 (Vida-Waymark) after the [#156](https://github.com/Stride-dotcom/Stride-GS-app/pull/156) tenant-scope fix shipped. Verified by reading the deployed bundle: scoped fetcher and TaskPage `?client=` parsing are live. Real root cause: `AuthContext.handleSession` fired `supabase.auth.updateUser({role, clientSheetId})` fire-and-forget and transitioned to `authenticated` immediately. The very first Supabase task query in the cold-tab scenario could race a stale JWT whose `user_metadata` lacked role/clientSheetId. RLS for `tasks` keys admin/staff bypass off `user_metadata.role`; with that missing, even admin's scoped lookup returned 0 rows → `setStatus('not-found')` — which is exactly what Justin saw, twice. Subsequent refresh worked because the JWT had been silently brought up to date by the fire-and-forget call. Fix: in both auth paths (cached fast-path + fresh GAS-verify), inspect `session.user.user_metadata` first; only `await supabase.auth.updateUser` when role/clientSheetId differ from the resolved user. Zero added latency when already in sync; one round-trip on the rare cold-mismatch path. PR #160 squash-merged + deployed.

**2026-05-01 session 87 follow-up #5 — Email CTA &client= precedence + fetcher fallback (real fix for repeated "Task Not Found").** After [#156](https://github.com/Stride-dotcom/Stride-GS-app/pull/156), [#159](https://github.com/Stride-dotcom/Stride-GS-app/pull/159), [#160](https://github.com/Stride-dotcom/Stride-GS-app/pull/160) shipped, user re-reported "Task Not Found" from inspection-email CTA for INSP-62945-1 (Vida-Merit) and INSP-63026-1 (Vida-Waymark). Hard-refresh + re-deploys didn't help. Diagnosed via reading deployed bundle (confirmed all my prior fixes were live) + reading the actual sender path. The auto-completion email is sent by StrideAPI.gs `api_sendTemplateEmail_` (NOT Triggers.gs's SH_sendTemplateEmail_ — that's a different code path). `api_sendTemplateEmail_` built the `&client=` suffix from `settings["CLIENT_SPREADSHEET_ID"]` first, falling back to the explicit `clientSheetId` param last. Older client sheets don't have that setting set, so the suffix was empty and the auto-injected "Open in Stride Hub" CTA shipped with `?open=<task>` and no `&client=`. The frontend fetcher (without hint) returned null because of the (then-existing) "ambiguous if >1 row" guard, but with my new fetcher fallback the unscoped path resolves single-row cases too. Two-part fix: (a) Server: reorder precedence so the explicit `clientSheetId` param wins, settings is the fallback. Plus a final safety net that re-checks the chosen `ctaUrl` and appends `&client=` if it slipped through. StrideAPI.gs v38.142.7 → pushed + deployed (Web App v421). (b) Frontend: `fetchTaskByIdFromSupabase` scoped miss falls through to unscoped fetch; multi-row+hint prefers the matching tenant. PR #162 squash-merged + deployed.


**2026-05-02 session 91 — Perf sweep + worktree convention + billing-page audit close.** Late-day session that started as a single production fire (release-items timing out on multi-item orders) and turned into a full sweep of the per-cell `setValue`-in-loop antipattern across the GAS surface, plus the close of the 2026-05-02 billing-page audit. Two parallel-builder HEAD-stomp incidents in the canonical clone forced a process change. Eight PRs landed. **PR [#186](https://github.com/Stride-dotcom/Stride-GS-app/pull/186) (release-items)** — production fire. 50-item release was 3 setValue() per item × 50 × 500ms-2s round-trips = 3-5 min, frequent timeouts. New code collects mutations into in-memory arrays, computes the contiguous range covering changed rows, writes each affected column ONCE via `setValues` over that range. Untouched-but-in-slice rows preserved from the read snapshot. ~3-5 min → <5 sec, constant time. StrideAPI.gs v38.142.8 / Web App v425. **PR [#187](https://github.com/Stride-dotcom/Stride-GS-app/pull/187) (invoice-commit)** — same antipattern in `api_markClientLedgerInvoiced_` + Email Status update inside `handleCreateInvoice_`. 200-line monthly invoice was 800 round-trips and frequently timed out. Drops to constant 4 round-trips for client Billing_Ledger + 1 for Consolidated_Ledger Email Status, regardless of N. v38.142.9 / Web App v426. **PR [#188](https://github.com/Stride-dotcom/Stride-GS-app/pull/188) (cancel-wc)** — Class A bug at smaller scale. Cancellation set 1 setValue per WC_Items row in a loop. 20-item WC = 1-2 min hang (not a timeout; users assumed click hadn't registered). Same recipe. Index-math note: `wciData` here uses `getDataRange()` so includes header at index 0 — sheet row R maps to `wciData[R-1]` (vs PR #186's slice-from-row-2 form). Captured inline. v38.142.10 / Web App v427. **PR [#190](https://github.com/Stride-dotcom/Stride-GS-app/pull/190) (writethrough batch)** — audit revealed every bulk handler also called `api_writeThrough_` afterward to mirror to Supabase, and that path was per-row: each ID = `SpreadsheetApp.openById` + `sheetToObjects_` + linear scan + single-row `supabaseUpsert_` POST. 50-item release-items had ~10-25 sec of writeThrough on top of the (already-fixed) sheet write. New `resyncEntitiesBatchToSupabase_` opens sheet ONCE, reads ONCE, builds id→row map, constructs all upsert objects in memory (using same `sb*Row_` helpers `api_fullClientSync_` uses), single `supabaseBatchUpsert_(table, rows)` (which already chunks at 50 + retries per-row on chunk failure, robustness inherited). `api_writeThrough_` dispatches when `ids.length > 1 && entityType !== "clients"`. Single-ID router cases (~25 sites) unchanged. On batch failure falls back to per-row loop so `gs_sync_events` still gets per-entity failure rows for the React FailedOperationsDrawer. Also updated 4 batch handlers to pass `succeededIds` as array. `handleReleaseItems_` already passed an array — picks up the batched path automatically. 50-item release writeThrough: ~10-25 sec → ~0.5-1 sec. batch-cancel-20-tasks: ~4-10 sec → ~0.5 sec. v38.142.11 / Web App v428. **PR [#191](https://github.com/Stride-dotcom/Stride-GS-app/pull/191) (Class C)** — `handleStartTask_`, `handleCompleteTask_`, `handleCompleteRepair_` were each doing 4-8 separate setValue() calls per request (one row across many columns) — 5-15 sec of latency. Refactored each: read full row once at function start (already happening), replace each `setValue` with `setRowVal_(col, val)` that mutates in-memory rowData and tracks modified columns, single `setValues` over contiguous slice at end-of-try. Untouched-but-in-slice columns write back existing values. Also dropped a now-redundant `SpreadsheetApp.flush()` inside `handleCompleteTask_`'s Custom Price branch. `handleCompleteRepair_`'s `Email Sent At` setValue stays standalone — fires AFTER lock release in separate control flow. Each handler now <2 sec. v38.142.12 / Web App v429. **PR [#194](https://github.com/Stride-dotcom/Stride-GS-app/pull/194) (cancel-wc cleanup)** — two cleanups from #188's "out of scope" list. (1) `handleBatchCancelWillCalls_` was re-reading WC_Items snapshot inside outer WC loop AND writing one setValue per cascaded item. M WCs × N items = M reads + M·N round-trips. 5-WC × 20-item bulk cancel was ~60-180 sec. New code reads `wciData` ONCE before outer loop, accumulates cancel-row sheet numbers across ALL WCs into `wciCancelRows`, single bulk setValues over contiguous range. Index-math: `wciData` sliced from row 2 (no header), sheet row R → `wciData[R-2]`. (2) `handleCancelWillCall_`'s section-5 email-table builder no longer re-reads sheet into `wciMap2`/`wciData2` — reuses section-4 snapshot (item-level fields don't change with cancellation; section 4's bulk setValues only touched Status). v38.143.1 / Web App v431. **PR [#197](https://github.com/Stride-dotcom/Stride-GS-app/pull/197) (worktree convention)** — chore/docs. Two HEAD-stomp incidents this session: builder A ran `git checkout -b ...` to start, builder B then ran `git checkout ...` for theirs in same canonical clone, A's next commit landed on B's branch because both shared one HEAD. Recovered by cherry-picking onto the right branch each time. Second occurrence was while shipping THIS very PR — strong evidence the convention is needed. Added "⚠️ CRITICAL: Worktrees for parallel builders" to CLAUDE.md (and stride-gs-app/CLAUDE.md mirror). Convention: `git worktree add -b fix/<scope>/<desc> /c/dev/stride-<topic> source` per session. Each worktree has own HEAD/index/working-tree, shared `.git`. Git enforces "one worktree per branch," so collisions become physically impossible. Documented session-end cleanup, npm-install + .env-copy requirement (worktrees don't share node_modules; `.env` is gitignored so it doesn't follow), and the existing "Never deploy from a worktree without merging to source first" rule (worktrees for *building* in parallel; canonical clone for *deploying* the merged result). **PR [#200](https://github.com/Stride-dotcom/Stride-GS-app/pull/200) (Category filter — billing-page audit PR 3)** — closes the audit's last open follow-up. Added `Category` MultiSelectFilter between Sidemark and Service on Billing → Report tab. Categories derive from `useServiceCatalog` (already swapped from `usePricing` in [#185](https://github.com/Stride-dotcom/Stride-GS-app/pull/185) / audit PR 2). Selecting categories reactively narrows Service dropdown via `SVC_OPTIONS_FOR_FILTER`. `useEffect` drops service selections that fall out of view when categories change (no ghost selections). `BillingFilterParams.categoryFilter?: string[]` flows through both Supabase path (`.in('category', filters.categoryFilter)`) and GAS path (URL param). `billing.category` already populated on every write; no migration. **End-of-session doc updates** in this PR: corrected the worktree-convention `-b` form (the omit-`-b` form fails because canonical has `source` checked out), added the npm-install + .env-copy first-time-in-worktree steps, refreshed handoff TL;DR with PRs through #200, marked billing-page audit PR 2 + PR 3 ✅, added this session 91 entry to BUILD_STATUS + Session_History. **Parallel work** same day from other builders: [#185](https://github.com/Stride-dotcom/Stride-GS-app/pull/185) services filter from Supabase, [#189](https://github.com/Stride-dotcom/Stride-GS-app/pull/189) storage charges Postgres RPC + GAS commit-rows write-only (progress on long-term step 5), [#192](https://github.com/Stride-dotcom/Stride-GS-app/pull/192) respect `client.separate_by_sidemark`, [#193](https://github.com/Stride-dotcom/Stride-GS-app/pull/193)+[#195](https://github.com/Stride-dotcom/Stride-GS-app/pull/195) billable task add-on services, [#196](https://github.com/Stride-dotcom/Stride-GS-app/pull/196)+[#198](https://github.com/Stride-dotcom/Stride-GS-app/pull/198)+[#199](https://github.com/Stride-dotcom/Stride-GS-app/pull/199) BillingPreviewCard / BillingCalculator port from WMS.

**2026-05-02 session 92 — Transfer-system overhaul + Invoice Review tab + client inline edits + realtime/access fixes.** Multi-thread session covering (a) the long-tail of "transferred items break X" bugs Justin had been hitting, (b) client-portal usability gaps, and (c) a real Invoice Review tab to replace the empty stub. **Transfer system, three layers.** (1) `inventory_live` view (security_invoker=true, excludes `status='Transferred'`); `fetchItemByIdFromSupabase` now reads from the view so the historical row can never reach the detail page. Tenant-scope param kept as defense in depth. (2) New GAS helper `api_postTransferSupabaseSideEffects_` runs after `fullClientSync` inside the `transferItems` case handler — migrates `entity_notes` (entity_type='inventory') + `item_photos` rows from source tenant to destination, strips transferred items from any open `will_calls` on the source tenant (cancels the WC if its `item_ids` becomes empty). Storage RLS `photos_select_tenant` extended with row-based OR clause so photos remain readable to new tenant without physically moving objects. New `supabasePatch_` + `supabaseSelect_` helpers next to `supabaseUpsert_/supabaseDelete_`. (3) Provenance columns: `inventory.transferred_from_tenant_id` + `transferred_at`, indexed. Backfill DDL migrated 31 historical transfer pairs in one pass — items already transferred (62596, 62632 etc) immediately had notes/photos surface for the new owner without waiting for the next transfer. StrideAPI.gs v38.145.0 / Web App v435. Migrations: `inventory_live_view_and_transfer_provenance`, `photos_storage_rls_via_item_photos_tenant`, `backfill_transferred_item_aux_tables`. **Invoice Review tab (real impl).** Old code: `useState<InvoiceReviewRow[]>([])` — stub, never fetched. New `InvoiceReviewTab` + `InvoiceReviewLineItems` in Billing.tsx: Supabase-only read of `billing` where `status IN ('Invoiced','Void') AND invoice_no IS NOT NULL`, group by `invoice_no` into summary list (client / invoiceDate / total / lineCount / status / Mixed badge for partial voids), per-invoice expand to show line items with svc badge + DeepLink to Item / Task / Repair / Shipment + notes; search across invoice# / client / item descriptions / item IDs; filters (client, status, invoice-date range); sortable headers (invoiceNo / clientName / invoiceDate / total); per-invoice PDF link (opens `invoice_url` new tab); per-invoice Void button (staff/admin only) — optimistic update flips status + child lines to Void immediately, on failure restore from snapshot + re-throw so WriteButton flashes red ✗; custom `loadingText="Voiding…"` / `successText="Voided"`. Subscribes to `entityEvents('billing')` for cross-tab refresh. **New API + GAS:** `postVoidInvoice` in api.ts; `handleVoidInvoice_` + case wiring in StrideAPI.gs (withStaffGuard + withClientIsolation, fullClientSync + audit row). **Client inline edit on Inventory.** New `canEditClientFields = canEditInventory || user?.role === 'client'` predicate gates the Reference + Room cells only. Operational fields (vendor / sidemark / description / location) stay admin/staff-only. Critical bug found in same change: columns useMemo deps array tracked only `canEditInventory` — without `canEditClientFields` in deps, cells stayed disabled forever for clients because the only signal in the deps array doesn't change for them. **ItemDetailPanel realtime emit.** Save handlers (field edits + coverage edits) called `postUpdateInventoryItem` but never fired `entityEvents.emit('inventory', itemId)`. Other consumers learned about the change via Supabase realtime echo only — laggy enough to navigate back before it landed. Added the missing emit on both paths. **I/A/R/W/D badges polish.** ItemIdBadges gained third state ('failed') for the I badge — bright red `#DC2626` with `fontWeight: 900` when an INSP task is Completed with `result='Fail'`. Plumbed through `useItemIndicators` (now also pulls `result`) and every consumer. Also fixed thin-letter sizing — all five badges sit in `minWidth: 12px` square with centered text. **Item access check parent-clients fix.** `useItemDetail`'s access check compared `result.clientSheetId` against `user.clientSheetId` (single primary) — switched to `accessibleClientSheetIds.includes(...)` matching every other detail hook. **Other small fixes:** `useOrders` removed `isSupabaseCacheAvailable()` gate so Delivery Orders works during impersonation; CreateDeliveryOrderModal client picker filters to `accessibleClientNames` for non-staff; client filter dropdown leak ("51 selected") fixed across Inventory / Tasks / Repairs / WillCalls / Shipments / Orders pages via ONE-TIME ref-guarded init that always force-overwrites for clients but only auto-fills if empty for staff. **Mid-session correction:** I told Justin to "paste GAS changes into the Apps Script editor" — he correctly pointed me at CLAUDE.md's Deploy Reference which lists `npm run push-api && npm run deploy-api` (auto-deploys Web App, no paste). Updated `feedback_run_deployments.md` to call this out explicitly so it doesn't recur. End-of-session doc updates: BUILD_STATUS Current Versions table → v38.145.0 / Web App v435; new Recent Changes section; CODE_MAP Billing + Inventory rows updated with new files/migrations; this Session_History entry.

**2026-05-13 — Public service-request form RLS RETURNING fix.** Customer reported the public delivery order form failing 100% of submissions with the generic "We could not submit your request" error since at least 2026-04-27 (the last successful `source='public_form'` insert in `dt_orders`). Diagnosed against production via curl + the Supabase MCP after the project's `get_logs` tool errored on a BigQuery analytics reservation 404. Schema, constraints, and the existing `dt_orders_insert_public_form_anon` WITH CHECK all matched the form's payload — yet anon REST POST returned 401/42501 even with a brand-new `WITH CHECK (true)` permissive INSERT policy added. Smoking-gun bisection: anon POST WITHOUT `Prefer: return=representation` returned 201; same payload WITH the header returned the 42501. supabase-js's `.insert(...).select(...)` chain emits that header → PostgREST issues `INSERT ... RETURNING id, dt_identifier` → RLS evaluates SELECT permission for the row → anon had no SELECT policy on `dt_orders` → "no policy permits this row" surfaced as the misleading WITH CHECK error code. Fix: migration `20260513124647_dt_orders_anon_select_just_inserted.sql` adds a tightly-scoped anon SELECT policy (`source='public_form' AND review_status='pending_review' AND tenant_id IS NULL AND created_at > now() - interval '30 seconds'`). 30-second window prevents enumeration of older submissions; everything else mirrors the existing INSERT WITH CHECK. Applied via `apply_migration` MCP and verified live (curl POST with the header now returns 201 with the row). No React changes needed. Followup backlog item recorded: replace anon direct-INSERT with a SECURITY DEFINER RPC that wraps order + items in one transaction and never exposes RLS-readable rows to anon, which would let us drop both anon INSERT policies AND this new SELECT policy.

**2026-05-13 follow-up — Public-form proper fix: SECURITY DEFINER RPC.** Replaces the anon direct-INSERT path (and yesterday's tightly-scoped anon SELECT policy hack) with `public.submit_public_request(p_order, p_items)` — SECURITY DEFINER, locked search_path, REVOKE-then-GRANT to anon+authenticated only. Single transaction wraps order + items insert (no half-success orphans). Forces source=public_form, review_status=pending_review, tenant_id=NULL, created_by_user=NULL, created_by_role=public, pricing_override=true, customer_tax_exempt=NULL regardless of caller input — security-critical fields locked server-side. Defense-in-depth RAISE on tenant_id/created_by_user tampering for clear error messaging. React form switched to `.rpc('submit_public_request', {p_order, p_items})` — items collection no longer threads dt_order_id. Code review caught two bugs in the first commit (original_quantity always landing at 1 due to wrong `NULLIF(a,b)` semantics; quantity cast to `::integer` vs schema's `numeric`) — fixed in a corrective migration before merge. Verified live: tampering returns 400 with the right error; quantity=3 → original_quantity=3. Two-phase rollout — three legacy anon policies (dt_orders_insert_public_form_anon, dt_orders_select_just_inserted_public_anon, dt_order_items_insert_public_form_anon) intentionally left in place as safety net; follow-up migration drops them once the RPC has soaked.

**2026-05-13 housekeeping — Gitignore supabase/.temp.** PR #384's deploy swept two transient Supabase CLI files into source via `git add -A` (`stride-gs-app/supabase/.temp/cli-latest`, `linked-project.json`). Added `supabase/.temp/` to `.gitignore`, `git rm --cached` the two files. Recreated by `supabase link` automatically — no source-of-truth value.

**2026-05-13 follow-up — dt-sync-statuses cron schedule.** Justin asked how to cancel a delivery order from inside the app. Investigation found two blockers: (a) DT's API has no documented cancel endpoint (only `add_order` upsert; no `delete_order`/status-update visible in our code or the JS-rendered public docs portals; the `_archive/reference/` PDF is image-based), and (b) DT doesn't fire a webhook on cancel (confirmed Alert_Type values: Started/Unable_To_Start/Unable_To_Finish/In_Transit/Service_Route_Finished/Notes/Pictures — no Cancel). However, `dt-sync-statuses` already has full cancel-handling: pulls export.xml per order, mirrors status, soft-deletes items DT no longer returns. Cancelled orders flip `dt_orders.status_id`→32 (CANCELED, category 'cancelled'), which auto-clears the "D" inventory badge because Inventory.tsx filters cancelled-category orders out of dtOpenItems/dtDoneItems. The only missing piece was triggering the sync without operator action. Migration `20260513134114_dt_sync_statuses_cron_schedule.sql` ensures pg_cron+pg_net are available; the actual `cron.schedule()` was applied separately via MCP execute_sql (service-role JWT inlined per the existing intake-reminder cron pattern, not committed to source). Job: `dt-sync-statuses-active-every-5min`, schedule `*/5 * * * *`, body `{"scope":"active"}`. Workflow: cancel in DT → cron picks up within 5 min → app reflects + D badge clears. Cost ~360-600 DT API calls/hr (within 1000/hr rate limit). The original in-app Cancel button is deferred until we know DT's cancel API — design notes captured in this session's transcript.

**2026-05-14 — P+D create regression: NULL id from mixed-shape Supabase batch insert (PR #424).** User reported new Pickup+Delivery orders failing every save with `P+D promote items insert failed: null value in column "id" of relation "dt_order_items" violates not-null constraint`; retries had left 3 orphan dt_orders pairs behind (ROC-00087/88/89-101400941, six rows total, item_count=0 each). Root cause traced to PR #389's PU→Delivery item-sync work: to stamp `parent_pickup_item_id` on the delivery-side mirror row, `buildPDItemRows` started generating a client-side UUID and assigning `id: pickupRowId` on that ONE row in the batch, while the inventory / delivery-mirror / delivery-only ad-hoc rows kept their previous no-`id` shape. Supabase JS's batch insert normalizes the array to the union of keys across all rows, so rows missing a key that another row has get that key sent on the wire as explicit NULL — which OVERRIDES the column's `DEFAULT gen_random_uuid()` rather than letting the default fire. Before PR #389 no row supplied `id`, PostgREST omitted the column entirely, and the DEFAULT generated UUIDs server-side. PR #389's single explicit id poisoned the whole batch. Fix: assign `crypto.randomUUID()` to every row built in `buildPDItemRows` so the batch has uniform shape. No schema change, no migration. Long comment block on the helper documents the failure mode + names the prior regression so the next builder doesn't re-introduce it on a different selectively-set column. Cleanup: 6 orphan dt_orders rows hard-deleted in a single transaction after the fix deployed (linked_order_id NULL'd first since it has no ON DELETE clause). Other six `dt_order_items.insert(...)` call sites in the same file are uniform-no-id arrays today and stay correct — flagged as latent same-class trap if a future change adds an explicit id (or any other server-defaulted column) to a subset of rows. Build process used the canonical worktree workflow end-to-end: branch off source in worktree → commit → tsc → npm run build → code-reviewer (Opus 4.7) approved → PR #424 squash-merged → npm run deploy from canonical clone.

**2026-05-14 — `scripts/deploy.js` step-3 push-failure propagation (PR [#428](https://github.com/Stride-dotcom/Stride-GS-app/pull/428)).** Same-day photo-default deploy from canonical printed `✓ all steps complete` and exited 0 even though step 3's `git push origin source` was rejected non-fast-forward — local source had diverged (stale commit on local + newer commits on origin). Operator believed the deploy landed; 30 min spent chasing why the change wasn't live. Root cause: in deploy.js step 3's `!hasUncommittedChanges` branch, the rev-list ahead-check AND the `pushWithRetry` call were both wrapped in a single `try { rev-list + push } catch (_) { /* ignore */ }` block. The empty catch was meant to tolerate `origin/source` not being fetched locally (benign rev-list failure), but it indiscriminately swallowed `pushWithRetry` rejections including both the initial push failure and the schannel-TLS-retry failure inside `pushWithRetry`. Fix: narrowed the try/catch to wrap ONLY the `git rev-list --count origin/source..HEAD` execFileSync call — the push moved outside any try/catch so its rejection propagates to Node's default uncaught-exception handler and the script exits non-zero. Hardened `pushWithRetry` in the same PR: wrapped the second-attempt execSync in its own try/catch that prints `✗ push failed on both attempts: git push origin source` with both attempts' error messages and re-throws, so the operator sees the real reason (non-fast-forward / auth / etc.) instead of a bare schannel-TLS hint. The retry already propagated via uncaught execSync before, but the explicit throw + diagnostic is refactor-safe. The primary step-3 push (the `hasUncommittedChanges` branch at line 175) was already a bare call with no surrounding catch — fix doesn't regress that path. Verification: tsc --noEmit clean, npm run build clean (2,282 modules, all 5 build steps + dist referential-integrity passing), code-reviewer (Opus 4.7) returned no Critical/Important findings. Script-only change — no React/GAS behavior shift — but full canonical-clone deploy run anyway to exercise the new code path and confirm zero-regression on the happy path.

**2026-05-14 — `repair_items` CASCADE FK (PR [#430](https://github.com/Stride-dotcom/Stride-GS-app/pull/430)).** Morning check turned up 15 orphan `public.repair_items` rows for Seva Home (tenant `1_E5xG0PZR8pGxxFVudrRDLU8NyLYDIXkG4rbPcBfj0s`) — parent `public.repairs` rows had been deleted manually on 2026-05-13 during PR #397 (multi-item repair) testing, but child rows survived because the join columns were never bound by an FK. Manual cleanup: restored one parent (`RPR-63280-1778715634749`), deleted 9 ghost orphans. New migration `20260514120000_repair_items_cascade_fk.sql` adds the missing FK with `ON DELETE CASCADE` — three parts: Guard 1 `RAISE EXCEPTION` if any orphans still exist at apply time, Guard 2 idempotently adds `UNIQUE (tenant_id, repair_id)` on `public.repairs` if no matching constraint exists (production already had `repairs_tenant_id_repair_id_key` so this skipped), then the FK itself wrapped in a `DO` block for idempotency. Caught at apply time: Guard 2 had a `name[]=text[]` type mismatch that raised 42883 — fixed inline with `::text` casts on `pg_attribute.attname` and the literal array, applied successfully on retry, pushed the corrected SQL back to the PR file so source matches what shipped. Postgres-only change — no React/GAS/edge-function deploy needed. Migration applied to production via `apply_migration` MCP and verified with `pg_constraint` showing `repair_items_parent_fk FOREIGN KEY (tenant_id, repair_id) REFERENCES repairs(tenant_id, repair_id) ON DELETE CASCADE` live.

**2026-05-14 — Delivery → P+D conversion in-place (PR [#431](https://github.com/Stride-dotcom/Stride-GS-app/pull/431)).** Justin asked whether a client could decide AFTER a delivery order had been created that they also need a pickup, and have the operator add the pickup leg without losing the inventory items already assigned to the delivery. Pre-PR behaviour: the modal's mode-card UI wasn't gated for edit mode, so clicking "Pickup + Delivery" on a single-leg-delivery edit was allowed — but the save handler's three existing branches (P+D edit, single-leg edit, brand-new create) didn't cover the conversion case, so save fell through to the create path and produced a duplicate disconnected P+D pair while orphaning the original delivery. Decisions locked before build via AskUserQuestion (identifier scheme = keep delivery's ROC-N-D + mint matching -P; DT republish = both legs; gate = any non-cancelled non-completed delivery). Implementation in `CreateDeliveryOrderModal.tsx`: new save branch inserted before the existing P+D-edit branch with a four-condition gate (`editingDraftRowIdRef && !editingPickupRowIdRef && mode === 'pickup_and_delivery' && originalOrderTypeRef === 'delivery'`) routes only the conversion case. Inside: load existing identifier + tenant → derive new -P by trailing-`-D` substitution → pre-flight collision check (branched on tenant_id NULL using `.is('tenant_id', null)` vs `.eq` since PostgreSQL UNIQUE allows multiple NULL-tenant rows with the same identifier) → INSERT new pickup row with pricing fields NULL'd per the create-P+D convention (line ~2520) → UPDATE existing delivery row's order_type + linked_order_id + operator edits + taxFields → roll back the just-inserted pickup if UPDATE fails (no orphan half-pair) → DELETE + INSERT items via buildPDItemRows (inventory FKs preserved via selectedInvItems[].inventoryRowId; item row UUIDs change but `parent_pickup_item_id` is the only external FK to dt_order_items.id and the helper re-stamps it) → audit log row with `action='convert_to_pd'` → repushOrdersAfterEdit on both legs (the dt-push-order edge function follows linked_order_id via its `isPDDeliveryPrimary` branch ~line 881 so pushing the delivery handles both legs; brand-new pickup with `pushed_to_dt_at IS NULL` is skipped client-side but picked up by the edge function via the link). New refs `originalStatusIdRef` + `originalOrderTypeRef` captured on edit-load. Mode-card UI gate: P+D card disabled with title-attribute tooltip when source delivery is terminal (status_id ∈ {7=arrived, 9=deleted, 100=delivered, 102=partial_delivery} or review_status='cancelled'). Amber "Adding a pickup leg to this delivery on save" hint badge rendered on the P+D card whenever the operator has it selected during a valid conversion — visible cue that this is a destructive-ish action without a full confirm step. `convert_to_pd` added to the `DtOrderAuditAction` union. Build process: branch in fresh worktree `C:\dev\stride-convert-to-pd` → two commits (feature + audit-type extension) → tsc clean → npm run build clean → code-reviewer (Opus 4.7) approved with no Critical/Important blockers (one procedural Critical: rebase onto current source — PRs #428 + #429 had landed mid-flight — handled before opening PR) → PR #431 squash-merged → `npm run deploy` from canonical clone. Scope deliberately narrow: only delivery → P+D today; pickup-only → P+D has different pickup-contact field semantics (separate PR if needed); reverse conversion (P+D → delivery by dropping pickup leg) not addressed. Pre-existing latent risk flagged in code review: `selectedInvItems` derivation silently drops items that have been released/transferred since the order was created — affects the existing P+D-edit and single-leg-edit branches too, not a regression. Backlog item.

**2026-05-14 — CB Consolidated_Ledger auto-reconcile on QBO push (StrideAPI.gs v38.222.0).** Justin flagged INV-001152 (Vida Design - Merit, sidemark `MERIT MODEL`, $50) failing to push to QBO while siblings in the same batch (INV-001111/112/113) pushed cleanly. Diagnosis via the `qbo_push_jobs` row showed React correctly sent both of INV-001152's ledger_row_ids in the job's `ledger_row_ids` array, but GAS's grouping loop at `handleQboCreateInvoice_` ~line 41090 only built 3 invoiceGroups (`total_count=3`). Grouping reads CB Consolidated_Ledger filtered by STATUS='INVOICED' + LEDGER ROW ID match; INV-001152's two rows in CB had drifted (missing or non-Invoiced) so they got silently dropped from the push payload. Same drift on 6 sibling invoices stuck since 2026-05-05/06: INV-001015, 001038, 001076, 001099, 001100, 001147. Root cause traced to `handleCreateInvoice_`'s CB-write dedupe-skip at ~line 25303 (bare `continue`, no UPDATE), compounded by the symmetry gaps in `handleVoidInvoice_` and `handleReopenTask_` (open backlog #5/#7 — both intentionally **not** patched per MIG-005, which retires CB entirely in P4b). Strategic call with Justin: don't harden the dying GAS code; bridge with a reconciler that reads `public.billing` (post-migration source of truth) and brings CB into agreement on every push. New helper `reconcileCbFromBilling_(invoiceNos)` reads `public.billing` non-Void rows, indexes CB by Ledger Row ID, UPDATEs matched rows in place (overwrites Status / Invoice # / Sidemark / Client / svc fields / Invoice Date — preserves QBO INVOICE ID / Email Status via slice()-then-overwrite-mapped-only) or APPENDs fresh rows for IDs missing from CB. `handleQboCreateInvoice_` derives the covered invoice numbers from a single `supabaseSelect_` on the incoming ledger_row_ids and calls the reconciler before reading CB — every push self-heals. Defensive cross-tenant guard (skip + log if existing CB row's Client Sheet ID disagrees with incoming `tenant_id`), input sanitized against `^[A-Za-z0-9_\-]+$` before PostgREST `in.(...)` injection, `SpreadsheetApp.flush()` before return. Wrapped in try/catch so a Supabase outage degrades to prior behavior. Code review (Opus 4.7 subagent) caught 4 Important items — all fixed in the second commit (Invoice Date mapping, cross-tenant guard, flush, 6-min-exec TODO). Apps Script version 517 deployed live. Justin retries QBO Push for the 7 stuck invoices to verify. Saved feedback memory: never harden GAS for CB-symmetry bugs — bridge with SB-authoritative reconcilers, structural fix lives in the SB-native handlers in P4a/P4b.

**2026-05-14 — Migration parity infrastructure, Phase 1 (PR pending).** Extends the existing parity substrate (PR #310 / migration_parity_substrate) with lifetime counters (`total_checks`, `mismatch_count`) + GENERATED `match_rate numeric(5,2)` on `feature_flags`, `input_summary` + FK + `supabase_realtime` publication + authenticated INSERT RLS on `parity_results`. Seeds 4 missing function_keys from Justin's canonical 24-function list (`releaseWillCall`, `generateStorageCharges`, `sendTaskCompleteEmail`, plus `updateShipment`→supabase + `generateStorageCharges`→supabase overrides); preserves existing keys (`processWcRelease`, `commitStorageCharges`, `cancelRepair`, etc.) since deployed GAS already references them. New `src/lib/shadowRunner.ts` fires the shadow backend non-blocking after the primary returns, hashes both via stable-key SHA-256, writes `parity_results`, bumps counters (read-then-PATCH with acknowledged race window). New `src/lib/apiCall.ts` is the routing wrapper — `apiCall(key, gasFn, sbFn?, opts?)` reads a module-level snapshot mirrored from `FeatureFlagContext` (so non-hook callers don't round-trip to Supabase), routes to active backend, falls back to GAS with a warning when SB isn't wired, fires `runShadow` when `parity_enabled` + `shadow_backend` are set. `MigrationSettingsTab` extended with a "Match rate" column (green ≥99.5% / amber ≥95% / red <95%) showing lifetime check count alongside the existing 7-day mismatch indicator. Migration `20260514170000_parity_infra_phase1_extend.sql` applied to dev DB. Most of Justin's spec was already shipped from prior work (FeatureFlagContext, MigrationSettingsTab, both tables seeded with 28 keys) — this PR is the delta to satisfy the build-spec column list + add the shadow-runner + routing wrapper that were the gating Phase 1 deliverables.

**2026-05-15 — Shared-doc anon spinner hang fixed (PR #443, React-only).** Justin: drivers on the public shared-attachments page (`/#/shared/attachments/{shareId}`) saw doc metadata but PDFs never opened — infinite spinner, blocking live deliveries. Verified RLS is share-scoped (not blanket): `documents_anon_read_via_share` + `documents_storage_anon_read_via_share` both gate on an active non-expired `photo_shares` row containing the doc id (migrations 20260426120000 / 20260514120000). Root cause: anon `createSignedUrls` on the private `documents` bucket fails silently (the `/object/sign` path swallows the storage RLS denial) and `loadDocs` discarded the error. Fix in `PublicPhotoGallery.tsx`: removed pre-minted signed URLs; per-click lazy `supabase.storage.download()` -> blob URL -> new tab, errors surfaced inline (no more endless spinner). Locked-in code-reviewer subagent type was unavailable in the harness session — applied `.claude/agents/code-reviewer.md` checklist directly (same Opus 4.7 model): no landmines, one non-blocking Important (post-await `_blank` may be popup-blocked in mobile webviews — fast-follow). Could NOT verify prod RLS or deploy the `get-shared-doc` Edge Function / migration fallback: no Supabase MCP or access token in this session. If `.download()` still fails for anon, the storage policy from migration 20260514120000 is not live in prod and needs a separate deploy. Deployed React via `npm run deploy`; bundle live on origin/main.

**2026-05-15 — Shared-doc service-role proxy, closes the #443/#444 open risk (PR pending).** Built the `get-shared-doc` Edge Function (`supabase/functions/get-shared-doc/index.ts`) the prior session couldn't: GET `?share_id=&doc_id=` → service-role lookup that re-implements the anon RLS predicate exactly (`photo_shares.active` + `expires_at` NULL-or-future + `doc_id ∈ doc_ids` + `documents.deleted_at IS NULL`), single generic 404 for every deny case (no enumeration oracle), UUID-validated `doc_id`, sanitized `Content-Disposition`, `Cache-Control: no-store`, `--no-verify-jwt` by design (browser opens it directly; the high-entropy share slug is the gate). `PublicPhotoGallery.tsx` `openDoc()` rewired from `supabase.storage.download()` to `fetch({VITE_SUPABASE_URL}/functions/v1/get-shared-doc?…)`, same inline-error UX, photos path untouched. tsc + `npm run build` clean. Code review: registered `code-reviewer` subagent type still unavailable in this harness — ran its exact `.claude/agents/code-reviewer.md` definition via Opus general agent; no Critical, one Important (wildcard CORS on service-role bytes) accepted as a conscious decision (128-bit slug gate, parity with prior anon path, repo edge-fn convention). Same blocker as #443: no Supabase MCP / `SUPABASE_ACCESS_TOKEN` in this environment, so the Edge Function is committed but **NOT deployed** — exact deploy command + verify step handed to Justin in BUILD_STATUS "Pending User Actions". React side shipped via `npm run deploy` (no regression: a missing function returns a visible 404, same diagnostic state as #443, not the old silent hang).

**2026-05-17 — Orders service-date range filter (PR #452, React-only).** Justin: filter the delivery Orders list by service/scheduled date and keep the filter on browser-Back from an order. Added module helper `orderServiceDateISO()` mirroring the Service Date column precedence exactly (`localServiceDate` -> DT `scheduledAt` in `America/Los_Angeles`, `en-CA` YYYY-MM-DD key); `filteredByDate` useMemo as the final link in the existing filter chain before `useReactTable` (no new Supabase query). Range stored in URL via `useUrlState` (`?from=&to=`, default push) so back-from-detail restores it like the client/status/search filters. UI: native date inputs + Today (Pacific) quick-set + Clear dates, folded into the global Clear filters reset. Code review (registered code-reviewer subagent type still unavailable in harness; ran its `.claude/agents/code-reviewer.md` checklist via Opus general agent): no Critical, one Important — over-narrow range showed a bare empty grid — fixed with a "No orders match the current filters" empty state. tsc + `npm run build` clean. Squash-merged #452, deployed via `npm run deploy` from the canonical clone; bundle live on origin/main.

**2026-05-17 — Removed retired Rate Parity tab from Billing (PR #454, React-only).** Justin: the old MPL-sheet-vs-Supabase Rate Parity tab is dead, replaced by the Migration Dashboard at `#/migration`. Deleted `ParityMonitor.tsx`, `LiveBillingEvents.tsx`, `useParityMonitor.ts`, `useBillingParityLog.ts` + the now-orphaned pricing-parity type/fn cluster in `api.ts` (verified consumed only by the deleted files via whole-tree grep). `Billing.tsx`: dropped `parity` and the long-dead `review` from `BillingTab`/`VALID_BILLING_TABS`, removed import+button+render+unused `Scale` icon, fixed stale URL-state comment. Final tabs: Report, Storage, Activity, Coverage; stale `?tab=parity|review` falls back to `report`. Net -895 lines. Code review via `.claude/agents/code-reviewer.md` checklist (registered subagent type unavailable in harness; Opus general agent): no Critical/Important, one stale-comment nit fixed. tsc + build clean. Squash-merged #454, deployed from canonical clone via `npm run deploy`; bundle live on origin/main. CODE_MAP Billing section pruned of the deleted pages/hooks.

**2026-05-18 — COD orders always charge sales tax (PR #457, React-only).** Justin: COD (`customer_collect`) is a direct-to-consumer sale the customer pays the driver for, so it must always be taxed even when the client is resale tax-exempt. `CreateDeliveryOrderModal.tsx`: added `DEFAULT_TAX_RATE = 10.4` (Kent WA fallback) + one memoized `effectiveTaxRatePct` (null = not taxable) as the single source feeding the tax math, the `taxFields` snapshot, and the displayed breakdown. COD uses client `tax_rate_pct` when usable else the default; `bill_to_client` unchanged. `taxFields` records `customer_tax_exempt:false` + effective rate for COD. Fixed the pricing-summary gate so COD-for-exempt-client now shows Subtotal+Sales Tax lines (was hidden, total jumped unexplained). Code review via `.claude/agents/code-reviewer.md` checklist (Opus general agent, registered subagent unavailable): one Important UX finding (hidden breakdown) fixed in follow-up commit, no Critical; React-billing posture unchanged (existing Task 8a snapshot pattern). tsc + build clean. Squash-merged #457, deployed from canonical clone; bundle live on origin/main (b40a4d7).

**2026-05-18 — CRITICAL: onboarding created Supabase auth users with empty raw_user_meta_data (PR #459, GAS-only).** Since 2026-04-11 every onboarded client user had no role/clientSheetId in `user_metadata` — 73 users invisible app-wide because all RLS keys off `auth.jwt()->'user_metadata'->>'role'/->>'clientSheetId'/->>'accessibleClientSheetIds'. Root cause: `createSupabaseAuthUser_` never sent `user_metadata` on the GoTrue admin create. `apply-intake-on-submit` Edge Function ruled out (does not create auth users). Fix: optional `metadata` param POSTed as `user_metadata`; new `api_buildAuthUserMetadata_` centralizes the AuthContext 4-key contract (role/clientSheetId/accessibleClientSheetIds/childClientSheetIds + clientName) — corrected the original `tenantId` hypothesis (RLS uses `clientSheetId`). All 5 auth-create sites stamp it (api_upsertClientUser_, handleCreateUser_, handleEnsureAuthUser_, handleAdminSetUserPassword_ create branch, helper). Existing-73 remediation: 422 branch self-heals via `api_backfillAuthUserMetadata_` (idempotent, only when no role — never clobbers live login-synced metadata). childClientSheetIds always [] (no RLS keys off it; parent scope still resolved by AuthContext on login). Two-pass Opus review via `.claude/agents/code-reviewer.md` checklist (registered subagent unavailable): pass 1 flagged missing childClientSheetIds → resolved, final SHIP. node --check clean. StrideAPI v38.222.0→v38.223.0. Squash-merged #459, deployed via npm run push-api && npm run deploy-api.

**2026-05-19 — Delivery-order piece count double-counted transferred items (`fix/delivery/dt-transferred-doublecount`, React-only).** `CreateDeliveryOrderModal.selectedInvItems` filtered raw `liveItems` by `itemId`; after a transfer the same `item_id` exists as an Active row (receiving tenant) + a Transferred row (originating tenant), and the staff/admin `useInventory` fetch is un-scoped so both were in `liveItems` → every transferred item counted twice → inflated `itemCount`/`extra_items_count` → over-billed extra pieces (ALL-00100-FAHRINGER: `extra_items_count` 9 vs 3). Picker never showed the dup (renders `activeItems`, status==='Active'+client). Fix: `selectedInvItems` resolves against `activeItems` with a defensive itemId de-dupe Map (each pick → exactly the Active row; `inventory_id`/`location` from the Active row). Threaded `inventoryRowId` UUID through `InventoryItem`/`useInventory.mapToAppItem`+batch/`liveItems` map — was dropped by `mapToAppItem`, so `dt_order_items.inventory_id` had been null on the self-fetch path (also hardens OrderPage Release-Items FK). Behavioral note: re-saving a historical order whose item later went Released/Transferred now excludes it from the recompute — intended per the bug; resubmit-diff banner surfaces the change. Opus review via `.claude/agents/code-reviewer.md` checklist (registered subagent unavailable): no Critical; the one Important (edit-load exclusion) is the explicitly-requested behavior. tsc + `npm run build` clean. Files: `src/lib/types.ts`, `src/hooks/useInventory.ts`, `src/components/shared/CreateDeliveryOrderModal.tsx`.

**2026-05-20 — Impersonation fidelity, full series (PRs #472 / #473 / #474, React + Supabase).** Closed the long-standing "impersonation isn’t what the client actually sees" gap in three sequenced PRs. **PR #472 (shipped, deployed live):** namespaced every list-view filter localStorage key by `user.email`. New `src/lib/userScopedStorage.ts` (`userScopedKey`, `migrateLegacyKey`) consumed by `useClientFilterPersisted` + Claims/Shipments status-chip filters; one-shot legacy-key migration runs in BOTH the useState initializer AND the rehydrate effect so cold-start auth-loading still ports admin selections forward; `lastWriteUserRef` skips the stale-closure write that fired once after `userEmail` flipped. **PR #473 (shipped, deployed live):** moved table prefs to Supabase. New migration `20260520180000_user_view_prefs.sql` (table keyed `(user_email, page_key) unique`, RLS self r/w + admin-staff read-any, data-API grants, updated_at trigger). New `src/lib/userViewPrefsClient.ts` (fetch / upsert / debounced 250ms scheduler with per-(email,page) timer / beforeunload flush). `useTablePreferences` refactor: localStorage stays as first-paint cache + offline write-cache, Supabase load fires async, `serverHydratedRef` blocks first-mount write from racing the server fetch, `userEditedRef` protects mid-fetch column drags, `isImpersonating` makes writes a deliberate no-op. **PR #474 (this session, ready to ship):** true Supabase-session impersonation. New migration `20260520200000_impersonation_log.sql` (audit table: admin/staff + target-self SELECT-only RLS; INSERT/UPDATE only via service_role through the edge function). New edge function `impersonate-mint-session` (deployed v2 ACTIVE): admin role re-check on bearer JWT, lookup target in `cb_users` via `.eq`, insert audit row, mint magic-link OTP via `supabase.auth.admin.generateLink`; `end` action stamps `ended_at` on most-recent open row. AuthContext rewired: `realUser` decoupled from authState (kept as admin during impersonation via stash + mirror-effect gated on `getImpersonationFlag()`); `impersonateUser` stashes admin session tokens + AUTH_CACHE_KEY into sessionStorage → edge fn → `verifyOtp` swaps live JWT to target → `resolveUserFromApi(target)` → setAuthState. `exitImpersonation` reads stash → `setSession(adminTokens)` swaps back → restores admin cache → `endImpersonationEdge` closes audit row (admin JWT is back). `impersonationSwapRef` makes `handleSession` + `onAuthStateChange` bail during swaps so they don’t wipe AUTH_CACHE_KEY underneath us. Refresh-during-impersonation handled (realUser initializer hydrates admin from stash; bootstrap skips localStorage hydration when FLAG_KEY is set). Sign-out clears all impersonation state. Cleanup migration `20260520200100` drops the now-redundant `user_view_prefs_admin_read` policy (admin holds client JWT during impersonation, self policy covers it). Removed `setSupabaseImpersonating`/`_impersonating` cache-bypass from `supabaseQueries.ts`. Code-review iteration on each PR (Opus 4.7 locked-in reviewer) caught + fixed: PR #472 cold-start migration race, PR #473 initial-mount write race + mid-fetch user-edit clobber + impersonation write guard, PR #474 orphan-audit-row on resolveErr (now closes audit) + documented rare exit-failure orphan (admin refresh token mid-session) with operator cleanup SQL. tsc + `npm run build` clean across all three PRs.


**2026-05-20 — [MIGRATION-P1.9] Live audit-shape shadow firing wired into apiPost for 20 GAS actions.** Closes the MIG-014 backlog item — only `startTask` fired live shadows before (`completeTask` parity volume came from operator-run replay-shadow, not live). New `src/lib/shadowRegistry.ts` (20 entries: updateInventoryItem, updateTask* ×4, updateRepairNotes, startTask, startRepair, cancelRepair, sendRepairQuote, respondToRepairQuote, requestRepairQuote, completeTask, completeRepair, batchCreateTasks, createWillCall, processWcRelease, releaseItems, transferItems, commitStorageRows, reissueInvoice, completeShipment, onboardClient — each with flag key + shadow EF name + audit-shape derivation + callId derivation), new `src/lib/fireShadow.ts` (fire-and-forget wrapper around runShadow using synthesized audit shape as the GAS-side baseline), one-line hook in `apiPost` (`src/lib/api.ts`): `fireShadow(action, bodyWithId, extraParams?.clientSheetId ?? null)` after every successful GAS call — no-op for unregistered actions. **startTask 41/41 artifact fix:** retired the `apiCall(...start-task SB primary...)` wrap in TaskDetailPanel that ran the SB primary as the shadow second (GAS already started the task), producing different response shapes. New audit-shape comparator against `start-task-shadow` is deterministic from payload (`{status:{new:'In Progress'}}`) independent of GAS state. Per-function `toAuditShape` overrides mirror each in-source shadow's exact `.changes` shape (fixed-shape: startTask/startRepair/cancelRepair/sendRepairQuote; complex-shape: completeTask `{status:{old,new}, result:resultValue||''}`, respondToRepairQuote `{decision, status:{new: Approved|Declined}}`, requestRepairQuote `"Repair quote requested for items: " + JSON.stringify(arr).substring(0,200)`); default (payload minus identifiers) for the 10 MCP-deployed shadows where source isn't in-repo. Opus 4.7 code review via `.claude/agents/code-reviewer.md` checklist (registered subagent unavailable) caught + fixed before merge: updateInventoryItem strip-set was DEFAULT (broader) instead of the shadow's actual `{itemId, requestId}` → narrowed; requestRepairQuote was sort+comma-join vs the shadow's exact JSON-substring concat → mirrored exactly (would have flipped 9/0 clean → 9/9 mismatch); completeRepair's `p.result` fallback diverged from shadow's resultValue-required validation → dropped; defensive try/catch around fireShadow's sync spec derivation so a buggy `toAuditShape` never propagates into apiPost's success return. Rebased onto `origin/source` after PR #479 (client-settings writeback) merged. Not wired (no shadow EF deployed yet): createInvoice, voidInvoice, qboCreateInvoice, createStaxInvoices, runStaxCharges, importIIF, generateUnbilledReport — P4a/P6 prereqs. Email flags intentionally skipped — fire as side-effects from host handlers. New decision MIG-015. tsc + `npm run build` clean.

**2026-05-20 (later) — DO modal REFERENCE column + doc-template print-CSS parity (PRs #475, #476).** Two small fixes on the same day after the impersonation-fidelity series. **#475:** `CreateDeliveryOrderModal.tsx` self-fetch path (when opened from Orders without `liveItemsProp`) mapped `useInventory().items` into the `LiveItem` shape but dropped the `reference` field, so the item picker's REFERENCE column rendered `—` for every row even when the inventory row had a populated reference / PO number. One-line additive fix + `reference?: string` on `LiveItem` interface + comment block above the map flagging it as the choke point for field-drop bugs (prior `inventoryRowId` 5/19 miss + today's `reference` miss as worked examples). Picker-display-only — no `dt_order_items` insert path references the new field. Opus 4.7 review confirmed scope. Deployed live. **#476:** The 2026-05-13 print-CSS-parity migration had touched only Task + Repair Work Order templates, leaving `DOC_RECEIVING` with hardcoded `table{width:8in}` + no `@page` rule (printed content overflowed ~7.5in Letter printable area). Repair + Task also still carried `<div style="width:8in;margin:0;">` outer wrappers the 5/13 fix didn't touch. Three of the four docs also carried a `<span>Stride Logistics </span><span>WMS</span><br>` title block that could wrap "Stride Logistics" / "WMS" onto two lines under tight column widths (DOC_WILL_CALL_RELEASE already shipped with the right `<div white-space:nowrap>` structure). Two new migrations applied via MCP: `20260520220000` (extend @page + box-sizing + width:100% to Receiving, replace title block on the three non-WC docs, tighten logo→text gap 10→8px) + `20260520220100` (drop residual 8in wrapper on Repair + Task). Both end in `DO $$` assertion blocks that abort on partial state. Critical realization mid-deploy: `api_getDocTemplateHtml_` in StrideAPI.gs reads from Supabase first via `api_getTemplateFromSupabase_` (sheet cache is fallback only), so **no GAS redeploy needed** — future GAS-generated Docs-tab PDFs auto-pick up the fixed template. Same for the React Print-button path via `workOrderPdf.ts:fetchTemplate`. Historical PDFs in `public.documents` Storage remain as frozen blobs.

**2026-06-01 — Storage preview re-showed already-invoiced storage (STOR-SUMMARY blind-spot) + Unbilled/Invoiced filter + transfer harden (PR #589, SB migration + React).** Justin: after invoicing storage (Allison Lind, 10 sidemarks), the same line items kept reappearing on the Storage tab — unlike the Unbilled report. Root cause: `_compute_storage_charges` deduped against already-billed periods by matching `billing.item_id` + `STOR-{item}-{dates}` task_id, but the v38.256 commit collapses invoiced per-item STOR rows into ONE `STOR-SUMMARY-{tenant}-{sidemark}-{dates}` row (blank item_id, non-per-item task_id) — invisible to that dedup; the per-item invoiced periods moved to `storage_billing_items`, never queried by the preview. Broke the documented invariant (Billing.tsx "the RPC excludes already-invoiced periods"); commit path independently double-gated (`handleCommitStorageRows_` lockedSidemarks + storage_billing_items sbiAlreadyBilled) so no actual double-bill occurred. Migration `20260601120000_storage_preview_dedup_summary_transfer.sql` extends `_compute_storage_charges`'s `v_billed` with finalized `storage_billing_items` ranges + sidemark-matched `STOR-SUMMARY` periods + unbilled `STOR-TRANSFER-*` backfill (the harden for a destination item whose transfer_date is blank so the cutover can't fire). `generate_storage_charges` shares the engine so the commit gains the narrowing too (strictly protective). React: Unbilled/Invoiced View toggle on the Storage tab — `fetchInvoicedStorageItems` reads `storage_billing_items` for a read-only itemized invoiced view (the per-item detail behind a collapsed STOR-SUMMARY line), decoupled from commit. Validated via test-copy diff across ALL tenants: May excludes EXACTLY the invoiced storage (Allison Lind $2,419.40 = sum of INV-020125..134, KIPP $2.40, ISLAND PARK $36), June byte-identical ($57,551.48), ZERO rows introduced (subtract-only). Post-apply: FAHRINGER May preview → 0. Opus code-review (locked-in): no Critical/Important. tsc + `npm run build` clean. Migration applied via Supabase MCP + verified; React deployed via canonical clone.

**2026-06-02 — Storage commit/regen delete-passes preserve the transfer backfill (PR #595, GAS v38.258.0).** Follow-up to PR #589, surfaced by Justin asking where transfer storage charges live. Established: the transfer **backfill** (pre-transfer holding days, `STOR-TRANSFER-{item}-{receive}-{transfer-1}`, created Unbilled on the destination) belongs on the **Unbilled report**; the **monthly** storage (transfer→end) is the **Storage report** projection — disjoint windows split at the transfer date, so no double-bill (proved on item 63558: backfill 5/20–5/26 vs preview 6/01–6/03, zero shared days; the cutover refuses to project pre-transfer days even when the query window spans them). BUT found a latent footgun: both `handleCommitStorageRows_` (~24960) and `handleGenerateStorageCharges_` (~24629) deleted EVERY in-window Unbilled STOR row before writing the summary/regenerated rows (svc_code=STOR + Unbilled + date-in-window) — the backfill matched but the transfer_date-forward summary never re-creates it, so committing a month's storage while that month's backfill was still Unbilled would silently delete it (mirrors to public.billing → revenue loss). Fix: both loops `continue` on `ledger_row_id LIKE 'STOR-TRANSFER-%'`, sparing only the backfill (per-item + STOR-SUMMARY rows still deleted/replaced). No double-bill (cutover keeps it disjoint; once Invoiced the dedup subtracts it). Dormant at deploy (0 Unbilled STOR-TRANSFER rows). Opus code-review: no Critical/Important (confirmed `continue` lands in outer for, only STOR-TRANSFER-* spared, the backfill's canonical Task ID could ALSO have triggered the newStorTaskIds delete branch — guard precedes both). `node --check` clean. GAS deployed via push-api && deploy-api from canonical clone.

**2026-06-02/03 — SB task-completion incident (feature-flag misflip) + forward fix (PR #596, edge functions).** Justin: completing inspection 63880 (Michelle Dirkse) flips to Pass then reverts to In Progress on refresh, and no inspection email sends. Root cause NOT code: on 2026-06-01 evening PT, 9 migration `feature_flags` (completeTask, startTask, transferItems, releaseItems, updateItem, updateTask, createWillCall, processWcRelease, releaseWillCall) were flipped fleet-wide to Supabase by **nulling `tenant_scope`** (vs documented canary=Justin Demo only), routing real tenants (Michelle Dirkse, Seva Home, Lisa Sherry, Cohesively Curated) onto the in-progress SB task path. Two SB defects: (1) `complete-task` was a dry-run that never sent INSP_EMAIL/TASK_COMPLETE; (2) ~47% of completions reverted because `start-task`'s fire-and-forget GAS `action=startTask` write-back re-asserts `status='In Progress'` via `api_notifySupabase_` and lands after a quick `complete-task`. Mitigation: reverted all 9 flags to canary scope (incident note on each). Forward fix PR #596 (edge-function-only, canary-only): `complete-task` now SENDS the email via `send-email` (mirrors complete-repair-sb), gated on `clients.enable_notifications` (null⇒no-send, matches GAS `toBool_`), adds SVC_NAME/SHIPMENT_NO/REPAIR_NOTE tokens, deduped on `task-complete:{taskId}:{result}`, shadow-safe → **complete-task v2**; `start-task` GAS sheet sync switched from `action=startTask` to cell-level `writeThroughReverse` `table:'tasks'` (writes only Status/Started At/Assigned To, never writes back to public.tasks, can't clobber a later completion) → **start-task v3**; `api.ts` additive emailSent/emailError/emailSkipped. Locked-in Opus code review: 0 Critical (no email double-send, no billing/counter regression, race eliminated); 2 Important addressed (enable_notifications null fail-safe, stale dry-run comment). tsc + `npm run build` clean. Deployed via supabase CLI from canonical clone (verify_jwt=true preserved). Re-cutover rule: re-scope flags deliberately, **NEVER null tenant_scope**. 6 inspections stuck In Progress from the incident pending re-completion (safe — GAS bills idempotently on same ledger id). New memory: `project_task_completion_flag_landmine`.

**2026-06-03 — Storage Invoiced view shows + exports per-item billable days (PR #597, migration + GAS v38.259.0 + React).** Justin viewing the Invoiced view (PR #589) saw "—" under DAYS for every item + no per-day count in the export — clients need proof of how many days each item was billed. Root cause: `storage_billing_items.billable_days` was ALWAYS null (`handleCommitStorageRows_` wrote amount + rate but never the day count). For storage `amount = daily_rate × days` exactly, verified across all 2456 rows (every rate>0, every amount/rate an integer). Fix: (1) migration `20260603120000_backfill_storage_billable_days.sql` backfills `billable_days = ROUND(amount/rate)` for the 2456 null rows (idempotent + assertion; applied via MCP, verified INV-020190 → 1/1/1/1/1/1/4/20/20/2/2/5); (2) `handleCommitStorageRows_` grpSbiRows now persists billable_days (preview qty else round(amount/rate)) so new rows carry it; (3) Billing.tsx `invoicedStorageDays()` feeds the DAYS cell (derives when null) + the Invoiced-view Export emits a dedicated client-proof CSV (Client/Sidemark/Item ID/Description/Period Start/Period End/Billable Days/Daily Rate/Amount/Invoice #/Invoice Date). Flagged cutover blocker in MIGRATION_STATUS: SB commit path (commit-storage-charges-sb → generate_storage_charges) writes only public.billing, not storage_billing_items — flipping commitStorageCharges reintroduces this + loses per-item tracking; port the SBI write incl. billable_days into the EF first. Opus code-review: no Critical/blocking (GAS fallback == migration formula, billable_days key always present so no batch-shape fracture, sbiActive fail-safe + totals untouched, no landmines). tsc + build + node --check clean. Migration via MCP; GAS push-api/deploy-api + React npm run deploy from canonical clone.

**2026-06-03 — Real .xlsx export for Billing report + Storage (preview & invoiced) (PR #599, React).** Justin asked for a genuine Excel export on both reports; the shared toolbar "Export xlsx" button (renderTable, shown on the Billing report + Storage tabs) was actually emitting CSV — mislabeled. New `src/lib/exportExcel.ts` `downloadRowsAsExcel(rows, sheetName, filename)` via SheetJS (`xlsx`, already a dep; pattern from exportPriceListExcel.ts — json_to_sheet + auto-sized cols + writeFile, sheet name sanitized). Billing.tsx export button now builds clean row objects per active view and produces a real workbook: Billing report (COL_LABELS business columns, internal IDs dropped) → stride-billing-report.xlsx; Storage Unbilled preview (Client/Sidemark/Item/Description/Class/Period/Billable Days/Daily Rate/Amount) → stride-storage-preview.xlsx; Storage Invoiced (client-proof itemized w/ Billable Days) → stride-invoiced-storage.xlsx (supersedes PR #597's CSV). Numbers stay numeric (no toFixed coercion) for Excel sort/sum. Removed unused toCSV helper (other pages keep their own; COL_LABELS still used). Opus code-review: no Critical/Important — export-only read path, no billing writes, no landmines, no COL_LABELS collision, SheetJS matches shipped call sites. tsc + build clean. React deployed via canonical clone.

**2026-06-03 — Dashboard: inspection due-date drift + tablet open (PRs #601, #602).** Justin: (1) every open inspection showed due 6/05 regardless of receipt date — "work next" ordering useless; (2) double-click to open a task works on PC but not a tablet. **Due dates:** ~729 INSP tasks (all statuses, created across 41 days) were pinned to today+48h. Root cause: `tasks_auto_stamp_due_date` filled blank `due_date` with `COALESCE(NEW.created_at, NOW())+SLA`; GAS `sbTaskRow_` resyncs send `created` (text) but OMIT `created_at`, so the resync UPSERT's INSERT phase defaults `NEW.created_at` to now(), the BEFORE-INSERT trigger stamps today+SLA, and ON CONFLICT propagates it onto the existing row (stored created_at unchanged). So every START/sync of a blank-due INSP task (auto-created INSP land blank Due Date) drifted to today+48h; a builder's one-off bulk SQL seeded the initial corruption, the trigger made it recurring (caught it reverting INSP-63628-1 14s after a GAS start). Confirmed the per-tenant sheets are BLANK (read via getTasks webapp) → corruption was SB-side, sheet untouched. Fix migration `20260603200000_tasks_due_date_base_on_created`: base SLA on `NEW.created` (upsert-stable), not created_at; High paths unchanged; one-shot heal of open/in-progress rows. Verified: null a 5/26 task → 5/28 not 6/05; all open inspections show created+SLA. New memory `project_auto_stamp_trigger_created_at_landmine`. **Tablet:** touchscreens fire onClick on tap but not a reliable native dblclick → `Dashboard.tsx` TasksTab row opens on single tap when `matchMedia('(pointer: coarse)')` (mouse keeps select+double-click); only TasksTab used the select+double-click pattern. Both applied/deployed; migration via MCP, React npm run deploy from canonical clone.

**2026-06-03 — Create Tasks modal no longer pre-selects INSP (PR #603, React).** Justin: creating a task (e.g. from the Inventory screen) had Inspection (INSP) auto-selected, so an operator who didn't notice it was checked could accidentally create an unwanted INSP task. Root cause: `CreateTaskModal.tsx` (~line 64) initialized `selectedCodes` to `new Set(['INSP'])`. Fix: initialize to empty `Set()` — operator must actively pick at least one type. Empty-selection was already safe (Create button disabled on `!selectedCodes.size`, handleSubmit early-returns); also cleaned the disabled-button label to "Create Tasks" (was "Create  Tasks") when nothing selected. Shared modal covers all 3 entry points (Inventory/ItemPage/ShipmentDetailPanel), none seed selectedCodes so fully encapsulated. Opus code-review (locked-in checklist): no Critical/Important/Nits — traced all 14 selectedCodes refs, no mount-time effect assumed the old default, no landmines (pure client-side React). tsc + build clean. React deployed via canonical clone.

**2026-06-03 — Repair Work Order items table gains a Location column (PR #608, all 6 renderers + 2 template stores).** Justin asked to show the item's warehouse Location on the repair WO. Two code-review rounds revealed the doc is rendered by 6 paths across 2 template stores: Supabase `email_templates` (React docTokens.ts buildRepairTokens; StrideAPI handleStartRepair_ + handleRespondToRepairQuote_) AND the MPL Email_Templates sheet (Triggers.gs SH_generateRepairWorkOrderPdf_; Repairs.gs generateRepairWorkOrderPdf_; standalone task board script.txt renderer, deployed via push-taskboard). Any renderer not filling {{ITEM_LOCATION}} would emit a literal token once its template gained the column — so all six + 3 hardcoded fallbacks were updated. Migration 20260603140000 (Supabase template, idempotent), React (location on RepairWorkOrderInput + token from repair.location), StrideAPI v38.261.0, client scripts CI v4.8.0 / SHARED_HANDLER_VERSION 1.2.0 (Triggers + Repairs renderers + SH_getDefaultRepairWorkOrderHtml_ + Emails getDefaultDocHtml_ fallbacks, .location from header-mapped helpers), Task Board v1.8.0, Doc Templates/*.txt sheet-source (push-templates). Task-WO fallbacks left untouched (verified 0× token). Deploy order: ALL renderers (push-api/deploy-api, rollout/deploy-clients ×49, push-taskboard, React) BEFORE templates (push-templates + apply_migration) — no literal-token window. Round-1 review caught the client-script renderers, round-2 the Task Board; exhaustive "Unable to Repair" grep confirmed all 6. node --check + tsc + build clean.

**2026-06-04 — Intake-completion alert to office inbox only, not full staff (PR #610).** Justin: the client-intake completion email (INTAKE_SUBMITTED template) was blasting all 14 active admin+staff (neeko@/jon@/cel@/travis@/whse@/demitrie@/ken@/andrew@/adielle@/evan@/dispatch@/kc@/info@/justin@). Traced to email_sends a4209f93 (2026-06-03 17:29 PDT). Root cause: migration 20260424090000 set this template's `recipients` to `{{STAFF_EMAILS}}`, which send-email/index.ts expandToken resolves to `profiles WHERE role IN ('admin','staff') AND is_active` — the whole warehouse roster. Intake completion is an office concern. Fix migration `20260604000000_intake_submitted_office_only_recipient`: `recipients` → literal `info@stridenw.com` (admin-editable from Settings → Email Templates; literal, not a token, so role/seat churn can't re-inflate it). Same pattern as PR #459's repair-quote fix. React caller (useClientIntake.ts) passes no `to`, so the edge function reads the column directly — verified resolveRecipients keeps a literal as-is; only updated two stale `{{STAFF_EMAILS}}` comments (bundle byte-identical, no React redeploy). Confirmed no other intake-submission email path (the AFTER-INSERT trigger only writes in_app_notifications; legacy GAS handleNotifyIntakeSubmitted_ removed in v38.121.0). Migration applied to prod + recorded in schema_migrations; live as of 2026-06-04 15:12 UTC. Opus code-review caught the second stale comment. tsc + build clean.
