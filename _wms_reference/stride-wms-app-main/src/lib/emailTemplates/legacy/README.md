# Legacy alert templates

Legacy trigger/template compatibility is tracked in the database registry:

- `public.communication_trigger_catalog.is_legacy`
- `public.communication_trigger_catalog.legacy_replacement_key`
- `public.communication_legacy_trigger_registry`

This folder is reserved for legacy template snapshots or migration artifacts if physical template files need to be preserved during future trigger deprecations.
