# Stride GS App — Builder Instructions

The canonical builder guide lives one level up at [`../CLAUDE.md`](../CLAUDE.md).

This file used to be a duplicate kept for IDE subdir opens, but Claude Code already
walks up the directory tree to find a parent `CLAUDE.md`, so a pointer is enough
and removes the drift risk. **Edit only `../CLAUDE.md`.**
